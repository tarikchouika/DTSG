# خطة الإصلاح v2.44 — حصّ الأسباب وتقسيم المراحل

> مبنيّة على قياسات فعلية (متصفح Chromium حقيقي + A/B على المحرك + استعلامات API)، لا على تخمين.
> تاريخ: 2026-09-18 · نطاق الجلسة صغير ⇒ كل مرحلة تُسلَّم وحدها (نشر + تحقق) قبل التالية.

---

## 0) نتيجة الحصّ — السبب الجذري لكل عطل

### ع1) بوت الڤوتشير: «❌ bad-input» دائماً
- **الدليل**: `/api/health` على خادم الهاتف = `build 2.42.1` (قياس مباشر هذه الجلسة)، والحقل `missing` يعود من `cf-worker/payments-core.js`
  المضمَّن في **خادم الهاتف**، لا من الصفحات المنشورة ⇒ طبقة التسامح (v2.43) **غير منشورة على الهاتف**.
- **السبب**: `server-payments.js` يمرّر المسار إلى `core.handleFetch`، والملف يعيش على الهاتف؛ تحديث Pages لا يُحدّثه.
- **الفجوة الوظيفية**: التدفّق المطلوب (يختار المستخدم/الأدمن وسيلة الدفع والمبلغ والدليل ← إشعار السوبر ← مصادقة حصرية ←
  **إنشاء كود تلقائي** ← شحن عند الاستخدام بالدولار + الكوينز + **بونص الشريحة تلقائياً**) غير موجود: الكود يُنشأ يدوياً بأمر
  `/voucher` من السوبر فقط، ولا يوجد مسار «طلب شحن عبر البوت → مصادقة → كود».
- **يحتاج**: (أ) نشر `payments-core` على الهاتف، (ب) بناء التدفّق الكامل + جدول `vouchers` بالبونص + سجل.

### ع2) خلل الرصيد: الدولار يتغيّر ثم يعود، والكوينز ثابتة، ولا سجل للمعاملات
- **الدليل المقاس**: `__creditUsd(id,usd)` → `creditGoldLocal` → `users.gold += usd×100` (source of truth صحيح)، لكن مسار
  «اعتماد الأدمن/السوبر» في `server-payments.js` لا يحمل صلاحية كافية للتأكد، و**لا يوجد أي سجل تدقيق** يُظهر ما حدث.
- **السبب المؤكَّد جزئياً**: العرض الدولاري في الواجهة مشتق من `gold/rate` بلا حفظ مستقل ⇒ أي مسار يشحن «دولاراً» خارج `creditGoldLocal`
  (أو يخصم ثم يُعاد الحساب من `gold` القديم) يُظهِر رقماً ثم يعود عند التحديث.
- **ناقص تماماً**: سجل الشحن/السحب للمستخدم **وللأدمنز** في داشبورد السوبر أدمن (لا تبويب ولا جدول).
- **يحتاج**: مصدر حقيقة واحد للشحن (بالكوينز) + ترحيل `balance_usd` محسوباً + **سجل تدقيق `pay_audit`** يعرض كل عملية
  (من، لمن، ماذا، الوسيلة، الحالة، من صادق) + تبويب في داشبورد السوبر.

### ع3) بارتشي: خانات مستطيلة مشوّهة/محشورة
- **الدليل الرقمي**: الخانة القديمة **64 × 27.5 = نسبة 2.33:1**؛ الجديدة **93.33 × 30 = نسبة 3.11:1** (+34% استطالة)
  مع هامش داخلي 3px فقط ⇒ الأعمدة 30px والطول 93 ⇒ تبدو أشرطة ملتصقة.
- **السبب**: تغييري في v2.43 كبّر PR_BOARD/PR_B وألغى هوامش اللوحة، فأصبحت الخانة أكثر استطالة.
- **الحل**: **تحويل أفيني موحّد على الهندسة الكلاسيكية القديمة** (مقياس 600/580 ≈ 1.0345 مع إزاحة −10)
  ⇒ اللوحة تبقى flush (بلا هامش) والخانات تعود بنسبها 64:27.5 والمركز 140 والأذرع 8 صفوف.

### ع4) الشطرنج: بيادق شفافة/مشوّهة
- **الدليل من الكود**: CSS يستعمل `fill: url(#chGradW|#chGradB)`، ودالة `chessInjectDefs()` **غير مُناداة من أي مكان**
  ⇒ مرجع التدرّج غير موجود ⇒ `fill` يفشل ⇒ القطع تُرسم **بحدود فقط (شفافة)**. مطابق تماماً للقطة المستخدم.
- **الحل**: مناداة `chessInjectDefs()` عند بناء اللوحة (واحتياط: تعبئة صلبة إن فشل الـSVG defs) + تحقّق بصري بدقة 3x.

### ع5) ضاما: «البوت صار غبياً + قانون النفخ تضرّر»
- **قياس موضوعي A/B (قديم v2.42.1 مقابل جديد v2.43)**: زمن البحث متطابق (2760ms عند عمق 10 = نفس الميزانية)،
  ونتائج الأسر المادي متكافئة (1.8 مقابل 1.4 — داخل هامش الخطأ) ⇒ **لا تدهور في قوّة المحرك**.
- **لكن**: سلوك الاختيار تغيّر في افتتاحيات كثيرة (5/6 مواضع: نقلة أولى مختلفة) لأن قاعدة النفخ/الإلزام تغيّرت.
- **السبب الجذري**: توثيق المالك داخل الكود يقول: «١) **الضائم (الملك) يُلزَم قبل البيدق** ٢) الأطول يُلزم قبل الأقصر
  ٣) البيدق ملزم بإتمام السلسلة وإلا نُفخ»، **وقد عكستُ الأولوية في v2.43** (الأطول أولاً) وغيّرت شرط النفخ من
  «لم يأكل بالمُلزَم» إلى «قصر الأكل» ⇒ هذا هو «تضرّر قانون النفخ» الذي يراه اللاعب.
- **الحل**: إعادة تطبيق **قاعدة المالك كما هي موثّقة** (الملك أوّلاً ثم الأطول ثم الأكثر أسراً) مع إبقاء الاستثناء المشروع
  الذي شكا منه سابقاً بصيغة أدقّ: **لا يُنفخ من أكمل أطول سلسلة متاحة إن كان هو نفسه صاحب أطول سلسلة**؛
  ثم اختبارات قاعدة صريحة (نفخ/إتمام/ملك-بيدق) بمصفوفة حالات.

### ع6) سجل الشحن/السحب في داشبورد السوبر أدمن (مفقود)
- **الدليل**: لا تبويب ولا جدول؛ `pay_transactions` موجود لكن لا عرض موحّد للمستخدمين والأدمنز.
- **الحل**: تبويب «السجل المالي» للسوبر: كل العمليات (إيداع/سحب/كوبون/تحويل بين الحسابات) مع المُراجِع والوقت والوسيلة،
  وفلترة (معلّق/مكتمل/مرفوض) ومرشّحات بالمستخدم/التاريخ.

---

## 1) المراحل (كل مرحلة قابلة للتسليم وحدها)

| # | المرحلة | المحتوى | الاختبار/القبول | الحجم |
|---|---------|---------|------------------|-------|
| **م1** | **الواجهة (سريع، بلا باك)** | شطرنج: هجمة `chessInjectDefs` + تحقّق بصري 3x · بارتشي: تحويل أفيني للهندسة الكلاسيكية (flush) | لقطة 412×915 بدقة 3x + `_parchisi_layout_v243` محدّث + `_parchisi_browser` | صغير |
| **م2** | **ضاما: إعادة قانون المالك + تصليب الذكاء** | `obligationPiece` (ملك ← أطول ← أكثر أسراً) + شرط النفخ الصحيح + `obligedMax` بمصدر واحد + اختبارات قاعدة صريحة + A/B للقوة | `_dama_engine` + `_dama_fuzz` + اختبار نفخ جديد (مصفوفة 6 سيناريوهات) + `_dama_browser` | متوسط |
| **م3** | **المال: مصدر حقيقة واحد + سجل تدقيق** | `creditGoldLocal` كمصدر وحيد (USD = gold/rate) + جدول `pay_audit` + كتابة كل عملية (إيداع/سحب/كوبون/إنشاء كود/مصادقة) + تبويب «السجل المالي» للسوبر (مستخدمون + أدمنز) | `_pay_v243` + `_admin_payments_ui` + اختبار جديد للسجل (`_pay_audit_test`) + تحقّق حيّ: شحن ← رصيد ← تحديث الصفحة ← ثبات | كبير |
| **م4** | **بوت الڤوتشير الكامل** | (أ) نشر `payments-core` على الهاتف · (ب) تدفّق: `/start` ← ربط الحساب ← «شحن» ← اختيار الوسيلة ← المبلغ ← **صورة/رقم الدليل** ← إشعار السوبر بأزرار **مصادقة/رفض** ← عند المصادقة **إنشاء كود تلقائي بطول المبلغ** ← إشعار المستخدم بالكود · (ج) الاستخدام: كود ⇒ دولار + كوينز + **بونص الشريحة تلقائي** + سجل | اختبار خلفي كامل بلا شبكة (`_voucher_flow_test`) + تحقّق حيّ على البوت بعد النشر | كبير |
| **م5** | **نشر + تحقّق حيّ نهائي** | Pages + الهاتف + جولة تدقيق متصفح على dtsg.pages.dev واللعب الحقيقي | 8 صفحات بصفر أخطاء + جولة لعب لكل لعبة معدَّلة | متوسط |

**قاعدة التسليم**: لا أنتقل لمرحلة تالية قبل: (١) اختبارات خضراء، (٢) لقطة/تحقّق بصري حيث يلزم، (٣) كوميت + دفع،
(٤) تحديث `CHANGELOG.md` و(إن مسّ الهاتف) تعليمات `PHONE_UPDATE_v244.md`.

---

## 2) ما تحتاجه أنت (لا يمكن فعله من هنا)
1. **تحديث الهاتف** بعد م3/م4: `bash scripts/apply-support-now.sh` (سأحدّثه ليجلب ملفات v2.44).
2. **توكن بوت الڤوتشير** كمتغير بيئة على الهاتف فقط (لا يُخزَّن في المستودع).
3. **معرّف السوبر أدمن** للتأكيد الحصري (المعروف: 5700612979).
