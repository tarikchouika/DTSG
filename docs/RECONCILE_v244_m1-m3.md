# 🔀 مصالحة v2.44 — خط main مقابل خط المراجعة (م1–م3)

> **الخلاصة أولاً:** خط المراجعة (`4047a78 → 621b1f2 → 6900d30`) **غير موجود على main**
> (أُزيح بـforce-push سابق). أُعيد استرجاعه كاملاً وحُفظ على GitHub في فرع مسمّى:
> **`arena/v2.44-m1-m3` = `bdb6769`**. الفحص المتبادل يُظهر: **main أخضر بالكامل** (698 تأكيداً)
> والفرق الحقيقي بين الخطين في **ثلاث نقاط قرار** — لا في أعطال.

---

## 1) حالة الخطّين

| | main | خط المراجعة |
|---|---|---|
| HEAD | `1d3cbcd` | `bdb6769` (6900d30 + إصلاح اختبار pay_audit) |
| المنشور | **dtsg.pages.dev = 46e87aca** (v2.44) | غير منشور |
| الأجنحة | **705 ناجح / 0 فاشل** (11 ملفاً — أُضيف اختبار وضع القانون) | engine 85/0 · fuzz 459/0 · pay_audit 39/0 · cf 60/0 |
| البوت (فوتشير/[KOD]) | ✅ منفَّذ ومختبَر حيّاً (16/16) | ❌ غير موجود على الفرع |
| pay_audit | ❌ غير موجود | ✅ جدول + دالة + فلاتر |
| شطرنج | تدرّج + حقن defs (متحقَّق حياً) | تعبئة صلبة (بلا تدرّج) |

> ⚠️ **لا دمج أعمى في أي اتجاه**: تغطية الملفات متداخلة
> (`cf-worker/payments-core.js` · `server-payments.js` · `server.js` · `js/main.js` · `js/games/dama.js`)
> وكل خط ينقصه ملفات الخط الآخر.

---

## 2) ما تحقّق فعلياً على main (بعد المصالحة)

```
_dama_engine_test.js        84/0
_dama_fuzz_test.js         459/0     ← كان 451/8 قبل FIX2
_cf_payments_test.js        60/0
_money_v244_test.js         20/0
_bot_v244_test.js           16/0     ← فوتشير كامل حيّ (ربط→طلب→سوبر أدمن→كود→تفعيل)
_chess_v244_test.js         15/0     ← +3 فحوص جديدة (مراجع تدرّج معلّقة · صناديق صفرية · شفافية)
_parchisi_v244_test.js       7/0
_dama_v244_test.js          10/0
_dama_browser_v244_test.js   8/0 حيّ
_admin_payments_ui_test.js  19/0     ← كان 11/8 قبل الإصلاح
_dama_obligation_mode_test.js 7/0    ← جديد: مفتاح قانون النفخ مؤثّر (overall ⟷ piece)
```
> **وتحت الوضعين**: `DAMA_OBLIGATION=piece node tests/_dama_fuzz_test.js` ⇒ **459/0** (مثل الافتراضي).

### الجديد الذي كشفته المصالحة (ولم يذكره تقرير المراجعة)

1. **فشل ذاكرة الواجب في ضاما (FIX2)** — كان `obligationInfo` يخزّن **معرّف القطعة المُلزَمة**،
   والمعرّفات تُعاد ترقيمها بين الحالات ⇒ معرّف بائد ⇒ نفخ حجر خاطئ أو لا نفخ.
   الآن يُخزَّن **الموقع** — 8 حالات فشل اختفت (451/8 ← 459/0). **منشور** (`dama.js?v=dtsg13`).
2. **`_admin_payments_ui_test.js` كان يفترض `user_id=18` صلباً** — على قاعدة جديدة يفشل 8/19
   (ليس عطلاً في المنصة بل في الاختبار). صار يحلّ المعرّف من القاعدة ⇒ 19/0.

---

## 3) نقاط القرار الثلاث (تحتاج كلمة المالك)

### أ) قانون النفخ/الإلزام في ضاما — **صار تنفيذاً بسطر واحد** ✅ (تحديث v2.44)

> **حُدِّث بعد كتابة هذا التقرير:** الوضعان موجودان الآن في الكود عبر `rules.obligation`:
> `'overall'` (الافتراضي المنشور) و`'piece'` (الأصرم). الافتراضي **لم يتغيّر**، والمفتاح **مُثبت أنه مؤثّر فعلاً**
> (اختبار `_dama_obligation_mode_test.js` = **7/0**: نفس النقلة ⇒ overall لا ينفخ · piece ينفخ المُلزَمة)،
> والرشّ يمرّ **459/0 في الوضعين**. القرار الآن كلمة واحدة: «بدّل» ⇒ أرفع المفتاح وأنشر · «اتركه» ⇒ يبقى كما هو.
> منشور ومتحقَّق حياً على `dtsg.pages.dev` (النشر `9ae0a58d` · `dama.js?v=dtsg14`).

#### التفصيل (مرجع)
- **main (المنشور):** `RULES-FIX` — الواجب = **أطول سلسلة متاحة في الدور كله**؛ من أتمّها بأي قطعة أدّى الواجب.
- **فرع المراجعة:** `RULES-RESTORE` — الواجب مربوط بـ**القطعة المُلزَمة نفسها**؛ إن أكل غيرها أو قصّر سلسلتها ⇒ نفخ.
- الأثر: يظهر عندما توجد قطعتان لهما نفس أطول سلسلة. اختبار الرشّ في كل خط يفترض قانونه ⇒
  لا معنى لمقارنة الأرقام بينهما (459/0 في كليهما، بقوانين مختلفة).
- **القرار لك.** التغيير = تعديل المحرّك + تحديث اختبار الرشّ + إعادة نشر.

### ب) ميزة `pay_audit` (سجل مالي موحّد) — عند الفرع فقط
فرع المراجعة يضيف: جدول `pay_audit` + `payAudit()` + `depositBonusPct`/`rateOf` مُصدَّرين + واجهة فلاتر.
main لا يملكها (سجلّه = جدول `transactions` + **تبويب «سجل المال»** في داشبورد السوبر أدمن، المنشور والمختبَر).
نقلها = **ميزة جديدة** (مخطط + كتابة سطر عند كل حركة + واجهة)، وليست إصلاح عطل.
- حاولنا تشغيل اختبارهم على main: `FATAL TypeError: core.depositBonusPct is not a function` ⇒ **غير قابل للنقل كما هو**.

### ج) رندر قطع الشطرنج — تصميمان
- **main (المنشور والمتحقَّق):** يبقي التدرّج `url(#chGradW/#chGradB)` + يحقن تعريفات SVG (`chessInjectDefs`).
  القياس الحيّ على الإنتاج: **32 قطعة · 48×48px · 0 مرجع معلّق · شفافية 1 · 100% بكسل صلب**.
- **فرع المراجعة:** يحذف التدرّج ويستعمل تعبئة صلبة؛ اختباره يفشل على main (6/4) **لأنه يفرض التصميم الصلب**، لا لعطل.
- لم نغيّر تصميم main، ولم نُودِع اختبارهم (يُفشِل الشجرة بلا سبب وظيفي).

---

## 4) ما نُقل فعلاً من خط المراجعة

- ✅ **إصلاح اختبار pay_audit** (تطابق جزئي `refunded`) — موجود في `bdb6769` على الفرع.
- ✅ **فكرة الفحص الشامل للقطع** — أُعيدت كتابتها على نموذج main وأُضيفت إلى `_chess_v244_test.js`
  (3 تأكيدات جديدة) بدل استعمال الاختبار الذي يفرض تعبئة صلبة.
- ❌ **اختبار pay_audit كما هو** — لا يُودَع (انظر ب).
- ❌ **اختبار القطع الصلب** — لا يُودَع (انظر ج).

---

## 5) أمان الخطوط الموازية (درس مُستفاد)

1. **لا force-push على فرع فيه عمل موازٍ** — الـforce-push السابق أزاح `6900d30` من main
   ولم يكن موجوداً في أي مرجع؛ صار اليوم محفوظاً في `arena/v2.44-m1-m3`.
2. **كل خط عمل يُرفع لفرع مسمّى أسبوعياً** كخطوة روتينية، ولو لم يُدمج.
3. `arena/01a0a948-dtsg` = `9fdbe9f` **ليس** عمل الشريك — بل استيراد v2.28.1 قديم.

---

## 6) أوامر التحقق (للمراجعة المستقلة)

```bash
# بيئة كاملة من الصفر (Node 24 + Playwright/Chromium + خادم اختبار 3971 + مستخدمو QA)
bash scripts/qa-env.sh
export PATH=$HOME/.cache/node24/bin:$PATH NODE_PATH=$HOME/.cache/pw/node_modules \
       PLAYWRIGHT_BROWSERS_PATH=$HOME/.cache/ms-playwright

# الأجنحة (698 تأكيداً)
for t in _dama_engine_test _dama_fuzz_test _cf_payments_test _money_v244_test _bot_v244_test \
         _chess_v244_test _parchisi_v244_test _dama_v244_test _dama_browser_v244_test _admin_payments_ui_test; do
  node tests/$t.js | tail -1
done

# خادم الهاتف بعد التحديث
ADMIN_API_SECRET="$ADMIN_API_SECRET" bash scripts/verify-phone-v244.sh http://127.0.0.1:3000

# خط المراجعة (مرجع)
git fetch origin arena/v2.44-m1-m3 && git log --oneline -1 FETCH_HEAD   # bdb6769
```

---
_آخر تحديث: v2.44 — main = `1d3cbcd` · منشور = `46e87aca.dtsg.pages.dev` · الفرع المحفوظ = `bdb6769`_
