# v2.55 — دليل وتعليمات مساعد قاعدة البيانات (cat) على الهاتف 📱

**التاريخ**: 2026-09-22  
**الإصدار المنجز**: `v2.55` (الضومنة: امتداد السلسلة 7 قطع + جسور الالتفاف بقطعتين لمنع التكدس + دوران الأدوار عكس عقارب الساعة + توسيط الشارة والحرفين + خفض البنك 130px ورفع الأفاتار 25px · الرامي: حسم تجمّد الأدوار وإصلاح مخالفات سحب المرموق وقانون الـ 12 ورقة · الخصوصية: تطهير كامل للبيانات الشخصية وشرح تعليمي غني لوسائل الدفع).

---

## 1. ملخص قاعدة البيانات المحلية (Local SQLite DB) 🗄️

> **النتيجة المباشرة لـ cat: لا توجد أي هجرة أو تعديل على جداول قاعدة البيانات نهائياً!**
> 
> ملف `data/royalcoin.db` وهيكلية الجداول سليمة 100% ولم يطرأ عليها أي تغيير (`No Migrations`, `No Schema Changes`, `No New Columns`).

- التعديلات البرمجية شملت منطق اللعب والعرض والخصوصية فقط:
  - محرك وواجهة الضومنة: `domino-core.js`، `domino-app.js`، `domino-renderer.js`، `css/22-look.css`.
  - سياسة الدفع والاسترداد: `refund-policy.html` و `js/wallet.js` (تطهير كامل لكافة البيانات الشخصية).
  - محرك وذكاء الرامي الخبير: `js/games/rami.js` (فك تجمّد العداد 0s عبر `_handleTurnTimeout`، ودعم استبدال الجوكر `trySwapJoker` وتصفير بطاقة المرموق المسحوبة).
- الأرصدة وسجلات المعاملات وجداول الرهانات تعمل كما هي دون أي مساس بها.

---

## 2. خطوات العمل المطلوبة على الهاتف (Termux / Phone Ops) 🛠️

### الخطوة 1: جلب التحديثات في الهاتف
```bash
cd ~/DTSG
# جلب أحدث التغييرات من الفرع الرئيسي
git pull origin main
```
*(أو في حال استعمال ملف الرقعة التراكمي: `git apply /path/to/DTSG-v255.patch`)*

### الخطوة 2: إعادة تشغيل خادم Node المحلي على الهاتف
بما أن التحديث يشمل ملفات الخادم ومحركات الألعاب المخدومة، يجب إعادة تشغيل عملية Node:
```bash
# إيقاف العملية القديمة إن كانت تعمل
pkill -f "node.*server.js" || true

# إعادة تشغيل السيرفر بدعم SQLite التجريبي
node --experimental-sqlite server.js &
```
التحقق من صحة الخادم المحلي في الهاتف:
```bash
curl -s http://127.0.0.1:3000/api/health
# يجب أن يرجع: {"ok":true,"service":"dmgames-arena","build":"2.45.0",...}
```

### الخطوة 3: نشر الواجهة الثابتة على Cloudflare Pages (dtsg.pages.dev)
يتم النشر من الهاتف حيث يتوفر مفتاح التوكن الآمن (الذي لا يُخزن في المستودع العام):
```bash
export CLOUDFLARE_ACCOUNT_ID=758fcc827f3338772847c28391b6c6c3
export CLOUDFLARE_API_TOKEN="<التوكن_الخاص_بك_في_الهاتف>"

# تشغيل سكربت النشر الآلي
bash scripts/deploy-pages.sh
```
*السكربت يقوم تلقائياً ببناء وتصفية الملفات إلى `/tmp/dmc-deploy` ورفعها إلى مشروع `dtsg` على فرع `main` ليعمل الرابط مباشرة: `https://dtsg.pages.dev`.*

---

## 3. قائمة التحقق السريعة لـ cat (Smoke Test Checklist) 🧪

1. **الضومنة (Dominoes)**:
   - افتح لعبة الضومنة (4 لاعبين): تأكد أن الدور يتحرك **عكس عقارب الساعة** (اللاعب الرئيسي ⬅️ الخصم الأيمن ⬅️ الخصم العلوي ⬅️ الخصم الأيسر ⬅️ اللاعب الرئيسي).
   - تأكد أن شارة اللاعب الرئيسي متمركزة في **الوسط أفقياً فوق قطع يده بارتفاع متناسق (+25px)** وتحتوي أول حرفين من اسمك (مثل `TA` أو `PL`).
   - تأكد أن بنك القطع نزل إلى الأسفل بمقدار 130px تحت بطاقة `R T` (`top: 186px; left: 10px;`).
   - تأكد أن السلسلة تمتد لـ 7 قطع لكل قطاع مستقيم، وأن المنعطفات تمتد لقطعتين لمنع تكدس الصفوف وتوفير مسافة فاصلة واسعة ومريحة بين الأعمدة والصفوف.
   - في نافذة **«إعدادات الغرفة والجولة»**: يظهر خيار **«عدد اللاعبين»** (2 لاعبين، 3 لاعبين، 4 لاعبين).

2. **مركز المساعدة والشات بوت (Help Center & Bot Chat)**:
   - كافة بطاقات الوصول السريع تستجيب بسلاسة (بوت الدعم، بوت التعبئة، فتح المحفظة، تتبع المعاملة).
   - بطاقة «تتبّع معاملة» تنقلك تلقائياً إلى تبويب الشات مع عرض تقرير المعاملات في بطاقات أفقية أنيقة ومنظمة بدون أي تشوه أو التفاف عمودي.

3. **الرامي (Rami)**:
   - ابدأ مباراة رامي: لا مزيد من تجمّد العداد عند 0 ثانية إطلاقاً.
   - البوتات لا ترتكب مخالفة سحب المرموق (+71 نقطة جزاء).

4. **سياسة الاسترداد والخصوصية (`refund-policy.html`)**:
   - لا توجد أي بيانات شخصية (أسماء، هواتف، حسابات CIH، عنوان محفظة ترون).
   - تظهر شروحات ثرية وتوجيه المستخدمين بأن بيانات الدفع والـ QR يتم عرضها وتحديثها ديناميكياً بأمان داخل المحفظة.

5. **التحصين الأمني لحماية الأرصدة (Security Remediations)**:
   - تم تشغيل اختبارات الأمان `node tests/_audit_remediation_test.js` وتمريرها بنسبة 100%.
   - رصيد الخادم صار الحاكم الوحيد عبر `/api/sync` ولا يمكن تزوير الكوينز من العميل.
   - طلبات السحب والمحفظة محمية ومحصورة بصاحب الجلسة فقط بدون أي ثغرات IDOR.
   - حماية تسجيل الدخول من التخمين بمحدد معدل (10 محاولات) ورسالة موحدة 401.

6. **سلامة النفق (Cloudflare Tunnel)**:
   - سكربت `scripts/phone-tunnel.sh` يواصل ربط هاتف المشرف بسلاسة دون تعارض.
