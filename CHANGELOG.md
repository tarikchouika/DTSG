# سجل التغييرات — DTSG (v2.27 → v2.59.4)

توثيق رسمي لكل التحسينات المنفَّذة منذ الإصدار v2.27 وحتى الحالي.
آخر تحديث: 2026-09-25 (v2.59.4 — إصلاح تجمّد نهاية مباراة الشوط الواحد + تسلسل مباريات الإعادة).

---

## v2.59.4 (2026-09-25) — التجمّد الثالث: مباراة «الشوط الواحد» (الوضع الافتراضي) تتجمد عند النهاية (الجولة-7)

**بلاغ المالك:** التجمّد لا يزال قائماً على `dtsg.vercel.app` رغم إصلاحي الجولة-5
(المحرك) والجولة-6 (انتقالات الأشواط) — مع طلب صريح: اللعب بمتصفح حقيقي
حي، واختبار **جميع الألعاب**، بدور المستكشف والمدقق ومحسّن التجربة.

### أ) السبب الجذري الثالث (مُعاد إنتاجه حياً على الإنتاج + مُصلَح)

الوضع الافتراضي في شاشة إعداد الرامي هو **«رهان على شوط واحد»** (أول خيار
في قائمة الهدف). في هذا الوضع يقفز المحرك عند إنهاء الشوط مباشرة إلى
`MATCH_END` دون المرور بـ `ROUND_END` إطلاقاً (شرط `isSingleRound` في
`_endRound`). لكن 6 مواضع في واجهة اللعبة كانت تفحص `ROUND_END` **فقط**:

- نهاية دور البوت (مسارا الرمي والإنهاء) — **وهذا بالضبط ما يراه المالك**:
  البوت يرمي ورقته الأخيرة، المحرك يُنهي المباراة، والواجهة تبقى على
  الطاولة الحية: المؤقت واقف، لا شاشة نتائج، لا زر «مباراة جديدة» — تجمّد
  دائم حتى إعادة تحميل الصفحة.
- مُكمل حركة اللاعب البشري (رمي/إنهاء) وافتتاح الميلدات.

أُعيد إنتاج العطل حياً على الإنتاج (زرع يد من ورقة لبوت حقيقي عبر مجرى
الدور الطبيعي): المحرك أعلن `MATCH_END` والواجهة علقت على الطاولة مع
مؤقت متجمد على 90 ث — مطابق تماماً لتسجيل شاشة المالك.

عطل ثانوي مكتشف في نفس الفحص: مفتاح لقطة النهاية `_endFreezeDone`
(`رقم الشوط:المرحلة`) يتصادم بين مباراتي «شوط واحد» متتاليتين
(`1:MATCH_END` = `1:MATCH_END`) فتتخطى مباراة الإعادة بانر مشاهدة
أوراق الفوز (تُعرض النتائج فوراً بدل 6 ثوانٍ من المشاهدة).

### ب) الإصلاح (7 تغييرات في `js/games/rami.js`)
1. **كشف `MATCH_END` في كل مواضع فحص نهاية الشوط الستة**: دور البوت
   (الرمي + الإنهاء)، مُكمل حركة البشري (الرمي + غير الرمي + الإنهاء)،
   وافتتاح الميلدات — كلها الآن `ROUND_END || MATCH_END`.
2. **تصفير مفتاح لقطة النهاية عند بدء مباراة جديدة** في `ramiStartGame`
   (مع إلغاء مؤقّت اللقطة الجاري) — بانر المشاهدة يعمل في كل مباراة.

### ج) التحقق الكامل (10 مجموعات خضراء)
- **اختبار جديد** `tests/_rami_single_round_end_test.js`: 6/6 — 3 نهايات
  بوت متتالية (مع إعادة لعب حقيقية عبر زر «مباراة جديدة») + نهاية اللاعب
  البشري؛ شاشة النهاية تظهر فوراً في كل مرة، صفر أخطاء صفحة.
  (قبل الإصلاح: كل نهاية شوط واحد = تجمّد دائم على الطاولة).
- `rami_ace_value` 21/21 · `rami_sample_v19` 100/100 · `rami_expert`
  58/60 بلا أي دور عالق · `_rami_partition_exact` 23/25 (الفاشتان
  المعروفتان monster0 حسب التصميم الموثّق).
- متصفح: `_rami_freeze_test` 13/13 · `_rami_persistence_and_freeze`
  100% · إجهاد انتقالات الأشواط (5 دقائق) 4 أشواط صفر تجمدات وصفر أخطاء
  والمقدمات كلها طبيعية.
- الحرّاس: `_ops_guards_test` 48/48 · `_security_static_test` 37/37.
- بصمة الكاش: `rami.js?v=dtsg20`.

> **ملاحظة نشر:** رُفع إلى GitHub main — نشر Vercel تلقائي. صفحة
> Cloudflare (`dtsg.pages.dev`) لا تزال تقدّم بناء `dtsg11` القديم بلا أي
> إصلاح — يجب تشغيل `deploy-pages.sh` لتحديثها (خارج نطاق هذا الإصلاح).

---

## v2.59.3 (2026-09-25) — تجمّد الشوط الجديد بعد الإصلاح السابق: جذر ثانٍ مكشوف ومُصلَح (الجولة-6)

**بلاغ المالك:** بعد v2.59.2 التجمّد «لا يزال قائماً» على نشر Vercel الجديد
(`dtsg.vercel.app`) — سجل شاشة يظهر توقف المؤقت عند الثانية الأولى/الثانية
وعدم رمي البوت للورقة. فحص حي بمتصفح حقيقي أعاد إنتاج العطل **8 مرات موثّقة**
وأثبت أن إصلاح الجولة-5 (المحرك) سليم — لكن يوجد **جذر ثانٍ مختلف كلياً**.

### أ) السبب الجذري الثاني (مُثبت بتدقيق استدعاءات حي + مُشرّح حالي)

التجمّد لا يحجب الخيط الرئيسي (صفر فجوات) — إنه **قفل منطقي** عند انتقال
الشوط: `playIntroAndStart()` كانت تقفل `RAMI_BUSY=true` **بلا مالك** وتعتمد
كلياً على سلسلة `setTimeout` (1100ms→900ms) لتحرير القفل. عند موت السلسلة
(خنق مؤقتات الهاتف/سباقات أرقام gen بعد أن كانت `_renderGame` تعيد بناء
المؤقتات في كل رسم) يبقى القفل معلّقاً: أول دور بوت في الشوط الجديد لا
يبدأ، المؤقت واقف على 90s/89s، والـwatchdog نفسه معطّل. قياس حي: قفل 66
ثانية متتالية في **كل** انتقال شوط (التوقيت 66ث = 6ث بانر النهاية + 60ث
العدّاد التلقائي)، واسترداد متأخر عبر مسار طوارئ فقط.

### ب) الإصلاح (دفاع متعدد الطبقات في `js/games/rami.js`)
1. **مقدمة محصّنة**: القفل بمالك + إتمام idempotent برمز لكل مقدمة (يمنع
   سباق المقدمات المتزامنة) + **مهلة قصوى** (4ث للأشواط اللاحقة/6.5ث للأول)
   تُنهي المقدمة قسراً وتحرر القفل وتشغّل الدور مهما ماتت المؤقتات.
2. **مؤقتات idempotent**: `_startTimer(force)` لا تعيد البناء إذا كانت
   المؤقتات حية — إنهاء سباقات gen التي كانت تقتل المؤقت الحي وتُبقي
   المعرف معبّأ بميّت.
3. **بصمة حياة للعدّاد**: `_tick` يختم `_lastTickAt`، والـwatchdog يكتشف
   العدّاد الميت بالزمن (>6ث) ويعيد بناءه قسرياً.
4. **تحرير القفل يستدعي الدور**: بعد تحرير RAMI_BUSY العالق يشغّل الـwatchdog
   دور البوت فوراً إن كان دوره (كان يحرّر فقط وينتظر).
5. **meta-watchdog على مستوى النافذة** (يُنشأ مرة واحدة في `initRami`): أثبت
   القياس أن مؤقتات النافذة تنجو بينما تموت مؤقتات المحوّل — الحارس يفحص
   كل 2ث: مقدمة عالقة ⇒ يُتممها، عدّاد ميت ⇒ يعيد بناءه، قفل معلق ⇒ يحرّره
   ويشغّل دور البوت. **لا يمكن لمحوّل معطوب أن يقتله.**

### ج) CORS لنشر Vercel (عطل مصاحب مكشوف حياً)
النشر `dtsg.vercel.app` كان **معطّلاً كلياً**: كل نداءات API تُرد 403 (فشل
الدخول صامتاً + محفظة معطلة) لأن النطاق لم يُضف للقوائم البيضاء. أُضيف
`dtsg.vercel.app` (مطابقة تامة، لا wildcard) في: `server.js` +
`server-payments.js` + `cf-worker/worker.js` (+ `dtsg-3e0.pages.dev` في
الووركر). تحقق حي: أصل Vercel ⇒ **204** · أصل شرير ⇒ **403** (لم يتغير).

### د) التحقق (كله على الشيفرة المرقعة داخل المستودع)
- محرك: `rami_ace_value` **21/21** · `rami_sample_v19` **100/100** ·
  `rami_expert` **59 فوزاً/60 وصفر جولات عالقة** · `_rami_partition_exact` **23/25**
  (فحصا monster0 الحرفيان كما في v2.59.2 — بالتصميم).
- متصفح رسمي: `_rami_freeze_test` **13/13** · `_rami_persistence_and_freeze`
  **100%**.
- **اختبار إجهاد انتقالات الأشواط الجديد** `tests/_rami_round_transition_stress_test.js`
  (مباراة 301 حية، لعب بشري سريع، إجبار نهايات أشواط كل 45ث عبر نفس مسار
  المحرك+الواجهة، كاشف تجمد ≤10ث): جولتان — **4 ثم 5 انتقالات أشواط، صفر
  تجمدات، صفر أخطاء صفحة** (قبل الإصلاح: كل انتقال = قفل 66ث). تشخيص منفصل
  أثبت الـmeta-watchdog يعمل حياً (`dead tick detected — restarting timers`).
- الحراس: ops-guards **48/48** · sec-static **37/37** · sec-audit **32/32**.

### هـ) النشر
- `index.html`: بصمة `js/games/rami.js` **`?v=dtsg18` → `?v=dtsg19`**.
- **مطلوب من المالك بعد الدمج**: (1) نشر الواجهة (Pages/Vercel)، (2) تحديث
  خادم الهاتف بالشيفرة الجديدة (server.js + server-payments.js — بدونهما يبقى
  نشر Vercel محجوباً 403)، (3) نشر الووركر الوسيط `cf-worker/worker.js`
  (wrangler deploy) لقبول أصل Vercel على مستوى البوابة.

---

## v2.59.2 (2026-09-24) — إصلاح تجمّد اللاعب الآلي في رامي (تقرير الجولة-5)

**العطل المُبلَّغ من المالك:** عند اللعب ضد اللاعب الآلي (بعد 10+ جولات) تتجمد
اللعبة عند دور البوت — المؤقت العكسي يتوقف عند الثانية الأولى أو الثانية، والبوت
لا يرمي ورقة، والطاولة تتعطل كلياً حتى إعادة التحميل.

### أ) السبب الجذري (مُثبت بالقياس المباشر على الإنتاج في الجولة-5)
- دور البوت كان يستدعي `getLegalMoves(bot.id)` في خطوة اللعب **والنتيجة لا تُستخدم
  مطلقاً** (استدعاء ميت في سطر 3734) — لكنه يشغّل تقسيمين كاملين
  `partitionSelectedCards()` بلا ميزانية زمنية على يد البوت = **79% من زمن الدور**.
- إصلاح `[FREEZE-CORE 2026-09-23]` السابق وضع سقف **حالات** (250k) للمحرك المخزّن
  لكنه لم يقيّد **الزمن**: الحالة الواحدة تمرّ على آلاف المجموعات الجزئية، فيستهلك
  المحرك 1.6-1.7 ثانية/استدعاء على حاسوب مكتبي و5-20 ثانية على هاتف.
- محرك الخبير لا يرمي الجوكرات ويحتفظ بأوراق المتتاليات، فتبني الأيدي «الغنية»
  (3-4 بريات + متتاليات مكررة) بعد جولات — لهذا يظهر العطل بعد 10+ جولات.
- أثناء الحجب، المؤقت (`setInterval`) والـwatchdog كلاهما على الخيط المسدود — لا
  إنقاذ ممكن، والعدّاد يبقى مجمداً على آخر قيمة (89s/88s = «الثانية الأولى أو الثانية»).
  قياس مباشر على الإنتاج: فجوة خيط 3499ms والمؤقت متجمد على 89s.

### ب) الإصلاح (3 تغييرات في `js/games/rami.js`)
1. **حذف الاستدعاء الميت** `const legalMoves = this.game.getLegalMoves(bot.id);` —
   إزالة 79% من الحجب فوراً بلا أي تغيير سلوكي (رميات البوت تُجلب من جديد عند
   الحاجة قبل الرمي).
2. **مهلة جدارية حقيقية** `PARTITION_EXACT_TIME_MS = 120` تشمل التعداد
   (`getCombos`) والمحرك المخزّن (`bestInc`) واحتياطي DFS معاً — سقف زمني فعلي
   ≈120-160ms بدل 1.8 ثانية، ويرجع أفضل نتيجة وُجدت عند التجاوز.
3. **مسار إيقاف سريع** `if (overflow) return` في رأس `bestInc` — بدون انتشار فوري
   لإشارة الإيقاف كانت الحالات البينية تعيد العمل الكامل فينفجر الزمن مجدداً
   (أول محاولة بقيت 1.3 ثانية؛ معه صارت 158ms).

### ج) التحقق (كل الأجنحة على الملف المُصلَّح داخل هذا المستودع)
- **تفاضلي أصل ↔ مُصلَّح: 7/7** — نتائج متطابقة حرفياً على 12 يداً عادية/منظمة؛
  اليد المرضية W3: 1832→152ms؛ خطوة البوت: 3613→301ms؛ مسار الخبير بلا تغيير.
- **أجنحة المشروع الرسمية:** `rami_ace_value` **21/21** · `rami_sample_v19`
  **100/100** · `rami_expert` الخبير يفوز 54/60 و**صفر جولات عالقة** ·
  `_rami_partition_exact` **23/25** (فحصا `monster0` الحرفيان يفشلان **بشكل مقصود**:
  اليد الوحشية تحصل الآن على أفضل نتيجة خلال ≤120ms بدل المطابقة الحرفية التي
  كانت تكلف 1.8 ثانية — هذا جوهر الإصلاح؛ يُنصح بتحديث الفحصين مستقبلاً ليقبلا
  «مجموعات قانونية + زمن محدود» بدل المطابقة الحرفية).
- **متصفح (خادم QA منفذ 3971):** `_rami_freeze_test` **13/13** (المؤقت يعمل، أدوار
  البوت تنفَّذ، استدراك الخطوات المؤجلة، الشوط التالي حيّ) ·
  `_rami_persistence_and_freeze_test` **100%** (66 نقلة متواصلة بلا أي deadlock).
- مسار بشري إضافي يستفيد تلقائياً: زر «افتتاح» و`_doOpen`/`__doFinish` على يد اللاعب
  البشري أصبحا محميين بنفس المهلة.

### د) النشر
- `index.html`: بصمة `js/games/rami.js` **`?v=dtsg11` → `?v=dtsg18`** (تفادي الكاش).
- بعد الدمج: نشر Pages (deploy-pages.sh) ثم تحديث خادم الهاتف إن لزم — الملف
  مستقل بذاته (نسخة كاملة قابلة للإسقاط مكان القديمة).

---

## v2.59.1 (2026-09-23) — تنفيذ الإطلاق على الهاتف + تحصين الوسيط (إنتاج حيّ)

نُفِّذ ما كان «متبقياً خارج المستودع» في تسلسل §7 من `docs/V259_DB_CAT.md`
(جلسة cat على الهاتف)، مع ثلاث تعديلات وقائية داخل المستودع:

### أ) في المستودع
- **`server.js` + `server-payments.js`**: إضافة `dtsg-3e0.pages.dev` إلى
  `ALLOWED_ORIGIN_HOSTS`. السبب: مشروع Pages ثانٍ بالاسم «dtsg» في حساب Cloudflare
  الآخر (ID `03efbdbe…`) يخدم المنصة فعلاً (`https://dtsg-3e0.pages.dev` — نسخة أقدم).
  بالقائمة الصارمة وحدها كان سيُردّ 403 فينكسر الدخول من ذلك المضيف. مطابقة تامة
  (لا wildcard)، وتُحذف إذا حُذف المشروع.
- **`.gitignore`**: `backups/` — نسخ `royalcoin.db` الاحتياطية بيانات شخصية (§4 من الدليل).
- **`scripts/phone-env-restart.sh`**: `BOT_API_SECRET` ضمن المفاتيح الإلزامية وضمن
  التحقق الحيّ لبيئتي `casino-server` (16 مفتاحاً) و`dtsg-voucher-bot` (5 مفاتيح) —
  بوابة v2.59 fail-closed ⇒ غياب السر = تعطّل صامت للبوت.

### ب) على الهاتف (إنتاج)
- `git fetch origin main && git reset --hard origin/main` ⇒ `613c8a7` (v2.59) ثم `56b0c10`.
- `BOT_API_SECRET` جديد (64 حرفاً) في `.env.local` + `phone-env-restart.sh` ⇒ كل
  المفاتيح حيّة (كان 15 ⇒ صار 16) والفحوص السريعة خضراء.
- بوابة البوت حيّاً: `/api/bot/request` بلا سرّ ⇒ **401** · بالسِرّ ⇒ **400 bad-input**
  (عبر البوابة إلى المعالج) · `/api/bot/link` بلا سرّ ⇒ **401**.
- CORS على الخادم: GET/OPTIONS من أصل شرير ⇒ **403** · `dtsg.pages.dev` و
  `dtsg-3e0.pages.dev` ⇒ **204 + ACAO**.
- الحراس على **نسخة QA معزولة** (منفذ 3999، قاعدة نظيفة — بلا أي أثر على الإنتاج):
  **ops-guards 48/48** · **sec-audit 32/32**.
- **الواجهة**: `deploy-pages.sh` بحساب `dmgames-api` (`758fcc82…`) ⇒ `dtsg.pages.dev`،
  وبصمة `index.html` الحيّة = بصمة المستودع حرفياً.
- **الوسيط `casino-phone`** (Version `da380a50`): CORS صريح (403 لكل أصل غير موثوق،
  بما فيه GET/HEAD)، إسقاط `x-backend-addr` (DTSG-008)، توحيد `Set-Cookie` مع إبقاء
  `SameSite=None; Secure`. تحقق حيّ: 403 للأصل الشرير · 204+ACAO للموثوق · صفر
  `x-backend-addr` · `/api/health` و`/api/payments/methods` سليمان عبر الوسيط.

### ج) تنظيف قاعدة الإنتاج (cat §3–§5)
- السحبان `wd-mudoc0qe11yh64` + `wd-mudeheka5h75oc`: **مرفوضان مسبقاً من المالك** عبر
  الواجهة (`reviewed_by = المالك / Tarikchouika`) ⇒ لا معلّق (0 صف).
- فك ربط `999888777` عن حساب id 252 بالأمر الحرفي (الربط الأصلي كان `8245481030`
  كما في `sup_users` واللقطة الأمنية ⇒ أُعيد NULL حسب الدليل؛ يُعاد الربط من الواجهة عند الرغبة).
- تذكرة الدعم #3 ⇒ **مغلقة** (`poC-test-message-round1`).
- `PRAGMA integrity_check` = **ok** · نسخة احتياطية قبل التنفيذ في `backups/`.
- **ملاحظة تدقيق (لم تُلمس)**: `money_log` لا يحوي سطر تسوية للسحبين المرفوضين
  (`id=40` عالق على `pending` والآخر بلا سطر) لأن مسار رفض الواجهة لا يكتب سجل المال —
  ومع ذلك لم يُعدَّل `money_log` (منع صريح في الدليل).

### د) ملاحظات مفتوحة للمالك
- `verify-phone-v244.sh`: افتراضه `TG_ID=999000001` (fixture QA) غير موجود في قاعدة
  الإنتاج ⇒ 4 فحوص شكلية تسقط؛ الجوهرية (401 للبوابة · الدفع · الصحة) خضراء.
- **PAT GitHub مخزَّن نصاً صريحاً** في `.git/config` و`/root/.secrets/github_token.txt`
  (خلافاً لما ورد في تقرير الجلسة السابقة) ⇒ يُوصى بتدويره.
- `tests/_money_invariants_test.js` قسم الكود/التفعيل يستدعي `/api/bot/admin-act` بلا
  `tg_id` (العقد الحالي يطلبه) ⇒ انحرافُ اختبار لا عطل إنتاجي (البوت الشرعي يرسله).
- `dtsg-3e0.pages.dev` يخدم نسخة أقدم من الواجهة؛ تجديده بملف واحد:
  `CLOUDFLARE_ACCOUNT_ID=03efbdbe… CLOUDFLARE_API_TOKEN=… bash scripts/deploy-pages.sh`
  (لا ضرر في تركه).

---

## v2.59 (2026-09-23) — إغلاق بوابة البوت (R3-001/2) + تحصين CORS + حارس DM_TEST_MODE

استجابة لتقرير الجولة-4 (CONDITIONAL): 19 بنداً تم التحقق من إغلاقها، وبقيت
اثنان داخل عائلة واحدة (بوابة البوت) + ملاحظتان. أُغلقت الكودية كلها في `main`:

### R3-001 (حرجة) + R3-002 (عالية) — `POST /api/bot/request` و `POST /api/bot/link`
- **المشكلة**: المساران بلا أي مصادقة — أي عميل إنترنت غريب كان يستطيع:
  - إنشاء **سحب** باسم أي ضحية (user_id/username/tg_id) ⇒ **يُخصم الرصيد فوراً**
    (PoC التقرير: 998→498 كوينز) مع تفاصيل دفع يتحكم بها المهاجم،
  - **ربط أي تيليغرام بأي حساب** بلا جلسة ولا إثبات ملكية.
- **الإغلاق** (`server-payments.js` — البوابة قبل المعالج، ملف واحد):
  - **سِرّ مشترك إلزامي** في ترويسة `x-bot-secret` (أو `x-admin-secret`) مقابل
    `BOT_API_SECRET` (مع `ADMIN_API_SECRET` بديلاً) — **مقارنة ثابتة الزمن**
    (`crypto.timingSafeEqual`) بلا oracle زمني؛ إن لم يُضبط أي سرّ ⇒ البوابة
    مغلقة على الجميع (fail-closed).
  - **حدّ معدل** 30/دقيقة لكل IP على `request`/`link` و20/دقيقة على `admin-act`
    (عنوان الجوار الفعلي فقط — لا ثقة بأي ترويسة `x-real-ip` قابلة للتدوير).
  - `admin-act` يحتفظ بمصادقته القائمة (سرّ الجسم/جلسة السوبر/تيليغرام الأدمن)
    مع قبول ترويسة السرّ الجديدة في `cf-worker/payments-core.js` (طبقة دفاع ثانية).
  - **البوت الشرعي** (`scripts/voucher-bot.js`) يرسل `x-bot-secret` تلقائياً من
    `BOT_API_SECRET` (أو `ADMIN_API_SECRET`) — لا كسر في المسار المشروع.
  - `scripts/verify-phone-v244.sh` مُحدَّث: يرسل السرّ (مرّر `BOT_API_SECRET` عند
    التشغيل) + فحص جديد «البوابة مغلقة بلا سرّ (401)» + يقبل الإصدارات 2.5*.

### R4-001 (متوسطة) — CORS كان يثق بالساندبوكس العامة
- `.e2b.app` و`.arena.ai` كانتا مقبولتين **افتراضياً** (أي طرف ثالث يفتح
  ساندبوكس يحصّل CORS موثوقاً بقراءات المحافظ) — الآن خلف `DM_TEST_MODE=1`
  أو قائمة صريحة `DM_DEV_ORIGINS` (مفصولة بفواصل) — في الإنتاج تُرفض بـ403
  (`server.js` + `server-payments.js`).

### R4-003 (منخفضة/متوسطة) — حارس إقلاع `DM_TEST_MODE`
- العَلَم يجمع 3 سلوكيات خطرة (باب `qa-admin-secret` + تسجيل مفتوح بشحن 100,000
  كوينز + CORS محلي) — إن وُجد مع علامتي إنتاج أو أكثر (`TELEGRAM_BOT_TOKEN` +
  مفاتيح Binance…) يُطبع **تحذير صاخب دائم** في لوج الإقلاع (`server.js`).

### الحراس والمختبرات
- `tests/_ops_guards_test.js` (حارس «cat»): **38 → 48 فحصاً** — البوابة مغلقة
  (401 بلا/خاطئ) + السّر الصالح يعبر + 429 عند تجاوز الحد + `.e2b.app`/`.arena.ai`
  ⇒ 204 فقط في وضع الاختبار و403 في وضع الإنتاج.
- `tests/_audit_remediation_test.js` (sec-audit): **28 → 32 فحصاً** — قسم 9:
  إغلاق R3-001/2 (4 فحوص: 401/401/401/السّر الصالح يعبر).
- **توثيق**: `docs/V259_DB_CAT.md` — الدليل الدقيق لمساعد قاعدة البيانات (cat):
  حدود الصلاحية، أعمال التنظيف الثلاثة قبل الإطلاق بأوامرها الحرفية (رفض
  `wd-mudoc0qe11yh64` و`wd-mudeheka5h75oc` مع الاسترداد · فك ربط `999888777` عن
  id 252 · إغلاق التذكرة 3)، بروتوكول النسخ الاحتياطي، فحوص ما بعد الإنجاز،
  وتسلسل بيئة `BOT_API_SECRET` الجديدة.
- **ما تبقّى للمالك (خارج المستودع)**: نشر v2.59 على الهاتف + رقعة الوسيط +
  ضبط `BOT_API_SECRET` — التسلسل الكامل في `CURRENT_TASK.md` و`docs/V259_DB_CAT.md` §7.

---

## v2.58 (2026-09-23) — حصانة أمنية شاملة: P0 + الثغرتان الدّاخليتان + حارس «cat» الميكانيكي

تصعيد تقرير إعادة التحقق الأمني (POST-FIX SECURITY REVIEW): كل البنود P0 وNEW مغلقة
في الشيفرة + حارس ميكانيكي يمنع تكرار أخطاء التشغيل الموثّقة (cat) وحارس حيّ يثبت
إغلاق الثغرات في النسخة المشغَّلة فعلياً.

### P0 — قبل الإطلاق
1. **DTSG-005/011 — CORS صارم لكل الطرق** (`server.js` + `server-payments.js`):
   - القائمة القديمة قبلت **أي** `*.pages.dev` و**أي** `*.workers.dev` — حساب Cloudflare
     مجاني كان يصبح «أصلاً موثوقاً» بالكامل (جذر تسرّب قراءات المحافظ).
   - الآن: نطاقات المنصة حصراً (`dtsg/dmgames/dmcasino/casino-9xj.pages.dev` +
     `casino-api.*.workers.dev` + `casino-phone.dmgames-api.workers.dev`) + محلي للتطوير.
   - **كل** الطرق (GET/HEAD/POST/OPTIONS…) من أصل غير موثوق ⇒ 403 `forbidden_origin`
     بلا ACAO — القراءات لم تعد تتسرّب لأي صفحة شريرة (وكان GET/HEAD يمرران سابقاً).
2. **DTSG-008 — `x-backend-addr`**: الترويسة يضيفها الوسيط `casino-phone` (خارج هذا
   المستودع) — أُبطِل أثرها بالحجب الجذري أعلاه، ورقعة الوسيط الحرفية (حذف الترويسة +
   CORS صارم + توحيد Set-Cookie) موثّقة حرفاً بحرف في `CURRENT_TASK.md` للمالك.

### الثغرتان الدّاخليتان (اكتشاف أثناء القراءات — لم يرهما التقرير)
3. **تجاوز كامل في `POST /api/2fa/login`**: كان `{userId: 20}` يكفي لإصدار جلسة كاملة
   (حتى السوبر أدمن) **بلا كلمة مرور** عندما تكون 2FA معطّلة! الآن:
   - رمز إكمال مؤقت (`two_fa_token`) يُصدَر **حصراً بعد فحص كلمة مرور ناجح** في
     `/api/login` — مقيد بالهوية، صالح 5 دقائق، استخدام واحد.
   - بلا رمز صالح ⇒ 401 بلا كوكي (التجاوز مغلق حتى لـ 2FA المعطّلة).
   - قفل رموز TOTP: 5 أخطاء خلال 10 دقائق ⇒ قفل 15 دقيقة (IP + مستخدم).
   - دورة كاملة مختبَرة حيّاً بـTOTP حقيقي (SHA1/30s/6).
4. **باب خلفي `qa-admin-secret` ثابت في الإنتاج** على كل مسارات الدفع
   (`server-payments.js`) — الآن مفعّل فقط مع `DM_TEST_MODE=1` (بيئة الاختبار المحلية).
   إثبات حيّ: نسخة بلا `DM_TEST_MODE` ⇒ `qa-admin-secret` مرفوض 401 والسر الحقيقي يعمل.

### بنود NEW
5. **NEW-1 — Binance verify** (`cf-worker/payments-core.js`):
   - **جلسة إلزامية** (أو سرّ أدمن): النسخة السابقة سمحت لأي غريب بمعرفة user_id
     فقط بتشغيل «التحقق + الشحن التلقائي». غير الأدمن مثبت على حسابه (لا user_id للغير).
   - **مطابقة بـ`ref` فريد**: إن أرسل المستخدم مرجع التحويل (Transaction ID) يُقبل
     التحويل المماثل حرفياً وحده؛ وإن لم يُرسل يُقبل فقط إن كان مطابقاً وحيداً للمبلغ
     (أكثر من مطابق ⇒ 409 `ambiguous` يطلب ref) — النسخة السابقة كانت بالمبلغ فقط
     فيمكن لأول طلب «سحب» تحويل مستخدم آخر متقارب المبلغ.
   - **أخطاء JSON بلا 502**: حالات 502 البينية (order-failed/readonly/probe) ⇒ 503.
   - الواجهة (`js/wallet.js`): حقل «مرجع التحويل» اختياري يُمرَّر `ref`.
6. **NEW-2 — رهانات بنوع خام** (`server.js`): `typeof number` + عدد صحيح (الذهب بلا
   كسور) + حد أدنى 1 — `0.5` و`10.5` و`"10"` (نص) مرفوضة 400 (كانت تدخل ذهباً
   كسرياً/مزوّراً). يشمل `/api/rounds` (تسجيل التذاكر).
7. **NEW-3 — قسائم ≤10/دقيقة** لكل حساب (أو IP بلا جلسة) ⇒ 429 — المسار كان بلا
   أي مقيّد (تخمين/تكرار آلي بلا كلفة).
8. **NEW-4 — المتصدرون** `/api/lb`: `{rank, username}` فقط — الأرصدة خصوصية
   (كانت تكشف ذهب كل متصدر).
9. **NEW-6 — أخطاء عامة بلا عقد مصادقة/تفاصيل**:
   - 403 `vouchers/create` كان يشرح للهاجم **حرفياً** كيف يتوثق (أسماء الترويسات/الحقول)
     ⇒ 403 عام.
   - حارس 500 عام موحد في كل معالِج `/api` (كان أي استثناء غير متوقع **يُسقط العملية
     كلها**) و`payments-internal` بلا `e.message` (كان يفضح التفاصيل الداخلية).
10. **DTSG-006 — تراجع أُسّي للدخول**: 5 إخفاقات خلال النافذة ⇒ قفل، يزداد
    1→2→4→8→15 دقيقة عند التكرار (قفل IP + اسم) — بدلاً من 10/60ث «leaky».

### إصلاح إنتاجي مكتشف أثناء العمل
11. **`d1shim` في `server-payments.js`**: أماكن ربط D1 `?1..?N` غير مدعومة في
    `node:sqlite` ⇒ «column index out of range» ⇒ `/api/wallet/balance` كان يسقط
    500 دائماً على مسار الهاتف/Node. أُعيد ترقيمها إلى `?` مع توسيع الفهارس
    المتكررة (مثل `balance_usd >= ?2`).

### حارس «cat» الميكانيكي (`tests/_ops_guards_test.js` — 38/38 ثابت + حيّ)
يمنع ميكانيكياً تكرار أخطاء CHANGELOG v2.48.1/حادثة 2026-09-22، **ووجد حيّاً 3
مواضع تكرار فعلية وأصلحها**:
- **ثابت**: لا `cd` نحو المستودع القديم (dmgames-arena) · لا `git checkout main + git pull`
  (نمط طمس الشجرة الحيّة) · `pm2 restart --update-env` مسموح **حصراً** في
  `scripts/phone-env-restart.sh` · كل `curl` فحص-حالة على `https` حرفي يحمل `-L`
  (إلا «0» زائفة) · `qa-admin-secret` خلف `DM_TEST_MODE` · لا wildcard CORS عريضة ·
  لا `x-backend-addr` · حارس phone-env-restart سليم.
- **حيّ** (على الخادم المشغّل فعلياً — يكشف النسخ البالية): CORS لكل الطرق · ثغرة 2FA ·
  دورة TOTP كاملة + القفل · أنواع الرهانات (نافذة رهان مفتوحة) · binance-verify بجلسة
  بلا 502 · القسaims 429 · المتصدرون بلا أرصدة · البواب الخلفية على نسخة بلا
  `DM_TEST_MODE`.
- **المواضع الحيّة التي أُصلحت**: `update-phone-server.sh` (كان يحمل الأمر نفسه!) +
  `apply-support-now.sh` + `setup-telegram-bots.sh` (تعليم خاطئ في نص تحذير) — كلها
  صارت تفوّض إلى `phone-env-restart.sh` وحده. إضافةً `-L` لكل الفحوص البعيدة
  (phone-doctor/verify/update/setup) والدوال `jget/code`.

### الاختبارات (الكل حتمي)
`sec-audit 28/28 ×2` · `ops-guards 38/38 ×2` · `fin-logs 22/22` · `coin+tourney 34/34` ·
`admin-i18n 53/53` · `leave 37/37` · `rami 25/25 + تجميد 13/13 + sample 100/100` ·
`wallet 5/5` · `hygiene 4/4`

---

## v2.57 (2026-09-23) — إصلاحات بلاغ المستخدم الثماني: المسائل 1-5 (تجميد رامي + زر الخروج + ترجمة الأدمن)

### 1) تجميد رامي بعد سحب الذكاء الاصطناعي بطاقة 🔴 (مسألة 1)
- **السبب الجذري**: `partitionSelectedCards` (تقسيم يد اللاعب عند التقييم) يعمل بـ`budgetedPartition`
  ضمن مهلة صارمة؛ على الأيدي الضخمة (20+ ورقة) كانت تُرجع `null`/فارغة قبل انتهاء المهلة ⇒
  محاكاة البوت تتعثر والعدّاد يتوقف عند ثانية واحدة.
- **الحل (دون مساس بالمحرك)**: ضمن الميزانية ⇒ `search()` الإرثي (سقف 24000 حالة)؛
  بلا ميزانية ⇒ `exactMemoSearch()` برمزي (DP + memoization) بنتائج مطابقة حرفياً للبحث الشامل.
  حتمية المحرك مثبتة عبر `tests/_mpdet_test.js` (كل البذور والأوضاع) و`tests/_rami_partition_exact_test.js` (25/25).
- قياس: يد 2404 حالة: 206ms بدل انهيار المهلة. تجميد UI: `tests/_rami_freeze_test.js` **13/13**.

### 2-4) زر مغادرة اللعبة: احترافي + تأكيد خروج + موضع بلا تراكب (المسائل 2-4)
- زر مشترك `#gameLeaveBtn` في كروم اللعبة (كل الألعاب الـ14) + قرص ترويسة `#gameLeaveBtnHead` في الوضع العادي.
- **تأكيد الخروج**: نافذة `#leaveModal` (z-16000) بتراخيص/هوية كل لعبة (`g.em` + `g.name`) ولونها الخاص
  (`GAME_LEAVE_ACCENTS` حسب مجال اللعبة: رامي عنابي، شطرنج أخضر، بلياردو… ) — التركيز يبدأ على «ابقَ»
  فلا يخرج Enter بالخطأ؛ Esc الأول يفتح التأكيد ولا يخرج مباشرة.
- **الموضع**: يسار زر ملء الشاشة بفاصل 6px على نفس الصف (`right: calc(10px + clamp(30px,7vw,36px) + 6px)`) —
  صفر تراكب مع مقبض بلياردو/الصينية/القضبان في كل الألعاب (62/62 UI).
- **درس CSS حاسم**: القواعد العامة `body.app-fs-on .game-fs-exit` في `05-ronda.css` و`22-look.css`
  مثبتة بـ`!important` (30-36px عند 10/10px)؛ الحل الفائز = **محدد بالمعرّف** `#gameLeaveBtn` + `!important`
  بنفس أحجام `clamp(30px,7vw,36px)`. لا محددات صنفية تكفي.
- الحارس: `tests/_leave_btn_static_test.js` **37/37** + `tests/_leave_btn_ui_test.js` **62/62** (14 لعبة × أزواج/تراكب/لهجات/تأكيد).

### 5) ترجمة لوحة السوبر أدمن كاملة (ar/fr/en/da) 🌐
- ~60 نصاً عربياً مثبتاً في `js/main.js` استُبدلت بمفاتيح `T('admin.*')` جديدة (~64 مفتاحاً ×4 لغات):
  تبويب الأكواد ونموذج إنشائها · عناوين جداول المعاملات/البطولات/الألعاب/المالي · أنواع المعاملات التسعة ·
  حالة السجل المالي (7 أنواع + 5 حالات) · تسميات aria الخمس · زر «🧾 سجل» · فلاتر «الكل/تصفية…» ·
  تصنيفات الألعاب السبعة عبر `admin.cat*` (card/casino/crash/instant/keno/slots/traditional) · `MAD (درهم)`.
- تواريخ رسائل التنسيق صارت حسب اللغة (`fr-MA`/`en-GB`/`ar-MA`) بدل `toLocaleString('ar')` المثبت.
- الحراس: `tests/_admin_i18n_test.js` **53/53** (كل مفاتيح T() موجودة ×4 + لا بقايا hardcoded في منطقة الأدمن)
  + `tests/_admin_i18n_ui_test.js` **14/14** (لوحة حيّة بالفرنسية: 6 تبويبات صفر عربية).
- إصدارات مخزن مؤقت: `translations.js`/`main.js` `?v=dtsg16` · `09-chrome.css` `?v=dtsg9`.

### 6) وجه ظهر العملة = medallion المنصة (المسألة 6) 🪙
- كان `coin-reverse.webp` نقش زليج ورق حائط لا يمت للعملة بصلة (أمام وجه أسد ذهبي أسطواني فاخر).
- **الحل**: `assets/dtsg/coin-reverse.svg` (SVG مكتوب يدوياً): medallion ذهبي من نفس عائلة وجه الأسد —
  خاتم الزليج الثمانية البارز + شعار DTSG في القلب + نقشة «DIGITAL TRADITIONAL SKILLS GAMES» الأسطوانية + 2026،
  خلفية سوداء وإطار منقّط مطابقان. وجه الأسد (هوية المنصة الحصرية) محفوظ كما هو.
- `04-games.css` :229→`?v=229` للوجهين + `04-games.css?v=dtsg7`. الحارس: `tests/_coin_and_tourney_test.js` **34/34** (يتضمن 7).

### 7) قائمة البطولات = ألعاب مواجهة 2+ المفعّلة فقط (المسألة 7) 🏆
- كانت `openTcModal` تستخدم `allowed` مثبتة `[rn,rp,pn,pn,pr,ke,av,rl,bj,bc]` — تضم كينو/aviator/روليت
  (ليست مواجهة لاعبين) وتفقد ألعاب مواجهة بأكملها (rm/rd/dm/ch/bg/do/billiards الأربعة).
- **الحل**: المصدر الواحد `Object.keys(Rooms.roomGameIds)` (16 لعبة مواجهة، محرّك الأونلاين نفسه) − `DISABLED`
  (ما عطّله السوبر أدمن لا يظهر تلقائياً). تحقّق حيّ: 16 خياراً بلا ke/av/rl (`TOURNEY_LIST_OK`).

### 8) سجلات السوبر أدمن: «سجل الرهان/الفوز/السحب لا تستجيب» — دمج المصادر الثلاثة (المسألة 8) 🧾
- **السبب الجذري**: تبويب «🧾 السجلات» كان يقرأ جدول `transactions` وحده، بينما:
  رهانات وفوز الألعاب تُسجَّل في **`bet_tickets`** (`POST /api/rounds` ← `logTicket`)،
  وطلبات الشحن/السحب الحقيقية في **`pay_transactions`** (pay_*) — فكانت خيارات «رهان/فوز/سحب»
  تعود فارغة دائماً. زد على ذلك تسمية مضلِّلة: `txDeduct`=«سحب» كانت تعني خصم الأدمن اليدوي لا طلبات السحب!
- **الحل**: `/api/admin/transactions` يدمج الآن ثلاثة مصادر مرتّبة زمنياً تنازلياً:
  `transactions` (تحويل/شحن يدوي/خصم/ضبط/مكافآت + رهان وفوز جولات غرف الأونلاين)
  + `bet_tickets` (كل تذكرة = رهان، والرابحة تضيف فوزاً بقيمة payout)
  + `pay_transactions` (شحن/سحب بالدولار مع الحالة والطريقة؛ `user_id` نصي كما فُرض في الحراسة).
- الواجهة: فلاتر «شحن»/«سحب» حقيقيتان في المقدمة، عرض الدولار `$/💸/📥` لصفوف المحفظة،
  شريحة الحالة (مكتمل/قيد المراجعة/مرفوض)، «—» للرصيد غير المتاح، وتغييرت `txDeduct` إلى «خصم يدوي» (4 لغات).
  زر «🧾 سجل» في جدول المستخدمين يعطي الآن **السجل الكامل للمستخدم** (رهانات، نتائج، شحن، سحب،
  تحويلات صادرة وواردة) = جوهر المسألة 8 عبر نفس التبويب بفلتر المستخدم.
- الحارس: `tests/_fin_logs_test.js` **22/22** (ثابت + E2E حيّ: تذاكر + محفظة + دمج + ترتيب + عزل).
- ✚ تسميات `?v=dtsg17` (main + translations) وتحديث مسمار الاختبار الثابت.

---

## v2.56 (2026-09-22) — استعادة ما أفسده مساعد سابق + تنظيف جذري + حرّاس تمنع التكرار

### 0) الحادثة والسبب الجذري (Post-mortem مختصر) 🔴
- مساعد سابق نفّذ `pm2 restart casino-server --update-env` **من صدفة لا تحمل إلا متغيراً واحداً**،
  فمُسحت من العملية كل متغيرات المنصة: `BINANCE_PAY_*` (تعطّل التحقق التلقائي) ·
  `SUPPORT_WEBHOOK_SECRET` (بوت الدعم صار يرد 200 لكل تحديث بلا تحقق) · `ADMIN_API_SECRET` ·
  `TELEGRAM_BOT_TOKEN` (إشعارات الدفع) · `CASH_PLUS_ACCOUNT`/`CIH_*`/`BINANCE_TRC20`.
  ثم `pm2 save` حفظ الحالة المكسورة ⇒ أي إعادة تشغيل تكرّر العطل.
- كما وُضع سرُّ ويبهوك جديد لبوت الدردشة الخاصة على الخادم **دون** استدعاء `setWebhook`،
  وبقيت بلا ويبهوك إطلاقاً ⇒ البوت لا يستجيب.
- وويبهوك بوت المنصة (المالي) كان يشير إلى نطاق ميت (`dtsg-payments.tarikc.workers.dev` ⇒ 404/1042).

### 1) مصدر واحد للبيئة + حرّاس إلزامية 🛡️
- **`scripts/phone-env-restart.sh` (جديد)**: يقرأ `/root/DTSG/.env.local` (غير متعقَّب في git، صلاحيات 600)،
  يرفض العمل إن كان الملف ناقصاً أو إن كان المجلد/الـremote هو المستودع القديم، ثم يعيد تشغيل
  `casino-server` و`dtsg-voucher-bot` و**يتحقق من 15 مفتاحاً داخل العملية الحيّة** قبل `pm2 save`.
- **`scripts/update-phone-server.sh`**: يحمّل `.env.local` تلقائياً + **حارس** يوقف السكربت إن كانت
  أي من 12 مفتاحاً حرجاً فارغة (كان يعيد التشغيل ويحفظ بيئة ناقصة).
- `.gitignore`: إضافة `.env.local` و`.env` (لا سرّ في المستودع العام).
- `scripts/qa-env.sh`: استثناء `.env.local` من نسخة الاختبار في `/tmp`.

### 2) استعادة الخدمات المتضرّرة ✅
- **Binance Pay — تحقّق تلقائي**: عاد (`pay_id=132972522` · `binance_readonly=configured` · الوسيلة `live`).
- **بوت الدعم** (@dtsgsupports_bot) و**بوت الدردشة الخاصة** (@dtsgprivatechat_bot): أعيد ضبط
  `setWebhook` على العنوان **الثابت** `https://casino-phone.dmgames-api.workers.dev/api/{support,private-chat}/webhook`
  مع أسرار متطابقة على الطرفين (كان نطاق النفق المؤقت يتغير ⇒ 503).
- **بوت المنصة (المالي)**: وُجّه إلى `/api/telegram/webhook` على خادم الهاتف (كان نطاقه القديم ميتاً).
- **رقم Cash Plus**: صُحّح الافتراضي إلى `0766672027` مطابقةً لرمز QR الرسمي
  (`assets/qr/cashplus.png` ⇒ `cpmapp://VirementInternScreen?numero=0766672027`) — الرقم القديم في الكود كان
  قيمة اختبار لا تُحوّل إلى حساب حقيقي.

### 3) شرائح البونص: لا ينزل حساب إداري تحت شريحة المستخدم 🎁
- `depositBonusPct`: حساب `admin/super` يأخذ **الأعلى** بين شريحة المستخدمين وشريحة الأدمنز.
  العطل المُبلَّغ عنه: طلب 450$ من حساب السوبر أدمن نال بونص 0% (شرائح الأدمنز تبدأ من 1000$).
- `tests/_bonus_tiers_test.js`: 22 فحصاً (منها 450$ ⇒ 5% · 999$ أدمن ⇒ 8% · 5000$ سوبر ⇒ 30%).

### 4) بوت أكواد التعبئة (@dtsgvoucher_bot) 💳
- **يعرض بيانات الاستلام الحقيقية لكل وسيلة** من `/api/payments/methods` (Cash Plus + الرقم ·
  CIH بالحساب/RIB/IBAN/SWIFT · Binance Pay بـ Pay ID · TRC20 بالعنوان والشبكة) — كان يعرض نصاً ناقصاً بلا حساب.
- **يعرض شرائح البونص** عند إدخال المبلغ (من `/api/promotions`) + قائمة الوسائل من الخادم بتسمياتها.
- إصلاح رابط المنصة: `https://dtsg.pages.dev` (كان الافتراضي يشير إلى منصة أخرى `dmgames.pages.dev`).
- `/codes` يرسل `tg_id` أيضاً (يعمل ولو كان `ADMIN_API_SECRET` غير مضبوط) ويعرض الشرائح عند الخطأ.
- `tests/_voucher_bot_scope_test.js`: 27 فحصاً (منها بيانات الدفع الأربع + الرابط الصحيح).

### 5) تنظيف جذري: لا مستودع قديم يلامس المنصة 🧹
- حُذفت نهائياً: `/root/dmgames-arena` و`/root/digital-moroccan-casino` (worktree للمستودع القديم
  `digital-moroccan-casino`) و`/root/dtsg-voucher-bot` و`/root/dtsg-v241-backup-20260918-2159`
  و`/root/dmgames-payments` (مصدر ووركر غير منشور) و`/tmp/full`.
- **قواعد البيانات محفوظة بالكامل**: أرشيف `/root/dtsg-db-archive-20260922-1644/` (قاعدة القديم بـ42 حساباً ·
  قاعدة arena · قاعدة v241 · قاعدة بوت الفوتشير القديم) + لقطة متسقة للقاعدة الحيّة و`tar` كامل في
  `/root/_dtsg-safety-20260922-1603/`. القاعدة الحيّة المعتمدة تبقى `/root/DTSG/data/royalcoin.db`.
- سُدّت السكربتات التي كانت تعمل على الشجرة القديمة: `phone-doctor.sh` و`apply-support-now.sh`
  (`APP_DIR=/root/DTSG`) · `setup-telegram-bots.sh` (تعليمات الهاتف) · نسخ الاحتياط في
  `apply-support-now.sh` صارت في `/root/_dtsg-tree-backups/`.

### 6) حرّاس أعيد تشغيلها وأُصلحت ✅
- `tests/_pay_v243_test.js`: **27/0** (كان 23/3 — أُضيف إرشاد 403 في `/api/vouchers/create`، وصُحّح
  فحص الهوية المجهولة ليطابق حماية DTSG-016: الجلسة تُثبِّت الهوية، والمجهول بلا جلسة يُرفض).
- `tests/_money_invariants_test.js`: **22/0** (كان 14/8 — كان يرسل طلبات السحب بلا جلسة فتُرفض 401، ويقرأ
  الكوكي من حقل `token` غير الموجود بدل رأس `Set-Cookie`).
- أجنحة أخرى: `_cf_payments_test` 102/0 · `_support_bot_test` 72/0 · `_v247_guard_test` 20/0 ·
  `_repo_origin_test` 13/0 · `_pay_methods_v240` 11/0 · `_wallet_i18n_html_test` 5/0 · `_repo_hygiene` 4/0 ·
  `_private_chat_test` ✓.

---

## v2.55 (2026-09-22) — التدقيق الأمني الشامل والتحصين المالي (DTSG Security Audit Remediations) + تحسينات الضومنة ومركز المساعدة وإصلاح تجمد الرامي

### 0) استئصال تجمد الرامي الجذري واختفاء المجموعات المنزلة (Rami Radical Freeze Elimination & Melds Persistence) 🃏
- **استمرارية المجموعات المنزلة ومنع اختفائها عند إعادة التحميل أو الاستئناف**:
  - حفظ واستعادة خانات اليد كاملة (`handSlots`) والورقة المعزولة (`isolateCardId`) ومؤشر الجوكر (`rules.jokerIndicator` / `rm.jokerIndicatorInfo`) والدورة الأولى (`isFirstTourCycle`, `turnCount`, `feederDiscard`) في التخزين المحلي `localStorage`.
  - إعادة بناء الخانات تلقائياً في `_updateHand()` من المجموعات المنزلة `player.melds` وأوراق اليد `player.hand` حتى في حال إعادة التحميل الباردة أو فقدان الخانات، لضمان بقاء الأوراق المنزلة ظاهرة بحلقتها الذهبية دوماً.
  - الحفظ التلقائي الفوري للحالة عند الضغط على افتتاح (`ramiOpenMelds`) بعد نزول المجموعات بنجاح وقبل رمي ورقة التخلص.
- **استئصال تجمّد اللعبة ومؤقت الـ 90 ثانية جذرياً**:
  - تحرير قفل الانشغال قسرياً (`setRamiBusy(false)`) عند انتهاء مؤقت الـ 90 ثانية في `_handleTurnTimeout()` ومنع حجب اللعب التلقائي نهائياً.
  - إضافة صمام أمان حتمي للتقدم بالدور (`rm.nextPlayer()`) في `_doAutoPlay()` وفي معالج الأخطاء `catch` لضمان عدم تكرار المؤقت 90 ثانية لنفس اللاعب أبداً.
  - تعزيز حارس التجمد المستقل `_watchdog` بفرض اللعب التلقائي `_doAutoPlay` فوراً إذا ظل البوت عالقاً لأكثر من 4 ثوانٍ دون تدوير العداد في حلقة مفرغة.
  - تحصين دالة إنهاء الشوط `_endRound(winner)` للتعامل الآمن مع التعادل ونفاد الأوراق دون فائز (`winner === null`) بدون أي استثناءات برمجية.
  - إضافة 8 اختبارات آلية صارمة ومحاكاة شوط كامل متصل (66 دوراً) في `tests/_rami_persistence_and_freeze_test.js` بنسبة نجاح 100%.

### 0.1) تحسينات واجهة لعبة الضومنة (Dominoes UI Enhancements) 🁫
- **محاذاة أفتار اللاعب الرئيسي في وضع اللاندسكيب (Landscape Avatar Position)**:
  - تضمين شارة اللاعب الرئيسي `#dmSeatMe` داخل حاوية اليد `.dm-handwrap`، بحيث يتموضع الأفتار وشارة النقاط أفقياً بجوار قطع اليد مباشرة ممركزين في أسفل الشاشة، مطابقاً ومناظراً لترتيب الخصم العلوي (`[قطع اللعب] [الأفتار + النقاط]`).
  - الحفاظ على تموضع البورتريه المعتمد فوق منتصف اليد (`bottom: 109px; left: 50%; transform: translateX(-50%)`) دون أي تغيير.
- **نقل البنك لزاوية الشاشة 90 درجة بالأسفل يساراً (Landscape Boneyard Corner)**:
  - تثبيت البنك `#dmBoneyard` في وضع اللاندسكيب بدقة في الزاوية السفلى اليسرى (`bottom: 12px; left: 12px; top: auto; right: auto`).
  - رفع زر التمرير الذهبي `#dmPassBtn` ليكون مباشرة وأعلى البنك عند ظهوره (`bottom: 84px; left: 12px`).
- **تحجيم ديناميكي لقطع اليد لمنع القص عند تجاوز 7 قطع (Dynamic Hand Tile Sizing)**:
  - تطبيق صيغة ديناميكية متجاوبة عبر المتغير `--dm-hand-count` في CSS.
  - إضافة دالة `_updateHandTileScale(count)` في `domino-app.js` لتقليص أبعاد القطع والفواصل `--dm-hand-gap` وهوامش الحواشي `--dm-hand-pad-x` تدريجياً وبسلاسة عند سحب اللاعب لأكثر من 7 قطع (حتى 14 قطعة مسحوبة) لتتسع كافة القطع عبر الشاشة دون أي قص يميناً أو يساراً.
  - عودة القطع لحجمها القياسي تلقائياً عند نزول عددها إلى $\le 7$ قطع، مع تطبيق نفس المرونة في اللاندسكيب والبورتريه على كافة أبعاد الشاشات (من 320px حتى 1024px).
  - إضافة جناح اختبارات هندسي وتحليلي مخصص `tests/_domino_v255_ui_improvements_test.js` يجتاز كافة الحالات بنجاح 100%.

### 1) التحصين الأمني وحماية الأرصدة (Security & Money Protection Findings) ✅
- **DTSG-001: جعل الخادم السلطة الوحيدة للرصيد ومنع التزوير في `/api/sync`**:
  - تم إلغاء قبول أي تعديل على الرصيد (`gold`) من طرف العميل نهائياً؛ الخادم وقاعدة البيانات هما مصدر الحقيقة الوحيد.
  - تجاهل أي قيم رصيد مرسلة وإعادة رصيد الخادم المعتمد حصراً مع رقم المرجع `gold_rev`.
- **DTSG-002 & DTSG-003: حماية السحب واستعلام المحفظة من هجمات IDOR وفرض التوثيق**:
  - حظر الطلبات غير المسجلة على `/api/withdrawals/request` و `/api/wallet/balance` برمز `401 Unauthorized`.
  - إلزامية مطابقة هوية الطالب مع هوية الجلسة المشفرة (`me.id`)؛ منع سحب أو استعلام أي رصيد لحساب آخر حتى لو تم تمرير `user_id` مغاير في الطلب.
  - إضافة `credentials: 'include'` إلى دوال استدعاء المحفظة في `js/wallet.js`.
- **DTSG-005 & DTSG-011: التحقق الصارم من الأصول المسموحة (Strict CORS) وصد CSRF**:
  - منع الانعكاس التلقائي لترويسة `Access-Control-Allow-Origin` مع الكوكيز للأصول الغريبة.
  - حصر الأصول الموثوقة بقائمة بيضاء (`dtsg.pages.dev`, `*.pages.dev`, `*.workers.dev`, `*.e2b.app`, `localhost`).
  - صد طلبات التعديل (`POST`, `PUT`, `DELETE`, `PATCH`) من أي أصل خارجي غير موثوق برمز `403 Forbidden` (`forbidden_origin`).
- **DTSG-006 & DTSG-007: مكافحة التخمين وتوحيد رسائل تسجيل الدخول ومحدِّد المعدل**:
  - إضافة محدد معدل لمحاولات تسجيل الدخول الخاطئة (10 محاولات فاشلة / دقيقة لكل IP/اسم) يردّ برمز `429 Too Many Requests`.
  - توحيد رسالة الخطأ برمز `401 Unauthorized` لكل من الحسابات غير الموجودة وكلمات المرور الخاطئة لمنع حصد وتخمين أسماء المستخدمين.
- **DTSG-009: التوليد العشوائي الآمن تشفيرياً للألعاب الفردية (CSPRNG `_rng()`)**:
  - استبدال استدعاءات `Math.random()` الحساسة في محركات الألعاب الفردية (Dice, Mines, Plinko, Slots, Coin Flip, Hi-Lo, Roulette, Sic Bo, Dragon Tiger, Penalty, Lucky7) بدالة عشوائية آمنة `_rng()` تعتمد على `crypto.getRandomValues()`.
- **DTSG-010: تأمين الدردشة العامة ومنع الإغراق**:
  - فرض تسجيل الدخول قبل إرسال الرسائل (حظر الزوار بـ `401`).
  - تحديد طول الرسالة بحد أقصى 300 حرف، وتطبيق فاصل زمني لا يقل عن 1.5 ثانية بين الرسائل لمنع السبام والإغراق.
- **DTSG-014: تنظيف رسائل الأخطاء من تلميحات الأسرار الداخلية**:
  - إزالة التلميحات التي تكشف ترويسات الإدارة أو معرّفات التيليغرام (`admin_secret`, `tg_id`) من استجابات بوابات الدفع.
- **DTSG-013 & DTSG-017 & DTSG-020: رؤوس الأمان وسياسة أمان المحتوى (CSP / HSTS / CORP)**:
  - إزالة نطاقات الأنفاق العامة (`*.loca.lt`, `*.trycloudflare.com`, `*.lhr.life`) من `connect-src` في `_headers`.
  - ترقية `Strict-Transport-Security` إلى سنة كاملة مع `includeSubDomains; preload` (`max-age=31536000`).
  - إضافة ترويسة `Cross-Origin-Resource-Policy` و `X-Frame-Options: DENY`.
- **DTSG-019: إصلاح مسار قائمة المتصدرين**:
  - تفعيل مساري `/api/lb` و `/api/leaderboard` لإرجاع أعلى اللاعبين رصيداً برمز `200 OK`.

### 2) هندسة مفاصل ومنعطفات السلسلة بدقة 90° وامتداد 7 قطع (7-Tile Segments & Multi-Piece Turn Bridges) ✅
- **تمديد طول كل قطعة مستقيمة في السلسلة إلى 7 أحجار (`maxSeg = 7`)**:
  - تتدفق السلسلة حتى 7 قطع متتالية في كل اتجاه قبل أن تبدأ بالالتفاف، مما يمنح الطاولة امتداداً رحباً ومتناسقاً.
- **جسر التفاف بعرض قطعتين يمنع تكدس الصفوف المتوازية (Multi-Piece Turn Bridges)**:
  - تمتد المنعطفات لأكثر من قطعة واحدة (قطعتان متعامدتان) مما يُبعد الأعمدة والصفوف المتوازية عن بعضها بمسافة فراغية واسعة ومريحة (`2u` على الأقل) ويمنع تماماً تراكم أو ازدحام الأحجار جنباً إلى جنب.
- **مفاصل متلاصقة بدون فجوات (`kind: 'turn'`)**:
  - حساب إحداثيات هندسية دقيقة بزوايا قائمة 90° وتطابق تام للقيم المشتركة على أطراف المفاصل (`touchesPrev`).

### 2) ضبط إحداثيات طاولة الضومنة في وضع البورتريه (Portrait Coordinates) ✅
- **إزاحة بنك القطع (`#dmBoneyard`) للأسفل بمقدار 130 بيكسل**:
  - تعديل موضع البنك ليصبح عند `top: 186px !important; left: 10px !important;` ليكون في موضع حر ومستقل لا يعترض أي عنصر آخر.
- **رفع أيقونة اللاعب الرئيسي (`#dmSeatMe`) للأعلى بمقدار 25 بيكسل**:
  - تعديل تموضع أفاتار اللاعب ليصبح عند `bottom: 109px !important;` لمنحه هامشاً واسعاً فوق شريط أوراق اليد.
- **تحديد حدود السلسلة الآمنة**:
  - ضبط الهامش السفلي للسلسلة في وضع البورتريه إلى `bottom: 170px !important;` لضمان عدم ملامسة السلسلة لأيقونة اللاعب نهائياً حتى عند أقصى درجات امتداد الأحجار.
- **زر الخروج من ملء الشاشة (`.game-fs-exit`)**: مثبت في الزاوية العلوية اليمنى بدقة متناهية (`top: 10px; right: 10px; z-index: 5100;`).
- **أوراق يد اللاعب (`.dm-handwrap`)**: محاذاة أوراق اليد مع الحافة السفلية تماماً للشاشة (`bottom: 0; padding: 0 4px 2px;`).
- **زر التمرير المستقل (`#dmPassBtn`)**: زر أكشن ذهبي متوهج مستقل أسفل يسار الشاشة (`left: 12px; bottom: 12px; z-index: 30;`).

### 3) إعدادات غرفة الضومنة: إضافة اختيار عدد اللاعبين (2، 3، 4 لاعبين) ✅
- **تحديث `js/core/rooms.js`**:
  - ضبط أقصى سعة لغرفة الضومنة `roomGameIds.do: 4` لتمكين إنشاء غرف جماعية حتى 4 لاعبين.
  - إضافة خيار عدد اللاعبين (`maxp`) في نافذة إعدادات الغرفة والجولة (`_gameOptsDefs`) مع خيارات واضحة (`2 لاعبين` · `3 لاعبين` · `4 لاعبين`) والافتراضي 4 لاعبين.
  - تمرير خيار عدد اللاعبين إلى تطبيق الضومنة وإشارة التهيئة (`init`) عبر `domino-room.js` لضبط المحرك التلقائي للمباراة الجماعية.

### 4) مركز المساعدة والشات بوت: استجابة روابط الوصول السريع + التقرير الأفقي للمعاملات ✅
- **إصلاح استجابة الروابط السريعة (`js/ui/bot-chat.js`)**:
  - ربط بطاقة المحفظة وزر «شحن الرصيد» بدالة `window.openWallet()` الرسمية بدلاً من الاستدعاء المعطل السابق، مع دعم الإغلاق السلس للشات وفتح المحفظة فوراً.
  - إصلاح بطاقة «تتبّع معاملة» لتقوم بالانتقال التلقائي إلى تبويب المحادثة (`switchTab('chat')`) فور النقر، ليرى المستخدم فوراً عدّاد التحميل وتقرير معاملاته دون أن تظل النافذة صامتة في تبويب البوتات.
  - دعم فتح بوت خدمة العملاء وبوت التعبئة بشكل آمن ومباشر (`data-bcsupport` و `data-bcvoucher`).
  - اعتراض أوامر المحادثة النصية (`/account` و `/last` و `/معاملات` و `/رصيد` و `/deposit`) لتنفيذها محلياً وفورياً دون إثقال فريق الدعم بتذاكر غير لازمة.
- **إصلاح التنسيق الأفقي لتقرير آخر تعامل بالمنصة (`css/09-chrome.css` و `bot-chat.js`)**:
  - القضاء التام على التشوه والالتفاف العمودي الغريب للبيانات الناتج عن تداخل نصوص RTL/LTR مع وسوم `<br>`.
  - بناء مكون بطاقات معاملات أفقي أنيق (`.bc-tx-box` و `.bc-tx-item`):
    - الصف الأول: أيقونة ونوع المعاملة (إيداع / سحب) + المبلغ بالدولار بخط ذهبي عريض + شارة الحالة الملونة (مكتمل ✅، قيد المراجعة ⏳، مرفوض ❌).
    - الصف الثاني: وسيلة الدفع (Cash Plus، CIH Bank، Binance) + معرّف المعاملة بخط أحادي أنيق.
    - شريط علوي يعرض رصيد الكوينز والدولار الإجمالي للمستخدم بشكل واضح.

### 5) حماية الخصوصية وتحديث صفحة سياسة الدفع والاسترداد (`refund-policy.html`) ✅
- **حذف وتطهير كامل للبيانات الشخصية**:
  - إزالة كافة الأسماء الشخصية (TARIK CHOUIKA)، وأرقام الهواتف الشخصية (0766672027)، وأرقام الحسابات البنكية (CIH IBAN / RIB / SWIFT)، وعناوين محافظ الكريبتو الشخصية (TRC20)، وصور رموز QR الشخصية.
- **شرح تفصيلي وغني لوسائل الدفع عبر اللغات الأربع (العربية، الدارجة، الفرنسية، الإنجليزية)**:
  - إضافة شروحات تعليمية وإرشادية واضحة لطرق الدفع المتاحة (Cash Plus، CIH Bank / CIH Express، Binance Pay & Crypto TRC20، أكواد الكوبونات والهدايا، ونظام السحب).
  - توضيح رسمي بأن بيانات الحسابات المعتمدة ورموز الاستجابة السريعة (QR) يتم عرضها بشكل ديناميكي وآمن داخل المحفظة الرسمية بالمنصة (`js/wallet.js`).

### 4) إعادة تموضع مقاعد اللاندسكيب لتفريغ طاولة اللعب (Landscape Mode) ✅
- **اللاعب الرئيسي (`#dmSeatMe`)**: في الزاوية السفلية اليمنى بمحاذاة الحافة (`bottom: 4px; right: 20px;`) بجوار أوراق اليد مع هامش أمان يمنع التداخل.
- **الخصم الأيمن (`#dmSeat3`)**: بمحاذاة الضلع الأيمن مع تموضع الأفاتار أسفل قطعه (`right: 6px; top: 50%;`).
- **الخصم العلوي (`#dmSeat2` / `#dmSeatOpp.seat-2p-top`)**: بمحاذاة الضلع العلوي مع تموضع الأفاتار على يمين قطعه أفقياً (`top: 6px; left: 50%; direction: ltr;`).
- **الخصم الأيسر (`#dmSeatOpp.seat-left`)**: بمحاذاة الضلع الأيسر مع تموضع الأفاتار أسفل قطعه (`left: 6px; top: 50%;`).

### 4) دوران الأدوار عكس عقارب الساعة (Counter-Clockwise Turn Rotation) ✅
- **تصحيح مسار انتقال الأدوار في محرك اللعبة (`domino-core.js`)**:
  - تصحيح انتقال الدور ليكون **عكس عقارب الساعة** بدلاً من مسار عقارب الساعة السابق:
    - يبدأ الدور من اللاعب الرئيسي (أسفل الشاشة).
    - يليه الخصم في **يمين الشاشة** (`dmSeat3`).
    - يليه الخصم في **أعلى الشاشة** (`dmSeat2`).
    - يليه الخصم في **يسار الشاشة** (`dmSeatOpp`).
    - يليه **اللاعب الرئيسي** (أسفل الشاشة).
  - الصيغة الرياضية `(p - 1 + count) % count` تضمن الدوران الدقيق في وضع 4 لاعبين (`0 → 3 → 2 → 1 → 0`) والتناوب المتبادل المنتظم في وضع لاعبَين (`0 ↔ 1`).
  - تحديث مراقب الذكاء الاصطناعي (`botDrive`) في `domino-app.js` ليعمل مع أي مقعد آلي (1 و2 و3) لضمان عدم توقف الأدوار التلقائية في أي مرحلة.

### 5) أول حرفين من اسم المستخدم داخل أيقونة اللاعب الرئيسي ✅
- استخراج أول حرفين من اسم المستخدم المسجّل (أو المحلي) بصيغة كبيرة ونظيفة (`initials`) وإدراجها نصياً داخل شارة الأفاتار `#dmMyAvatar` (`font-size: 16px; font-weight: 900`).
- الإبقاء على أيقونة الروبوت (`<i class="fa-solid fa-robot"></i>`) للذكاء الاصطناعي في مقاعد الخصوم الثلاثة كما هي تماماً دون أي تغيير.

---

## v2.54 (2026-09-21) — الضومنة: دقة تموضع المقاعد والبنك + الرامي: القضاء النهائي على تجمّد الأدوار وإصلاح قواعد الذكاء الاصطناعي الخبير

### 1) لعبة الضومنة (Domino UI Fine-Tuning) ✅
- **تموضع الأفاتار الخاص باللاعب الرئيسي (`#dmSeatMe`) فوق أوراق يده**:
  - رفع مقعد وأفاتار اللاعب الرئيسي إلى `bottom: 92px` في البورتريه و`bottom: 84px` في اللاندسكيب ليكون فوق قطع اليد تماماً وليس بجانبها.
  - تمركز شريط قطع اليد (`.dm-handwrap`) أفقياً في منتصف أسفل الشاشة (`bottom: 4px`) لملء العرض براحة تامة دون مزاحمة.
- **تموضع أفاتار الخصم الأيمن (`#dmSeat3`) يسار قطعه**:
  - إعادة تنظيم المقعد الأيمن عبر شبكة CSS دقيقة: الأفاتار وشارة النقاط في العمود الأيسر (باتجاه رقعة الجوخ) والقطع العمودية في العمود الأيمن (نحو حافة الشاشة).
- **تموضع أفاتار الخصم العلوي (`#dmSeat2`) تحت قطعه**:
  - ترتيب المقعد العلوي بحيث تكون قطع الخصم في الأعلى والأفاتار وشارة النقاط تحته مباشرة متجهة نحو الجوخ الأخضر (وليس فوق قطعه).
- **تموضع أفاتار الخصم الأيسر (`#dmSeatOpp`) يسار قطعه**:
  - الأفاتار وشارة النقاط على الحافة الخارجية اليسرى والقطع إلى يمينه متجهة نحو رقعة الطاولة.
- **نقل البنك (`#dmBoneyard`) بعيداً عن الخصم الأيسر**:
  - تموضع البنك في الزاوية العلوية اليسرى (`top: 48px; left: 10px;`) مباشرة تحت بطاقة المباراة `R1 T100`، مما أزال أي تكدس أو تداخل مع الخصم الأيسر نهائياً.

### 2) لعبة الرامي (Rami Engine & AI Expert Fixes) ✅
- **القضاء التام على تجمّد عدّاد وأدوار البوت**:
  - إصلاح العطل الجذري في `_handleTurnTimeout()` حيث كان المعالج يتوقف عند `curP.isBot` متجاهلاً استدراك الدور؛ الآن يقوم بتحرير قفل الانشغال وتشغيل اللعب الآلي فوراً (`_doAutoPlay`) لتمرير الدور وتصفير المؤقت.
  - تعزيز حارس المؤقت `_tick()` و`_watchdog()` لفرض دور البوت وتحرير `RAMI_BUSY` تلقائياً إذا تأخرت حركة البوت أكثر من 2 إلى 5 ثوانٍ (في حالات خلفية الهاتف أو بطء المعالجة).
- **منع المخالفات والجزاءات التلقائية (+71 نقطة)**:
  - معالجة ثغرة تفريغ حالة الورقة المسحوبة في `_runBotTurn()`: عند إدراج البوت لورقة المرموق/لا تور في مجموعات الطاولة لم تكن المتغيرات `drawnDiscardCard` و`drawnLaTourCard` تُصفَّر محلياً مما كان يوقع جزاء `DISCARD_DRAW` (+71) عند رمي ورقة التخلص.
  - إضافة دعم تبديل الجوكر على مجموعات الطاولة للبوت (`trySwapJoker`) واستيعابه في `canLayOff()`، مما يتيح للبوت استبدال الجوكر قانونياً وسحبه إلى يده وفق قانون الـ 12 دون أي مخالفة.
  - حفظ خطة الإنزال المباشرة في `chooseDraw()` عند توفر إنهاء فوري بالمرموق، لضمان إنزالها وتفادي أي خطأ في إعادة التقسيم.

---

## v2.53 (2026-09-21) — بوت الدعم: إصلاح استجابة الأوامر والردود + الضومنة: التوزيع الرباعي وتدفق السلسلة الطولي

### 1) بوت خدمة العملاء (Telegram Support Bot) ✅
- **إصلاح عدم استجابة أوامر البوت**:
  - تصحيح التحقق من سرّ الويب هوك (`x-telegram-bot-api-secret-token`) لمنع إرجاع 403 عند تجريد الترويسة بواسطة الووركر أو النفق، مما كان يحجب جميع التحديثات القادمة من تيليغرام.
  - تمكين أوامر المستخدم (`/account` · `/status` · `/privacy` · `/close` · `/unlink` · `/id` · `/whoami`) للأدمن والسوبر أدمن لتمكينهم من فحص رصيدهم وتذاكرهم وحسابهم مباشرة.
  - دعم صيغ الرد السريع المتعددة من الأدمن مثل `2/ مرحبا كيف يمكنني خدمتك` و`/2 مرحبا` و`#2 مرحبا` بالإضافة إلى `2 مرحبا`.
  - تحسين جلب بيانات المستخدم وسجل المعاملات مع دعم جدولي `pay_transactions` و`transactions`.
  - توجيه رسائل الأدمن غير المسبوقة بأمر كنصوص دعم مباشرة أو كردود على التذاكر النشطة.

### 2) لعبة الضومنة: إعادة التوزيع الهندسي الشامل ✅
- **توزيع اللاعبين على منتصفات هوامش الشاشة الأربعة (أمر المالك)**:
  - المقعد الرئيسي (اللاعب) في منتصف الضلع السفلي (`#dmSeatMe`).
  - الخصم الأول في منتصف الضلع الأيسر (`#dmSeatOpp` / `.seat-left`).
  - الخصم الثاني في منتصف الضلع العلوي (`#dmSeat2` / `.seat-top`).
  - الخصم الثالث في منتصف الضلع الأيمن (`#dmSeat3` / `.seat-right`).
  - في نمط لاعبَين: الخصم يتمركز تلقائياً في منتصف الضلع العلوي.
- **إظهار القطع المقلوبة لكل خصم**:
  - الخصم الأيسر: رزمة قطع عاجية عمودية بجانبه (`#dmOppTiles1`).
  - الخصم العلوي: صف قطع عاجية أفقي وسطه (`#dmOppRow`).
  - الخصم الأيمن: رزمة قطع عاجية عمودية بجانبه (`#dmOppTiles3`).
- **اتجاه السلسلة (الاتجاه الأطول)**:
  - في وضع **البورتريه (العمودي)**: تتدفق السلسلة طولياً (رأسياً) من الأعلى للأسفل لتستغل الارتفاع الأكبر للشاشة وتمنع التكدس والتزاحم نهائياً.
  - في وضع **اللاندسكيب (الأفقي)**: تتدفق السلسلة عرضياً (أفقياً) لتستغل العرض الأكبر للشاشة.
- **دقة هندسية وتصحيح القطع**:
  - تصحيح زاوية دوران القطعة الأولى والأخيرة في السلسلة لتكون الأطراف المفتوحة والملتصقة صحيحة هندسياً وحسابياً 100%.
  - التصاق تام بدون فجوات (`GAP = 0`) مع توسيط تلقائي على رقعة الجوخ الأخضر.
- **المواقع المستقلة للبنك والهدف (R / T)**:
  - شارة المباراة `R1 T100` في الزاوية العلوية اليسرى.
  - بنك القطع (Boneyard) في الزاوية السفلية اليسرى بعيداً عن السلسلة ومقاعد اللاعبين.

---

## v2.52 (2026-09-21) — الضومنة: دعم 2 و3 و4 لاعبين + مطابقة النماذج المرجعية + إصلاح تراص السلسلة

(تطوير شامل لمحرك لعبة الضومنة لدعم 2 و3 و4 لاعبين حسب القواعد والوثائق الرسمية، مع معالجة كافة الملاحظات البصرية وفق النماذج المرفقة.)

### 1) محرك الضومنة (DominoCore & DominoGame) لـ 2 و 3 و 4 لاعبين ✅
- **توزيع القطع حسب القواعد الدولية والمغربية**:
  - **لاعبان**: 7 قطع لكل لاعب (14 موزعة، 14 في البنك للسحب).
  - **3 لاعبين**: 7 قطع لكل لاعب (21 موزعة، 7 في البنك).
  - **4 لاعبين (كلاسيكي / بدون بنك)**: 7 قطع لكل لاعب (جميع الـ28 قطعة موزعة، البنك 0).
  - **4 لاعبين (سحب)**: 5 قطع لكل لاعب (20 موزعة، 8 في البنك).
- **دورة الأدوار**: دوران ديناميكي بين اللاعبين النشطين `0 -> 1 -> ... -> (N-1) -> 0`.
- **حساب الفوز والنقاط**:
  - عند إفراغ اليد (`handEmpty`): الفائز يحصل على مجموع نقاط كل الخصوم في الجولة.
  - عند الانسداد (`blocked`): يحسب مجموع نقاط كل لاعب، صاحب أقل نقاط يفوز بالفرق عن باقي اللاعبين، والتعادل 0 نقاط.
  - شرط الانسداد: تمرير متتالي لجميع اللاعبين (`passes >= N`).
- **الذكاء الاصطناعي (DominoAI)**: يدعم اتخاذ القرارات وحساب الاحتمالات لأي مقعد (الخصم 1، 2، 3).
- **الواجهة والقائمة**: إضافة خيار اختيار عدد اللاعبين في شاشة الإعدادات (`2 لاعبين` · `3 لاعبين` · `4 لاعبين`).

### 2) مطابقة النماذج المرجعية ومعالجة الملاحظات البصرية ✅
- **إصلاح اختفاء أيقونات وقطع اللاعبين**: منح كل عناصر الواجهة التموضع والطبقات الصحيحة (`position: absolute; z-index: 10`) حتى لا تحجبها رقعة الجوخ.
- **الشارات**: شارات دائرية أنيقة للاعبين مع أيقونة مميزة وتوهج أخضر ناصع في دور اللاعب النشط (`.myturn`)، وشريط نقاط ذهبي مدمج تحت الأيقونة.
- **قطع الخصم**: صف عاجي متمركز أعلى الشاشة مع ظلال واقعية وتحديد دقيق.
- **بطاقة المباراة (Round / Target)**: تصميم أنيق يحاكي لوحة الخشب والزمرد في النماذج المرجعية ويعرض `R1 T100` / الجولة والهدف.
- **السلسلة متراصة ومتناسقة**:
  - إصلاح التموضع المطلق للقطع داخل السلسلة (`position: absolute !important;`).
  - إزالة الفجوات بين القطع (`GAP = 0`) لتلتقي القطع المتجاورة وجهاً لوجه كأحجار الضومنة الحقيقية.
  - منعطفات هندسية 90 درجة ناعمة تربط الصفوف بسلاسة وتوسيط تلقائي على الرقعة.
  - توهج أصفر بارز للقطع القابلة للعب مع تلميحات أطراف واضحة.
- **التوافق التام**: تموضع مخصص ومثالي لكل من وضعي البورتريه واللاندسكيب.

---

## v2.51 (2026-09-21) — الضومنة: الجوخ الكلاسيكي + التموضع بأمر المالك + المحفظة + الشات

(تستبدل معاينة v2.50 غير المنشورة: أُعيد تصميم الجوخ الكامل السابق كما كان،
وطُبقت المواصفات المطلوبة فقط — تموضع + ترتيب مفاصل — بلا أي تغيير بصري آخر.
`dominoes.css` و`21-classic.css` **بلا أي تعديل**.)

### 1) الضومنة: نفس الشكل السابق + المواصفات حرفياً ✅
- **الجوخ**: كامل كما في السابق (بلا أعمدة بنّية ولا تضييق) · اليد أسفل ·
  القطع الصفراء القابلة للّعب والنقاط كما هي.
- **قطع الخصم أعلى الشاشة** (صف متمركز) في الوضعين.
- **بلا عبارات**: حُذف شريط الـHUD — الجولة والهدف شارة `R1 T100` فوق البنك
  يساراً · الدور **توهّج أخضر** على أيقونة صاحبه · الأيقونة دائرية تحمل
  **أول حرفين من اسم المستخدم** وبجنبها رقم النقاط **بلا حاوية** ·
  الأسماء والحالة وتسمية البنك مخفية في الـDOM (لا شيء مرئي).
- **الشارات**: الخصم تحت قطعه فوق الجوخ · اللاعب: بورتريه وسط فوق اليد ·
  لاندسكيب الزاوية اليمنى السفلى.
- **السلسلة**: مستطيل وسط الجوخ بإزاحات `px` صريحة تُخلي البنك (لا نسب %)
  · القطع **متراصة متلاصقة** والمفاصل **متطابقة القيم** (انتشار القيمة
  المفتوحة `jointFacing` بدل `leftMatchIsA` الذي كان يعكس القطع الملحقة عبر
  `b`) · **التفاف مبكر** يحجز عرض المنعطف ⇒ لا فيض ولا قصّ · **إزاحة
  التلميحات تُقاس حيّاً** من الـDOM بدل الثابت `30/6`.
- ملفات: `domino-html.js` (إزالة الـHUD + عمود `R/T` فوق البنك) ·
  `22-look.css` (قسم `[v2.51-DOMINO]` فقط) · `domino-renderer.js` (المفاصل +
  الالتفاف + الإزاحة + `R/T`) · `domino-app.js`/`domino-room.js` (الأحرف
  الأولى + توهّج الدور).
- التحقق: حزام `layoutChain` (329 بنداً: مفاصل متطابقة + داخل الحدود في
  5 سيناريوهات) · `tests/_look_v248_test.js` حُدّث للعقد الجديد (لا HUD ·
  `R/T` فوق البنك · توهّج واحد · لا عبارات) · `_classic_look` ساري بلا تغيير.

### 2) المحفظة: إزالة خيار Binance Pay المكرر ✅
- تبويب الشحن كان يعرض **خيارين**: «Binance Pay — تحقّق تلقائي» و«Binance Pay»
  (طلب دفع) ⇒ أُبقي الأول وأُخفي الثاني من القائمة (`renderMethods` في
  `js/wallet.js` — تصفية عرضية فقط، الخادم والـAPI والاختبارات untouched،
  وسحب Binance Pay باقٍ في تبويب السحب).

### 3) الشات بوت: إصلاح الرسائل والأزرار المقصوصة ✅
- **العطل المُثبت**: الكلاسان `.bc-b` و`.bc-card` مستعملان أيضاً في **الباكارات**
  (`css/04-games.css`: زر رهان `flex` ممركز + ورقة لعب `58×84` مخفية `overflow`)
  فتسرّبت قواعدهما للشات ⇒ فقاعات ممركزة مقصوصة وأزرار `58×84`.
- **الإصلاح** (`css/09-chrome.css`): تقييد قواعد الشات بالأب `.botchat`
  (تخصيص `0,2,0` > `0,1,0`) + تصفير صريح (`display:block` · أبعاد حرة ·
  `overflow:visible` · `opacity:1` · بلا animation) ⇒ يصلح الاتجاهين
  (الشات سليم والباكارات لم تعد تتأثر بقواعد الشات).
- حارس جديد في `tests/_botchat_ui_test.js` (فقاعة `block` · بطاقة بعرض حر بلا قصّ).

---

## v2.48.1 (2026-09-21) — نشر v2.48 + إصلاحات كشفها حارس التصميم بالمتصفح (مساعد الهاتف cat)

### 1) النشر على Cloudflare Pages ✅
- `bash scripts/deploy-pages.sh` ⇒ Deployment `3c10b39f` (Production · main · مصدر `362d8ea`) على مشروع `dtsg`.
- **تصحيح مهم في فحوص التكليف (Issue #3)**: `https://dtsg.pages.dev/index.html` يعيد **302 → `/` بجسم فارغ**،
  فالفحص `curl .../index.html | grep -c 22-look.css` يعطي **0 خطأً**. الصحيح: `curl -sL .../` (أو `-L`).
  بعد التصحيح: `22-look.css` في HTML = **1** · `css/22-look.css` = **200** ومطابق للمصدر بايتاً ·
  `21-classic.css` يحوي `bw-screen-active` = **5** · الـworker `{"ok":true}` · بصمة HTML الحيّ = بصمة `index.html` المحلي.
- **تحذير تنفيذي لمن يأتي بعدنا**: خطوة التكليف `cd /root/dmgames-arena && git checkout main && git pull`
  **خطِرة في هذا الصندوق**: `/root/dmgames-arena` هي worktree لمستودع **آخر**
  (`digital-moroccan-casino` · فرع `arena/samsung-fixes-20260911`) ⇒ تنفيذها يطمس الشجرة الحيّة بكود قديم.
  الطريقة الصحيحة المعتمدة: الجلب في `/root/DTSG` (main) ثم نسخ الملفات إلى الشجرة الحيّة، ثم `pm2 restart`.

### 2) عطل حقيقي: سطر «Pips» كان مخفياً في بطاقة الطاولة على الهواتف
- السبب: `backgammon-game/css/backgammon.css` فيه `@media (max-width:640px){ .bw-seatpip{display:none} }`
  ⇒ البطاقة كانت **سطرين** (الاسم · Score) بينما المرجع الملزم يطلب **ثلاثة** (الاسم · Score · Pips).
  والعنصر موجود في الماركب **داخل** `.bw-seatrow` (صف) لا كسطر مستقل.
- الإصلاح (`css/22-look.css`): إعادة إظهار السطر بـ`display:inline-block!important` من طبقة التصميم،
  وتحويل `.bw-seatrow` إلى عمود في البورتريه (كما اللاندسكيب) مع ترتيب `order` (Score ثم Pips)
  و`flex:0 0 auto` + `white-space:nowrap`.
- عطل ثانٍ في نفس السطر: كان يُطبع «Pips: نقاط: 167» (عربي) و«Pips: Pips:» (fr/en) لأن JS يكتب
  `T('bg.pip')+': '+value` بينما CSS يضيف `content:'Pips: '` ⇒ صار JS يكتب **القيمة فقط**
  (`backgammon-game/js/ui/bg-app.js`).
- التحقق: `tests/_look_v248_test.js` — بند «بطاقة اللاعب ثلاثة أسطر» **✓** في الوضعين.

### 3) عطل حقيقي: البنك (المخزون) في الضومنة على الجهة المعاكسة
- السبب: `#dmStage .dm-boneyard` كان بـ`inset-inline-start:8px` ⇒ في RTL يعني **يمين** الشاشة،
  فيتزاحم بصرياً مع رزمة الخصم (`right:0`) وتبقى الحافة اليسرى فارغة — عكس المرجع
  («البنك على اليسرى · رزمة الخصم على اليمنى»).
- الإصلاح: خصائص **فيزيائية** `left:8px; right:auto` (لا تُقلب مع RTL) ⇒ البنك يساراً والخصم يميناً
  في العربية واللاتينية معاً. التحقق: بند «البنك على الحافة اليسرى» **✓** بورتريه ولاندسكيب.

### 4) عطل في الحارس نفسه: فحوص الضومنة لم تكن تُنفَّذ (نجاح زائف)
- `tests/_look_v248_test.js` كان يضغط زر بدء الضومنة عبر اختيارات **غير موجودة**
  (`.dm-start` · `.dm-gobtn` · `button.big`) ثم «آخر زر في القائمة» ⇒ لا تبدأ المباراة ⇒ انتهاء مهلة ⇒
  **إسقاط كل بنود الضومنة بصمت** (14 فحصاً فقط تُنفَّذ والناتج يبدو أخضر).
- الإصلاح: الضغط على **`#dmStartBtn`** الحقيقي (مع بدائل). النتيجة بعد الإصلاح: **50 نجح / 0 فشل**
  وبنود الضومنة تُفحص فعلاً (قطع اليد · لا قصّ · النقاط داخل القطع · الجوخ أخضر · القابلة للّعب صفراء ·
  البنك/الخصم · توزيع اللاعبين · 0 أخطاء صفحة).
- بند «توزيع اللاعبين» صار يُميّز الوضع: بورتريه (جنباً إلى جنب: الخصم يساراً) ·
  لاندسكيب (بطاقتان في العمود الأيسر فوق بعضهما) — بدل اشتراط `top.x < bot.x` في الوضعين.
- **رسالة تنسيق للفريق**: أي تعديل تصميم لاحق ⇒ شغّل الحارس وتأكّد من **عدد الفحوص (50)** لا اللون فقط.

### 5) تشغيلياً
- بقي `NODE_EXTRA_CA_CERTS=''` في `casino-server` و`dtsg-voucher-bot` (طلب التلغرام يعمل ✓).
- لا إعادة تشغيل ولا ترحيل مطلوب في v2.48/v2.48.1 (واجهة فقط: css/js/html) — والقاعدة الحيّة لم تُلمَس.

### التحقق النهائي
- `tests/_look_v248_test.js` **50/0** (متصفح حقيقي: بورتريه 390×780 · لاندسكيب 1280×600).
- فحوص النشر الأربعة بعد التصحيح ✅ · الشجرة الحيّة `/root/dmgames-arena` مُزامنة مع `main`.

---

## v2.47 (2026-09-20) — بوت الفوتشير لأكواد التعبئة فقط + دمج في المنصة + بايننس بوضع القراءة فقط + السحب لأدمن الحساب

من رسالة المالك: «١) بوت الڤوتشر متضرر يجب أن يكون خاصاً بأكواد التعبئة فقط… ودمجه في الشات بوت
في المنصة مع بوت دعم العملاء. ٢) لا أريد تفعيل السحب عبر API بايننس — وضع القراءة فقط للتأكد
من وصول مبلغ المستخدم لمعرّفي (Pay ID/QR) فيُشحن تلقائياً. ٣) السحب: بيانات السحب تصل للأدمن
الذي سجّل حساب المستخدم وينفّذ ويصادق. ٤) تسرّب رموز `<b>` في المحفظة» (البند ٤ نُفِّذ في v2.46.1 أعلاه).

### 1) بوت الفوتشير (@dtsgvoucher_bot) = أكواد التعبئة فقط
- أُزيلت من البوت نهائياً: **طلب السحب** (`startWithdraw`/`submitWithdraw` وزرّه وخطوة `wd_amount`)،
  و**استعلام الرصيد** (`showBalance` وزرّه و`/balance`)، وزر/مسارات **الدعم**.
- اللوحة الآن: `🎟️ شحن سريع (كود تعبئة)` + `🔗 ربط/تغيير الحساب` فقط؛ والمصادقة على أزرار
  الأكواد `dapp_/drej_` **حصرياً** (أزرار السحب `wapp_/wrej_` لم تعد تصدر من البوت).
- أي رسالة خارج النطاق (سحب/رصيد/دعم/`/balance`/`/support`) ⇒ رسالة توجيه واضحة:
  السحب من **المحفظة** في المنصة، والدعم عبر **بوت خدمة العملاء**، والرصيد في المحفظة.
- المسار الكامل لأكواد التعبئة كما هو: ربط الحساب ← وسيلة الدفع ← المبلغ ← مرجع العملية
  ← دليل الدفع (نص/صورة) ← مصادقة الإدارة ← **الكود يصله في البوت** ← تفعيله في المحفظة.

### 2) دمج بوت التعبئة في مركز المساعدة بالمنصة
- `js/ui/bot-chat.js`: بطاقة **«بوت أكواد التعبئة»** بجانب بطاقة بوت الدعم، ورابطها يحمل
  معرّف المستخدم (`?start=plt_<id>`) فيربط البوت حسابه تلقائياً.
- `js/wallet.js`: زر «🎟️ بوت أكواد التعبئة» في تبويب الشحن (يفتح البوت بنفس الربط التلقائي).
- مفاتيح ترجمة جديدة (ar/fr/en/دارجة): `bc.cardVoucher|cardVoucherSub|cardVoucherLogin`
  · `wl.voucherBotBtn|voucherBotNote`.

### 3) بايننس: تحقّق «قراءة فقط» + شحن تلقائي (بلا أي صلاحية سحب)
- نواة المدفوعات: `GET /sapi/v1/pay/transactions` (سجل Binance Pay) موقَّعاً HMAC-SHA256
  بترويسة `X-MBX-APIKEY` — **لا** أمر سحب ولا تحويل ولا إنشاء طلب دفع. (`binanceSapiSilent`).
- `POST /api/payments/binance-verify`: يتحقق من **مبلغ مطابق (±0.02)** في نافذة زمنية (افتراضياً 120 دقيقة)
  وكون الصف وارداً وواصلاً لمعرّفنا (`BINANCE_PAY_ID`) ثم يشحن عبر `completeDeposit` (بالبونص وسجل المال).
  الاستجابات: `credited` · `already` (نفس `transactionId` ⇒ لا شحن مزدوج) · `not-found-yet` (أعد المحاولة)
  · `readonly-unavailable` مع **رمز Binance وIP الطلب** للتشخيص.
- `GET /api/payments/binance-probe`: فحص حيّ للمفتاح (أدمن فقط) يشرح هل يستجيب للقراءة أم أنه مرفوض.
- المحفظة: وسيلة **«Binance Pay — تحقّق تلقائي»** تعرض **معرّف Pay (Pay ID)** + QR محلي (QRMini)
  وزر «⚡ تحقّق من التحويل»؛ ومفاتيح ترجمة جديدة `wl.mBnbRO|roPayId|roHint|roVerify`.
- القاعدة: `binance_readonly` مُضاف إلى `CHECK(method)` في `cf-worker/schema.sql` و`server-payments.js`
  (+ ترحيل تلقائي عند الإقلاع).
- **اختبار حيّ للمفتاح الحالي**: `-2015 Invalid API-key, IP, or permissions` ⇒ المفتاح يحتاج ضبطاً في
  حساب Binance: **Enable Reading** فقط (بلا Enable Withdrawals) و**بلا قيد IP** (IP الخادم متغيّر:
  رُصد `41.140.47.188`). حتى ذلك الحين يعمل المسار اليدوي (Binance Pay يدوياً + بوت الأكواد).

### 4) السحب: يذهب لأدمن حساب المستخدم (users.admin_id)
- `notifyWithdrawalOwner()`: إشعار طلب السحب (بأزرار ✅/❌) يُرسل إلى **تيليغرام أدمن التسجيل**
  (`users.admin_id`) + نسخة رقابية للسوبر أدمن. مسارا `/api/withdrawals/request` و`/api/bot/request`
  (سحب) يستعملانه الآن.
- المصادقة على السحب (`wapp_/wrej_`) محصورة بـ**أدمن الحساب أو السوبر أدمن**:
  `adminActOnTransaction(..., actorTg)` ترفض غير المصرَّح (`not-owner-admin`)، ومسار الاستعلام من
  تيليغرام لا يكشف حالة المعاملة لغير المصرَّح (403).
- لوحة المنصة (`/api/admin/payments/act`): السحب يصادق عليه **أدمن الحساب أو السوبر** (403 لغيرهما).
- خطافات المنصة الجديدة: `__userAdminOf` · `__isUserAdminOf` (server-payments.js).

### 5) إصلاح حرج للإنتاج: تعطّل كل نداءات HTTPS الصادرة
- كلا الخدمتين (`casino-server` و`dtsg-voucher-bot`) كانتا تحملان `NODE_EXTRA_CA_CERTS` يشير إلى ملف
  CA خاص بأداة CLI ⇒ **كل نداء Node للتلغرام كان يسقط** (`fetch failed`/ETIMEDOUT)، أي أن بوت
  الفوتشير كان **لا يستقبل أي تحديث** وإشعارات الدفع لا تخرج.
- الإصلاح: تصفير المتغيّر في بيئة الخدمتين (`NODE_EXTRA_CA_CERTS=`) + إعادة التشغيل + `pm2 save`.
  الدليل: بعد الإصلاح يستجيب Node للتلغرام (getMe ✓)، ولم تعد الأخطاء متصلة (بقي **زمن انتقال
  متقطّع** في شبكة الهاتف ~⅓ من المحاولات — يعالجه حلقة إعادة المحاولة في البوت تلقائياً).

### 6) الدمج مع عمل «أرينا» (GitHub) — بلا فقدان أي إصلاح
- دُمج فرع `arena/01a0bd39-dtsg` (v2.45.1: RSA لإشعارات Binance · QR محلي بدل api.qrserver.com ·
  حماية CSP · ترحيل مع foreign_keys=OFF · مسح حساب صريح…) فوق v2.46/v2.46.1.
- حُلّت التعارضات يدوياً: CHANGELOG (القسمان معاً) · `payments-core` (وثيقة التحقق RSA + وثيقة الشرائح
  الجديدة، وإزالة تعليق الشرائح القديم المُضلِّل) · `index.html` (سكربت qr-mini + `wallet.js?v=pay9`).
- **استُعيد مفتاح `wl.noteCash`** الذي حُذف سهواً في نسخة أرينا (كان شرح Cash Plus يفقد ترجمته).
- اختبار أرينا «نفس المعرّف أُرسل لـBinance» كان يعتمد على `آخر نداء /v2/order` (هشّ) ⇒ صار يتحقق من
  «آخر نداء إنشاء» ويطابق الهوية بحسب المسار (v3 الشخصي بلا `merchantTradeNo` · v2 التجاري به).
- أُصلحت أيضاً فجوة النشر: الشجرة الحيّة كانت بلا `css/21-classic.css` ⇒ نُقلت مع النسخة المطابقة.

### التحقق
- `tests/_cf_payments_test.js` **102/0** (يشمل: توجيه السحب لأدمن الحساب · رفض غير المصرَّح · شحن تلقائي
  عند تطابق التحويل · بلا شحن مزدوج · تشخيص رفض المفتاح · probe · methods).
- `tests/_voucher_bot_scope_test.js` **19/0** (نطاق البوت: لا سحب/رصيد/دعم + توجيه خارج النطاق).
- `tests/_v247_guard_test.js` **20/0** (أحراس الانحدار للبنود الثلاثة) · `tests/_bonus_tiers_test.js` ✅
  · `tests/_wallet_i18n_html_test.js` **5/0** · `tests/_vouchers_ui_test.js` **17/0**
  · `tests/_pay_methods_v240_test.js` **11/0**.
- تحقق حيّ: `/api/payments/methods` يعرض `binance_readonly=live` و`binance_pay_id=132972522`،
  والترحيل ظهر في سجلات الخادم: `migrated pay_transactions.method → +binance +binance_pay +binance_readonly`.

---

## v2.46.1 (2026-09-20) — إصلاح تسرّب وسوم HTML في نافذة المحفظة وشروحات طرق الدفع

### المشكلة (مبلَّغ عنها من المالك)
ظهور رموز مفاتيح مثل `<b>` حرفياً داخل نافذة المحفظة وفي شروحات طرق الدفع بداخلها.

### السبب الجذري
مفاتيح القاموس `wl.noteCrypto` · `wl.noteCash` · `wl.noteVoucher` · `wl.noteWd` في `js/i18n/translations.js`
تحمل وسوم `<b>…</b>`، وكانت مربوطة بـ `data-i18n` في `js/wallet.js` — و`translateStatic()`
في `js/core/utils.js` يضبط مسار `data-i18n` عبر `el.textContent` ⇒ يُعرض الوسم كنص خام
(بينما مسار `data-i18n-html` يستخدم `innerHTML` وهو المقصود للنصوص المنسّقة).

### الإصلاح
- ربط شروحات المحفظة الأربعة بـ **`data-i18n-html`** (كما هو النمط في `index.html` لمفاتيح
  `ui.heroTitle` و`fair.toolDesc`) ⇒ يظهر التنسيق العريض فعلاً: **Binance Pay** · **Cash Plus** ·
  **كوبون** · **السحب**.
- رفع ذاكرة المتصفح لملف المحفظة: `js/wallet.js?v=pay7` (كان `pay6`) في النسختين
  (`/root/DTSG` و`/root/dmgames-arena` الحيّة).

### التحقق
- حارس جديد `tests/_wallet_i18n_html_test.js` (**5/0**): يفحص كل ربط `data-i18n` · `data-i18n-placeholder` ·
  `data-i18n-title` في `index.html` و`js/**/*.js` (225 ربطاً) ويرفض أن تحمل أي قيمةٍ وسماً،
  ويؤكد بقاء `<b>` في الشروحات (لم يُفقَد التنسيق)، ووجود كل مفتاح مربوط في القاموس.
- بلا انحدار: `tests/_vouchers_ui_test.js` **17/0** · `tests/_pay_methods_v240_test.js` **11/0**.
- تحقق حي على الخادم العامل (منفذ 3000): `js/wallet.js?v=pay7` يقدّم `data-i18n-html` للشروحات الأربعة،
  و`index.html` يشير إلى `v=pay7`.

### معلّق (من طلب المالك نفسه — لم يُنفَّذ بعد؛ الجلسة انقطعت قبل التعديل)
1. تخصيص بوت الفاوتشر **لأكواد التعبئة فقط** (لا دعم/سحب/استعلام رصيد) + دمجه في شات المنصة مع بوت الدعم.
2. **شحن بايننس بوضع القراءة فقط**: التحقق من تحويل المستخدم إلى معرف Pay/QR (بلا أي سحب عبر API).
   ملاحظة: بوابة Binance Pay ترفض المفتاح الحالي بـ `400004` لأن إنشاء طلب Pay يتطلب مفتاحاً بصلاحية
   سحب/تاجر — ومفتاح المالك Read-Only، وهو ما يتوافق مع المطلوب (قراءة فقط).
3. سحب المحفظة محصور: طلب المستخدم يصل **للأدمن الذي سجّل حسابه** لينفّذ ويصادق.

---

## v2.46 (2026-09-19) — إصلاح خلط شرائح البونص بين المستخدمين والأدمنز + تحويل صحيح لإشعارات طلبات التعبئة

### المشكلة (مبلَّغ عنها من المالك)
- تجربة شحن عبر **بوت الفاوتشر** أنشأت كود تعبئة لحساب مستخدم عادي **ببونص شريحة الأدمنز (+25% لـ100$)**.
- السبب الجذري: `depositBonusPct()` في `cf-worker/payments-core.js` كانت تطبّق `ADMIN_TIERS` على **كل** الشحنات
  بغضّ النظر عن دور صاحب الحساب — فانتقلت شرائح أكواد الأدمنز إلى المستخدمين العاديين
  (خطأ حديث في «Codes 2026-09-17»: كانت الشريحة 100$/25% منضوية تحت الأدمنز).

### الإصلاح (نظام شرائح واحد بلا خلط)
- **أدمنز** (أكواد خاصة بإصدار السوبر أدمن `/codes admin` + شحنات حساب إداري فعلي): 1000→30% | 10000→35% | 100000→40%.
- **مباشر** (أكواد خاصة `/codes direct`): 10→0% | 100→5% | 1000→10% | 10000→15%.
- **المستخدم العادي** (شحن بوت الفاوتشر/المنصة بلا كود مسبق): 100$→5% · 500$→8% · 1000$→10% · 5000$→15% · 10000$→20%
  (الدرهم: 1000→5% · 5000→8% · 10000→10% · 50000→15% · 100000→20%). تحت أول شريحة ⇒ **0%**.
- `depositBonusPct(usd, role, currency)` الآن: `role` يُحلّ من قاعدة المنصة (`users.role`) عبر `userRoleOf()`
  (وخطاف `env.__userRole` للمنصات التي تحمّل المستخدمين في الذاكرة) — شرائح الأدمنز لا تُطبَّق إلا على `admin|super`.
- ممرّ البونص الثلاثة كلها أصبحت تمرّر الدور: `completeDeposit` (تأكيد الإيداع) · فرع الكود في `adminActOnTransaction`
  (مصادقة طلب تعبئة) · `POST /api/bot/request` (معاينة البوت).
- `/api/bot/request` يعيد الآن `currency` و`coins_on_approve` محسوبين بعملة الطلب (دولار افتراضياً؛ الدرهم بـ`currency:'mad'` صراحةً)
  — بدل تقدير الدولار لكل الوسائل.
- استجابة `POST /api/bot/request` لمصادقة طلب تعبئة ترسل أزرار `dapp_/drej_` (مسار الكود) — `wapp_/wrej_` حُصرا لمسار السحب.
- بوت الفاوتشر: تحقق مبكر من المبلغ (1$–1,000,000$) قبل إرسال الطلب.

### التحقق
- `node --check` سليم للملفات المعدَّلة، واختبار `tests/_cf_payments_test.js` (بدون خدمات خارجية) يجري بنجاح
  بعد تحديث توقعات الشرائح (130,000 كوين لكود أدمنز 1000$)، واختبارات المال/البوت حُدّثت لشرائح المستخدمين (100$ ⇒ 10,500 كوين).

---

## v2.48 (2026-09-20) — إصلاح اللعب والتصميم للضومنة والطاولة (ثلاث مراحل)

من لقطات المالك: «الطاولة غير قابلة للعب — اللوحة تغطي نافذة الإعدادات» و«الضومنة مشوّهة: قطع ناقصة
ومقصوصة في منطقة بنّية، واللاعبان محشوران في الأعلى». المراجع الملزِمة في
`docs/DESIGN_SPEC_dm_bw.md`.

### المرحلة 1 — [v2.48-FIT] قابلية اللعب والملاءمة (`css/21-classic.css`)
- **الطاولة غير قابلة للعب**: `#bwStage #bwPlay{display:grid|flex!important}` (لاندسكيب/بورتريه) كانت
  تُظهر شاشة اللعب **فوق** شاشة الإعدادات ⇒ كل نقرة تصطدم بطبقة ميتة. الحل (من v2.45.1، لم يكن مدموجاً):
  تقييد القاعدتين بـ`.bw-screen-active` + قاعدة `display:none` للشاشات غير النشطة.
- **الضومنة مقصوصة**: `.dm-screen{position:absolute;inset:0;overflow-y:auto}` +
  `.dm-table{min-height:140px}` + قطع كبيرة ⇒ تمرير داخلي يقتطع اليد. الحل: المرحلتان تملآن
  `#gamePageBody` (`position:absolute;inset:0`)، الطاولة تتقلّص (`flex:1;min-height:0`)، واليد محدودة.
- **المنطقة البنية**: أُزيل الرفّ الخشبي `.dm-handwrap::before` والجوخ البنّي من اللاندسكيب ⇒ جوخ أخضر
  في الوضعين. اللاندسكيب: المقاعد متباعدة (الخصم أعلى-يمين · أنا أسفل-يسار).
- كاش: `css/21-classic.css?v=look1 ⇒ look2`.

### المرحلة 2 — [v2.48-LOOK] مطابقة التصميم (`css/22-look.css` جديد)
- **الضومنة**: ميزانية القطعة `--dm-tile-h` (ارتفاع محدود بالشاشة والعرض معاً) + عرض ثابت 0.54؛
  `.dm-face{inset:0}` و`overflow:visible` على اليد ⇒ **النقاط داخل القطعة دائماً والقطع كاملة**؛
  القطع المعطّلة عاجية واضحة (لا شبه شفافة) والقابلة للّعب صفراء متوهّجة؛ المسار متمركز؛ البنك صندوق
  على الحافة اليسرى؛ رزمة الخصم عمود رأسي بظهر عاجي على الحافة اليمنى؛ أدوات دائرية فوق اليد.
- **الطاولة**: بطاقة اللاعب ثلاثة أسطر (الاسم · `Score:` ذهبي كبير · `Pips:` فاتح)؛ المصادر خضراء نابضة
  والوجهات خضراء متقطعة والمختار بحلقة ذهبية؛ النرد 56px (بورتريه) عاجي بنقاط داكنة؛ بلا تمرير داخل
  شاشة اللعب؛ اللاندسكيب: البطاقات يساراً واللوحة يميناً بكامل الارتفاع.
- حارس جديد `tests/_look_v248_test.js` (متصفح): عدم تراكب الشاشات، عدم التمرير، كمال القطع، نقاط داخل
  القطع، مواضع البنك/الرزمة، لون الجوخ، أصفر القطعة القابلة للّعب، أسطر البطاقة، حجم النرد — بورتريه ولاندسكيب.
- توثيق: `docs/DESIGN_SPEC_dm_bw.md` (عقد DOM + المقاسات + الممنوعات + الفحوص).

### المرحلة 3 — [v2.48-SEATS] توزيع اللاعبين في الزوايا + إغلاق
- **العطل المُثبت:** ترتيب DOM يضع الخصم أولاً و«أنا» ثانياً، وداخل حاوية **RTL** يصبح الأول في
  **اليمين** ⇒ كان الخصم يميناً و«أنا» يساراً (معكوس عن المرجع). الإصلاح بالـ`order` فقط
  (لا تغيير DOM ⇒ لا كسر لمنطق اللعب): بورتريه ⇒ الخصم أعلى-يسار · الحالة وسطاً · أنا أعلى-يمين،
  ولاندسكيب ⇒ الخصم أعلى-يسار الطاولة · أنا أسفل-يمين فوق اليد. ونفس التصحيح لبطاقتَي الطاولة.
- خفّضنا حشو المقاعد وحجم الأفاتار/الخطوط في البورتريه (كانت مضغوطة في صفٍّ واحد).
- حارس التصميم اكتسب فحوص توزيع: «الخصم يساراً وأنا يميناً» (ضومنة + طاولة)، عدم تراكب البطاقتين،
  وعدم تغطية أدوات اللعب للقطع.
- كاش: `css/21-classic.css` و`css/22-look.css` ⇒ `?v=look3`.

## v2.45.2 (2026-09-20) — تثبيت (Freeze): الشطرنج تدريب مجاني + إسقاط مسار ڤيرسيل + ميثاق عدم الانحدار

قراران ملزمان من المالك، وُثّقا ونُفّذا، مع ميثاق حرّاس يمنع الانحدار:
`docs/FIXES_FROZEN_v2451.md`.

### 1) الشطرنج = تدريب مجاني بلا رهان (قرار المالك — مرفوض إضافة شريط رهان)
- **`js/games/chess.js`**: أُزيلت الكتلة الميتة التي كانت تكتب نصّ الرهان في `#chessStake` داخل
  `chessUpdateHUD()` (العنصر غير موجود في الشاشة أصلاً)، ووُضع تعليق تثبيت صريح:
  «لا شريط رهان داخل شاشة الشطرنج — ضد الآلي/وجه لوجه تدريب مجاني، والرهان للغرف أونلاين فقط».
  لا تغيير سلوكي (العنصر لم يكن موجوداً) — إزالة سبب للالتباس فقط.
- **`tests/_chess_browser_test.js`** (كان يفشل بلا سبب حقيقي) — تصحيح التوقعات المتقادمة:
  - القطع تُرسم **SVG** (`chessPieceSVG`) لا محرف `♛` ⇒ الفحص صار وجود `<svg>` داخل الخانة.
  - `#chessStake` **لا يوجد** ⇒ الفحص صار تأكيد **غيابه** (وجه لوجه وشاشة اللعب وغرفة الرهان).
  - الأرصدة تُقاس **بالفروق (Δ)** عن رصيد الخادم المرجعي (`AUTH.user.gold`) بدل قيم مطلقة
    (‏50000/49975/50025/49950) كانت تُطمس بمزامنة الخادم وتُفشل الاختبار.
  - مُنع تزوير الرصيد محلياً في `setup()` (`gold === false`) — لا مزامنة رصيد وهمي للخادم.
  - تأكيد **عدم الخصم المزدوج** عند إعادة المباراة (كان متوقعاً خصماً ثانياً خطأً).

### 2) ڤيرسيل: مسار تجريبي فقط (قرار المالك — Cloudflare هي الأساسية)
- `Dockerfile` و`.dockerignore` **أُعيدا** (كانا محذوفين بسبب محاولة ڤيرسيل).
- `.gitignore`: `vercel.json` و`.vercelignore` أصبحا مستثنيين مع تعليق يمنع رفعهما أو حذف
  ملفات المشروع من أجلهما.

### 3) ميثاق الحرّاس
- `docs/FIXES_FROZEN_v2451.md`: جدول 17 إصلاحاً مُجمَّداً + الحارس الآلي لكل واحد + الممنوعات،
  مع مصفوفة «أجنحة بلا متصفح (خضراء هنا)» و«أجنحة تحتاج متصفحاً».

## v2.45.1 (2026-09-20) — إصلاح عيوب مراجعة v2.44→v2.45 (منعطفات واجهة + Binance Pay)

مراجعة كاملة لنطاق `4047a78…711fc10` على متصفح حقيقي وكيل اختبارات (54 جناحاً غير متصفحي + 18 متصفحياً).
النتائج والإصلاحات المطبَّقة:

### 1) v2.45-LOOK (`css/21-classic.css`) — 4 منعطفات كانت تُعطّل اللعب فعلياً
- **الطاولة (Backgammon) لا تبدأ**: قاعدتا `#bwStage #bwPlay {display:grid/flex !important}` (بورتريه/لاندسكيب)
  كانتا تُظهر شاشة اللعب **المخفية** فوق القائمة، فيصطدم كل نقرة بطبقة ميتة (نقرة حقيقية على «ابدأ» = Timeout).
  الإصلاح: تقييد القاعدتين بـ`.bw-screen-active`.
- **زر «مرّر» في الضومينو ظاهر دائماً** رغم `hidden` (لأن `display:inline-flex !important` أعلى تخصيصاً):
  الإصلاح: `#dmStage .dm-tool[hidden]{display:none!important}`.
- **الضومينو «لاعبان — جهاز واحد» لم يكن قابلاً للعب**: `#dmStage .dm-opprow{pointer-events:none}` يمنع نقر يد
  اللاعب الثاني. الإصلاح: `#dmStage .dm-opprow > *{pointer-events:auto}`.
- **اللغات اللاتينية (fr/en)**: رزمة الخصم كانت مثبّتة `left:0` فتغطي البنك (وكلتا الاتجاهين) وتفيض على اليد
  في اللاندسكيب. الإصلاح: تثبيت يميني `right:0;left:auto` + `max-height` وتقليص `.dm-back` + لاندسكيب `top:58px`.
- التحقّق: `_classic_look_test` 36/0 · `_layout_edge_test` 49/0 · مجسّات نقر حقيقية (Backgammon/Dominoes) ناجحة.

### 2) Binance Pay — عيوب كانت ستُفشل الشحن التلقائي فعلياً
- **معرّف الطلب**: كان `uid('dtsg')` يولّد `dtsg-…` بشرطة، وBinance تشترط «حروف/أرقام فقط» (400103/400201).
  الآن `binanceOrderId()` = `DTSG…` (≤32، `A–Z0–9`).
- **توقيع الإشعارات**: كان يُقارَن HMAC-SHA512 بسرّ التاجر، بينما Binance توقّع **RSA-SHA256** بشهادتها
  (تجلِب الكود الشهادات من `/binancepay/openapi/certificates` مع ذاكرة 6 ساعات، أو `BINANCE_PAY_PUBLIC_KEY`).
  لا يُشحن رصيد من الإشعار أبداً — الاستعلام الموقَّع هو الحاكم (الخطر الأمني معدوم، والتشخيص صار صادقاً: `sig=ok|mismatch|unavailable`).
- **إشعار الإغلاق**: `PAY_CLOSED`/`CANCELED`/`EXPIRED` كان يُهمَل فيبقى الطلب «معلّقاً» للأبد ⇒ صار يُغلق الطلب (`rejected`) ويُشعر الأدمن.
- **المبلغ المدفوع يُقرأ من ردّ الاستعلام فقط** (`orderAmount` ثم `totalFee`) — لا من جسم الإشعار القابل للتزوير.
- **حالة الوسيلة**: `/api/payments/methods` كانت تُعلن `binance_pay=live` بمجرّد وجود `API_KEY` بينما الإنشاء
  يشترط `SECRET_KEY` أيضاً ⇒ صار الشرط موحّداً.
- **P2P**: أُضيف `binance_pay` إلى وسائل `/api/payments/p2p` (بوت الشحن كان يحوّل «كريبتو/Binance Pay» فيُرفض `bad-input`).
- **QR محلي**: رمز الدفع ورمز `otpauth` للـ2FA كانا يُبنيان عبر `api.qrserver.com` (تسريب رابط الدفع/سرّ 2FA لطرف ثالث).
  أُضيف `js/vendor/qr-mini.js` (مولّد QR محلي: وضع البايت · L/M/Q/H · الإصدارات 1–10) واختُبر بفكّ ترميز فعلي
  (23/23 عبر OpenCV) وأُزيل المضيف من CSP. + زر «نسخ رابط الدفع».

### 3) الترحيل والحسابات
- **ترحيل `pay_transactions`**: كان يُنفَّذ مع `foreign_keys=ON` فيفشل كلياً بصمت عند وجود صفّ لمستخدم محذوف
  ⇒ يبقى CHECK القديم ويُرفض كل `binance_pay`. الآن `PRAGMA foreign_keys=OFF` حول إعادة البناء + استرجاع الحالة،
  وإحصاء الصفوف اليتيمة، ورسالة `MIGRATION FAILED` صريحة (لا «skipped»).
- **مسح الحساب من لوحة السوبر**: كان `DELETE FROM users` يفشل (قيود FK) والخطأ يُبتلَع فيُعاد `{ok:true}`
  والحساب باقٍ في القاعدة. الآن يُعاد **409** برسالة واضحة، و`force:true` يحذف سجلات المستخدم المالية معه في معاملة واحدة.
- **`buildEnv`**: تمرير `BINANCE_PAY_CURRENCY/CERT_SN/PUBLIC_KEY/API_BASE` (كانت تُقرأ في الوحدة وتُهمَل على مسار الهاتف).

### 4) الاختبارات والوثائق
- `_cf_payments_test` 80/0 (13 تأكيداً جديداً: المعرّف، حالة live، P2P، RSA ok/mismatch/unavailable، الإغلاق، المبلغ المنسوب للاستعلام).
- `_parchisi_engine_test`: تحديث ثوابت الهندسة إلى قيم v2.44 (64×27.5 · القاعدة 204 · المركز 140) وفحص تحويل حافة‑لحافة ⇒ 155/0.
- `scripts/qa-env.sh`: ضبط `TELEGRAM_ADMIN_CHAT_ID`/`SUPPORT_SUPER_TG` (كان `_pay_v243 §د` يسقط 403).
- `docs/PHONE_UPDATE_v245.md`: تصحيح فحص webhook (كان `cryptomus`) وتوثيق متغيّرات البيئة الجديدة.
- `refund-policy.html`: نصّ الإيداع/السحب يذكر «Binance Pay» بدل «كريبتو» (ar/fr/en/da).

---

## v2.45 (2026-09-19) — إزالة بوابة Cryptomus واستبدالها بـ **Binance Pay** (طلب المالك)

### ما طُلب
إزالة وسيلة الشحن التلقائي عبر **Cryptomus Gateway** وتعويضها ببوابة **Binance Pay** بمفاتيح التاجر الجديدة.

### 1) الخادم والووركر (`cf-worker/payments-core.js` + `server-payments.js`)
- **إزالة Cryptomus كلياً من المسارات**: حُذفت دوال `cryptomusSign`/`md5`/`b64`، ومسار الفاتورة
  `POST /api/payments/crypto` لم يعد ينشئ فاتورة Cryptomus، وwebhook `/api/webhooks/cryptomus`
  أُزيل من `PAY_PATHS` — لم يبقَ أي مسار يقبل أو ينشئ معاملة Cryptomus.
- **Binance Pay (Merchant API v2)** في `payments-core.js`:
  - `POST https://bpay.binanceapi.com/binancepay/openapi/v2/order` لإنشاء الطلب،
    بترويسات `BinancePay-Timestamp` · `BinancePay-Nonce` · `BinancePay-Certificate-SN` (= **API Key**)
    و`BinancePay-Signature = HMAC-SHA512(ts \n nonce \n body \n)` بست عشرية **UPPERCASE**.
  - المبلغ يُرسل بالوحدة الصحيحة (`orderAmount: 10.00` لا `1000`) مع `goods` كائناً
    (`goodsType/goodsCategory/goodsUnitAmount`) و`currency` قابلة للضبط (`BINANCE_PAY_CURRENCY`، افتراضياً USDT).
  - `POST /api/payments/crypto` يعيد الآن `prepayId · checkoutUrl · universalUrl · deeplink · qrcodeLink · qrContent`،
    وعند فشل البوابة يعيد **502** مع `detail` مختصر (رمز/رسالة Binance) ويُشعر الأدمن.
- **تحصين webhook `/api/webhooks/binance`** (لا ثقة بالجسم وحده):
  1) يقرأ `data` سواء وصل كائن JSON أو **نص JSON** (السلوك الموثّق) ليستخرج `merchantTradeNo`؛
  2) يتحقق من التوقيع `HMAC-SHA512` (تسجيل فقط — Binance توقّع إشعاراتها بمفتاحها الخاص)؛
  3) **يؤكّد الحالة من Binance نفسها** عبر استعلام موقَّع (`/binancepay/openapi/v2/order/query`) ⇒ الشحن فقط عند `PAID`؛
  4) يقارن المبلغ المدفوع بالمطلوب — **المبلغ الناقص لا يُشحن آلياً** ويُرسل تنبيهاً للأدمن؛
  5) يردّ `{"returnCode":"SUCCESS"}` كما تطلب Binance، ويعيد `FAIL` عند عدم التأكيد لتُعيد Binance الإرسال.
  - **المطالبة الذرّية** في `completeDeposit` تمنع الشحن المزدوج عند تكرار الإشعارات.
- **إصلاح `normMethod`**: كان `"Binance Pay"` يُطبَّع إلى `binance` قبل فحص `binance_pay` (ترتيب شروط معكوس) ⇒ صُحّح.
- **ترحيل قاعدة البيانات** (`server-payments.js`): شرط إعادة بناء `pay_transactions` صار على غياب
  `binance_pay` (كان `binance` فقط ⇒ القاعدة الحيّة لم تكن تقبل `binance_pay` أصلاً).
  القيمة `cryptomus` أُبقيت في `CHECK` **للصفوف التاريخية فقط** (لا مسار جديد ينشئها).
- **الأسرار من البيئة فقط** (لا تُكتب في المستودع): `BINANCE_PAY_MERCHANT_ID` · `BINANCE_PAY_API_KEY` ·
  `BINANCE_PAY_SECRET_KEY` · `BINANCE_PAY_CURRENCY` (واختياري `BINANCE_PAY_CERT_SN` · `BINANCE_PAY_API_BASE`).

### 2) الواجهة
- المحفظة: وسيلة **Binance Pay** في الإيداع، وعند الطلب يظهر **QR** (عبر `api.qrserver.com`، مسموح في CSP)
  + زر «افتح Binance Pay» + مرجع الطلب والمبلغ، مع رسالة «يُشحن تلقائياً بعد تأكيد Binance».
- مفاتيح ترجمة جديدة (ar/fr/en/darija): `wl.openBinance` · `wl.payRef` · `wl.payAuto`، وتحديث نص
  `wl.noteCrypto` إلى Binance Pay، وحذف مفتاح `wl.mCrypto` غير المستخدم.
- **إزالة آثار إثبات ملكية Cryptomus**: ميتا `index.html`، وملفا `cryptomus_*.html` (محذوفان من الجذر)،
  وسطورهما في `_headers`/`_redirects`/`scripts/deploy-pages.sh`، ونص داشبورد الأدمن في `js/main.js`.

### 3) السكربتات والبوتات
- `scripts/update-phone-server.sh`: تصدير `BINANCE_PAY_*` بدل `CRYPTOMUS_*`، وفحص وجود بوابة Binance
  في `payments-core.js` بدل فحص ملف إثبات Cryptomus (كان يفشل بصوت عالٍ بعد الحذف).
- `scripts/phone-doctor.sh`: فحص بوابة Binance Pay بدل ملف Cryptomus.
- `scripts/voucher-bot.js`: وسيلة الشحن السريع صارت **Binance Pay**.

### 4) الاختبارات
- `tests/_cf_payments_test.js`: قسم Binance Pay كامل (توقيع مطابق للمعيار · Certificate-SN = API Key ·
  المبلغ بالوحدة الصحيحة · سجل `binance_pay` معلّق · webhook موقَّع + تأكيد استعلام ⇒ شحن ·
  إعادة الإرسال لا تشحن مرتين · استعلام غير مؤكد ⇒ FAIL · مبلغ ناقص ⇒ لا شحن + تنبيه أدمن ·
  طلب مجهول ⇒ SUCCESS بلا شحن · فشل البوابة ⇒ 502 مع السبب).
- `tests/_verify_v240.js` / `_verify_live_v240.js` / `_security_static_test.js`: فحوص «إزالة Cryptomus»
  (404 للملفات + لا أثر في `index.html`) بدل فحوص ملف الملكية.

### 5) تشغيلياً (مهم)
- المفاتيح أُدخلت في متغيرات بيئة pm2 للعملية `casino-server` (`pm2 restart casino-server --update-env`).
- **شرط التفعيل الحي** (تأكد فعلياً في الاختبار الحي على الهاتف):
  مفتاح Binance Pay يجب أن يكون مفعّلاً للـ Merchant API مع **السماح بعنوان IP للهاتف**
  (أو مفتاح بلا قيود IP). قبل ذلك تردّ Binance:
  `400004 Invalid API-key, IP, or permissions for action, request ip: 41.249.159.42`.
- **إصلاح بعد الاختبار الحي (400100)**: `Nonce` يجب أن يطابق `^[a-zA-Z0-9]{32}$` —
  كان يُولَّد أقصر من 32 ⇒ صار `crypto.randomUUID()` بلا شرطات (32 حرفاً).

---

## v2.45-LOOK (2026-09-19) — إعادة تصميم الضومينو والطاولة + محفظة الوضع المشع

من المراجع الستّ للمالك (4 لتصميم اللعبتين في الوضعين + 2 للمحفظة):

### الضومينو
- ملء حافة-لحافة بلا إطار نحاسي/ذهبي سميك · حافة داخلية رفيعة أنيقة على الطاولة.
- جوخ أخضر مُشعّ (بورتريه) وخشب طبيعي (لاندسكيب) كما في المرجعين.
- المسار متمركز في وسط الطاولة · رزمة الخصم عمود رأسي على الحافة اليمنى · البنك (المخزون) صندوق مربّع على اليسرى.
- اليد على الحافة السفلية: قطع كبيرة تنكمش لتناسب العرض، وكلّها واضحة (كانت `opacity:.5` فتبدو رمادية).
- القطعة القابلة للّعب **صفراء متوهّجة** في اللوحة وفي اليد (في المرجع).
- أفاتار دائري بحلقة خضراء للاعب · أدوات دائرية أعلى اليد · رفّ خشبي للقطع في اللاندسكيب.

### الطاولة (Backgammon)
- بطاقات بإطار ذهبي: الاسم ثم **Score:** ذهبي كبير ثم **Pips:** فاتح — كلٌّ في سطر (كما في المرجع).
- نرد عاجي كبير (48–64px) بنقاط داكنة واضحة + زر رمي ذهبي بارز.
- تلميحات الحركة القانونية **خضراء** نابضة على المصادر والوجهات + تحديد القطعة المختارة.
- شريط منتصف رفيع · لاندسكيب: البطاقات يساراً واللوحة يميناً بكامل الارتفاع · بورتريه: بطاقتان جنباً لجنب ثم اللوحة.

### المحفظة في الوضع المشع (إصلاح عطل قراءة)
- **السبب:** `.wl-sheet` خلفيتها داكنة مثبّتة بينما النصوص تأخذ متغيّرات الثيم — وفي `radiant` تصير هذه داكنة ⇒ نصّ داكن على داكن.
- **الحل:** ثيم `html[data-theme="radiant"]` كامل للمحفظة (نافذة، شرائح، طرق، حقول، أزرار، حالات، QR) عاجي/كريمي + ذهبي `#b8892f`.
- تباين مقيس على الإنتاج: الشرح 4.96:1 · الحقول 17.16:1 · العنوان 15.78:1.

### التحقق
- جناح جديد `tests/_classic_look_test.js`: **36/0** · لا انحدار: layout_edge 49/0 · parchisi 7/0 · chess 15/0 · dama 10/0.
- الملفات: `css/21-classic.css` (جديد) · `css/20-wallet.css` (+قسم radiant) · `index.html` (رابط + إصدارات).

---

## v2.44-EDGE (2026-09-19) — ملء الشاشة حافة-لحافة للألعاب الثلاث (طلب المالك)

### المواصفة المنفَّذة
1. **شطرنج/ضاما**: حاوية الإعدادات وحاوية اللعب تملآن الشاشة 100%؛ لا شريط ذهبي حول الحاوية
   ولا حول اللوحة؛ البورتريه ⇒ ضلعا اللوحة يميناً ويساراً على حدّ الشاشة، واللاندسكيب ⇒ أعلى/أسفل،
   بلا حدّ ولا فراغ. اللاندسكيب: لا شريط زالق ولا سحب أفقي، واللوحة والأزرار ثابتة.
2. **بارتشي**: إزالة الفاصل الخشبي المحيط؛ بورتريه ⇒ يمين/يسار على حدّ الشاشة، لاندسكيب ⇒ أعلى/أسفل.
3. **أيقونتا الاستسلام/التعادل**: SVG مضمّن + قرص دائري 44px متناسق (كانتا بيضاويتين ومرتاكبتين مع أيقونة اللاعب).

### التنفيذ
- **`css/15-edge.css` (جديد، يُحمَّل أخيراً)**: المرحلة `#chessStage/#damaStage` = `position:absolute; inset:0`
  بلا حدّ/ظلّ/تحجيم، والحاويتان + شاشتا اللعب تملآن 100% بلا حشو، و`--dtsg-square: min(100vw,100dvh)`
  هو قياس اللوحة بالضبط. إزالة `box-shadow: 0 0 0 4px #3a2a12, 0 0 0 6px #d9b45c` عن `.ch-board`
  وحدّ `.stage` الذهبي (css/11-game-theme.css:18-26) و`overflow:auto` عن `.dama-wrap`.
- **`js/main.js`**: `fitGameStage()` يستثني `chessStage` (القياس بالـCSS حتمي — لا تحجيم transform).
- **`js/games/parchisi.js`**: `prEdgeTransform/prEdgeInverse` — تحويل مقياس/إزاحة يمحو هامش الرسم (10)
  فيلمس حدّ الخانات حدّ الكانفس، وعكسه في قراءة النقر (دقّة اللمس محفوظة 600/rect.width).
  وإصلاح تراكم التحويل بين الإطارات (`ctx.save/restore`) الذي كان يُخفي اللوحة بعد ثوانٍ.
- **`js/games/{dama,chess}.js`**: أيقونات SVG مضمّنة بدل FontAwesome + فئة `dm-resign` للتمييز.
- **`tests/_layout_edge_test.js` (جديد)**: 49 فحصاً حيّاً (بورتريه/لاندسكيب × 3 ألعاب + شاشتا الإعداد)
  يقيس الحدود والفجوات والحشو والقصّ والتراكب.

### النتيجة
- **789 فحصاً / 0 فشل** (14 ملف اختبار) · الإنتاج **dtsg.pages.dev** ببصمات ملفات مطابقة للمُختبَر.

---

## v2.44 (2026-09-19) — قانون الإلزام/النفخ في ضاما: **قانون المالك** مطبَّقاً

### 1) الوضع `ladder` صار الافتراضي (قرار المالك 2026-09-19)
نصّ القانون كما أفصح عنه المالك:
1. **الأولوية في الإلزام والنفخ للضائم** (الملك الذي وصل منطقة الخصم)؛
2. ثم الإلزام ثانياً للقطعة التي يجب أن تُتمّ سلسلتها من الأكل؛
3. وإن أُتيحت سلسلتان بنفس الطول ⇒ **الإلزام أولاً على الضائم**؛
4. وإن كانت السلسلتان **لبيدقين عاديين** ⇒ إتمام إحداهما **يُسقط الإلزام** عن الثاني.

**التنفيذ** في `js/games/dama.js`:
- اختيار المُلزَم: الأطول أولاً، وعند التساوي الضائم (كان كذلك أصلاً ✔ مطابق للبندين ١ و٣).
- النفخ عند التساوي/الأولوية: **الضائم لا يُبرّئه أداءُ غيره** (`!obligedFulfilled || turnCaptures < need`)،
  أما البيدق فيُبرّئه إتمامُ سلسلة مساوية ولو ببيدق آخر (البند ٤).
- الأوضاع الثلاثة محفوظة للرجوع بأمر واحد: `DAMA.eng.rules.obligation = 'ladder' | 'overall' | 'piece'`.

**الأثر المقيس**: كانت v2.43/v2.44 تُبرّئ اللاعب إذا أتمّ سلسلة مساوية بقطعة أخرى — ولو كان الضائم
هو المُلزَم؛ والآن **يُنفخ الضائم** في هذه الحالة (وهو نصّ القانون).
- `_dama_engine_test`: 84/0 ⟶ **89/0** (أُعيدت صياغة الحالة [2e] لتُعبّر عن القانون الجديد + فحوص
  الهبوط الأمثل/الناقص للملك الطائر).
- `_dama_obligation_mode_test`: 7/0 ⟶ **15/0** (بنود القانون الأربعة مُثبتة فرادى).
- `_dama_fuzz_test`: **459/0** في الأوضاع الثلاثة (`DAMA_OBLIGATION=overall|piece|ladder`).
- الحيّ: `_dama_v244_test` 10/0 · `_dama_browser_v244_test` 8/0.

### 2) ملاحظة نطاق (تحتاج تثبيتاً من المالك)
حالة **ضائم بسلسلة أقصر مقابل بيدق بسلسلة أطول** لم تُذكر صراحةً؛ المُنفَّذ الآن (وفق «ثم صاحب
السلسلة الأكبر») أن **الأطول يُلزم**، والضائم يُلزم عند التساوي. إن أراد المالك تقديم الضائم دائماً
فهي سطر واحد في `obligationPiece`.

---

## v2.44 (2026-09-19) — قانون النفخ في ضاما صار مبدَّلاً بإعداد واحد

### 1) `rules.obligation`: 'overall' (الافتراضي) ⟷ 'piece' (الأصرم)
- **لماذا**: بين main وخط المراجعة خلاف حقيقي غير محسوم من المالك حول قانون الإلزام/النفخ —
  main يعدّ الواجب *أطول سلسلة في الدور كله* (إتمامها بأي قطعة يُبرّئ)، وخط المراجعة يقيّده
  بالقطعة المُلزَمة نفسها. حتى لا يبقى القرار رهين تعديل محرّك حسّاس، صار الوضعان معاً في الكود.
- **الافتراضي لم يتغيّر**: `obligation: 'overall'` — والكائنات القديمة بلا المفتاح تُعامل كـoverall
  (توافق خلفي مُختبَر).
- **التبديل بسطر واحد** عند قرار المالك: `DAMA.eng.rules.obligation = 'piece'`.
- **إثبات أن المفتاح مؤثّر (لا صوري)**: اختبار جديد `tests/_dama_obligation_mode_test.js` (**7/0**)
  يبني لوحاً بقطعتين متساويتَي السلسلة ويُلزم الأولى: إتمام السلسلة بالقطعة الأخرى ⇒ overall لا ينفخ ·
  piece ينفخ المُلزَمة؛ وإتمامها بالمُلزَمة ⇒ لا نفخ في الوضعين.
- **الرشّ في الوضعين**: `DAMA_OBLIGATION=piece node tests/_dama_fuzz_test.js` ⇒ **459/0**،
  والافتراضي **459/0**، والمحرّك **84/0**، والحيّ **8/0**.
- **منشور ومتحقَّق حياً على dtsg.pages.dev** (النشر `9ae0a58d` · `dama.js?v=dtsg14`): الوضع الافتراضي
  overall · المفتاح يقلب النتيجة على نفس النقلة · صفر أخطاء صفحة.

---

## v2.44 (2026-09-19) — تحصين مسارات المال ضد التنفيذ المزدوج (Idempotency)

### 1) `claimTx`: مطالبة ذرّية بالمعاملة قبل أي أثر جانبي
- **العلة الكامنة**: `adminActOnTransaction` كان يقرأ حالة المعاملة ثم يُحدّثها بلا شرط ذرّي،
  وكذلك `completeDeposit`. على قاعدة متزامنة (خادم الهاتف/SQLite) لا تظهر المشكلة عملياً —
  **قِسناها: 5 طلبات موافقة متزامنة ⇒ موافقة واحدة وكود واحد** — لكن على قاعدة حقيقية متزامنة
  (D1/عدّة عمّال) كان يمكن أن ينتج **كودا تعبئة لطلب واحد** أو **شحناً/استرداداً مزدوجاً**.
- **الإصلاح**: `UPDATE transactions SET status=?2 WHERE id=?1 AND status='pending'` كعملية
  ذرّية واحدة (نفس نمط `is_used=0` في تفعيل الفوتشير)؛ الفائز الأول فقط يُنفّذ الآثار الجانبية
  (إنشاء الكود · الشحن · الاسترداد · الإشعار)، والباقي يردّ رفضاً نظيفاً `already-handled`.
  و`completeDeposit` صار يطالب بنفسه ⇒ الحماية تشمل **كل** المسارات (داشبورد/بوت/ويبهوك).
- **تعويض**: إن فشل إنشاء الكود بعد المطالبة تُعاد المعاملة إلى `pending` (لا طلب مُتلف).
- **اختبار انحدار جديد**: `tests/_money_idempotency_test.js` (**16/0**) — 5 موافقات متزامنة ⇒
  موافقة واحدة وكود واحد · موافقة متأخرة تُرفض · تفعيل الكود 5 مرات ⇒ +12,500 كوين مرة واحدة ·
  رفض سحب 5 مرات ⇒ استرداد واحد (+900) · إيداع حقيقي 5 مرات ⇒ +1000 مرة واحدة وحالة واحدة.
- المسارات الأخرى لم تتأثر: `_cf_payments_test` 60/0 · `_money_v244_test` 20/0 ·
  `_bot_v244_test` 16/0 · `_admin_payments_ui_test` 19/0 · `verify-phone-v244.sh` 10/10.

### 2) ثبات الرصيد واكتمال سجل المال — اختبار خصائص + إصلاح سطر ناقص
- **دورة السحب صحيحة ومُوثَّقة الآن بالقياس**: حجز ذرّي عند الطلب (`-700` لسحب 7$)،
  واسترداد كامل عند الرفض (`+700` ⇒ الرصيد يعود بالضبط)، ولا استرداد عند القبول (خرج المال فعلاً).
- **النقص المُصلَح**: السحب **المقبول** لم يكن يُكتب له سطر في `money_log` ⇒ يبقى في سجل
  داشبورد السوبر أدمن **«⏳ قيد المراجعة» للأبد** بعد تنفيذه. صار يُسجَّل `withdrawal/approved`.
  (الواجهة كانت تدعم الحالة `approved` أصلاً — النقص كان في الخادم وحده، فلا حاجة لنشر واجهة.)
- **اختبار خصائص جديد**: `tests/_money_invariants_test.js` (**22/0**) — يمرّ على إيداع/سحب
  (رفض وقبول)/كود تعبئة//api/sync بمرجع قديم، ويتحقّق بعد كل خطوة من: صحّة الكوينز (عدد صحيح
  غير سالب) · مقدار التغيّر بالضبط · وجود سطر سجل مطابق (kind/status/coins) · وأن المزامنة
  القديمة لا تطمس شحناً خادمياً.

---

## v2.43.1 (2026-09-18) — حارس عنوان الـAPI في القناة الحيّة (إصلاح حي على dtsg.pages.dev)

### 9) خطأ كونسول على الموقع الحيّ: قناة البث الحيّة كانت معطّلة فعلاً (v2.43.1)
- **العلة**: جسر `live-ws-bridge` كان يقرأ العنوان **المخزّن** ليقرّر «وضع SSE» بينما المتغيّر
  `API_BASE` ما زال `null` (الوعد `api-url2.json` لم يُحسم) ⇒ يُبنى `null/api/live` نسبةً لمضيف
  الصفحات، فيخدمه Pages كـ HTML: «MIME type text/html is not text/event-stream» ويتكرر بلا نهاية
  (وحلقة إعادة اتصال) ⇒ **القناة الحيّة لا تعمل أصلاً على الإنتاج** ودلالة الرصيد اللحظي لا تصل.
- **الإصلاح**: دالة `curBase()` (المحلول أو المخزّن)، وواجهة `deferredLive()` تربط نفسها عند حسم
  العنوان (`withCredentials: true`) وتُسكِت نفسها إن لم يُحسم (مضيف ثابت بلا API) ⇒ صفر أخطاء كونسول
  وصفر محاولات بعنوان مجهول، وتراجُع SSE داخل القناة لم يعد يبني رابطاً بلا عنوان.
- اختبار انحدار جديد: `tests/_live_base_guard_test.js` (**17/17**) — زائر جديد/عنوان مخزّن/فشل الجلب/محلي/روابط أخرى/إغلاق قبل الحسم.

---

### 10) الووركر السحابي الاحتياطي casino-api — CORS الإنتاج + حارس غياب قاعدة D1
- **العلة المُثبتة**: قائمة الأصول المسموحة في الووركر المنشور لم تكن تشمل `dtsg.pages.dev`
  (كانت `dmgames.pages.dev` فقط)، فحين يسقط خادم المنصة ويتحوّل العميل للاحتياط السحابي
  يحجب المتصفح الطلب: `Access to fetch … blocked by CORS policy` في كونسول الإنتاج.
- **العلة الثانية**: لا توجد **أي قاعدة D1** في حساب Cloudflare (0 قواعد) وربط الووركر كان
  يشير إلى قاعدة محذوفة ⇒ مسارات القاعدة ترمي `Error 1101` (HTML بلا JSON).
- **الإصلاح (منشور فعلاً · Version 2e106814)**: إضافة `dtsg.pages.dev` (+النطاقات الفرعية)
  للأصول المسموحة · توحيد CORS على **كل** ردود JSON (بعض المسارات كانت تُنادي `json()` بلا رؤوس) ·
  حراس `dbReady()` في `dbOne/dbRun/dbAll` · ردّ JSON واضح `{ok:false,error:"no-db"}` مع CORS
  لمسارات القاعدة بدل 1101.
- أدوات: `cf-worker/wrangler.toml` (ربط Durable Object فقط) · `scripts/deploy-casino-api.sh`
  (نشر + تحقق CORS آلي) · `cf-worker/worker.js` صار مطابقاً للمنشور فعلاً.

---


## v2.43.0 (2026-09-18) — جولة إصلاح الأعطال السبعة (بتدقيق متصفح حقيقي)

### 1) بوت الشحن/الفوچر لم يكن يقبل شيئاً («❌ bad-input» دائماً)
- `cf-worker/payments-core.js`: طبقة توافق كاملة أُضيفت لكل المسارات المالية:
  - **أسماء حقول بديلة**: `amount|amount_usd|sum|total|price|mad_amount` · `method|pay_method|payment_method|gateway`
    مع **تطبيع الوسائل** («Binance (TRC20)» و«Cash Plus» و«CIH» و«كوبون»…).
  - **حلّ هوية المستخدم**: `user_id | username | tg_id/telegram_id/chat_id` عبر خطاف
    `__resolveUid` الجديد على المنصة (يقرأ الذاكرة والجدول).
  - **الوصل/التفاصيل**: `details|proof_details|reference|ref|txid|hash|phone|account|iban|rib|…`.
  - **إنشاء الأكواد**: السرّ يُقبل من ترويسة `x-admin-secret` **أو** `admin_secret` في الجسم/الاستعلام،
    وكذلك `tg_id` لسوبر أدمن أو دور super من الجلسة (`voucherActorOk`).
  - كل خطأ صار يرجع `missing[]` + `hint` عربي واضح بدل «bad-input» صامت.
- **حماية الرصيد**: أي هوية غير مطابقة لحساب منصة ⇒ `user-not-found` (بدل تسجيل معاملة يتيمة).
- **إصلاح شحن مزدوج**: `platformCredit` كان يُضاف فوق `__creditUsd` على المنصة
  (كوبون 10$ = 2000 كوين بدل 1000) — الآن مسار واحد فقط.

### 2) الشحن يُسجَّل ولا يغيّر الرصيد المعروض
- `server.js`: **دفع رصيد لحظي** عبر SSE (`pushWallet` + حدث `wallet`) بعد كل تغيير مالي
  (اعتماد إيداع/كوبون/طلب سحب/إعادة رصيد) — لصاحب الحساب فقط.
- `js/core/live.js` + `js/main.js`: مستمع `RC_wallet` يحدّث `ST.gold` والهيدر وسجل الحساب
  وأي محفظة مفتوحة **بلا إعادة تحميل**، مع رسالة «تم شحن رصيدك».
- `js/wallet.js`: مرآة `window.Wallet.refresh()` + حماية من غياب الحاوية (لا استثناء 0).

### 3) سجل الحساب: تاريخ الانضمام وآخر نشاط «غير متوفر»
- `server.js`: `publicUser` يرسل `created_at` و`last_seen` (بالثواني)، و`loadUsersFromDB`
  يحمّلهما أصلاً، و`last_seen` يُحدَّث في الذاكرة عند الدخول — انتهى «غير متوفر» نهائياً.

### 4) فلات دوغ — نافذة «فزت!» تعرض +30/−10 في التدريب المجاني
- `js/games/ronda.js`: `_isFreeTraining()` وتطبيقها في بانر الفوز/الخسارة، وسجل الجولة،
  وشريط الرهان (صار «تدريب» معطّلاً بدل رقم)، وسطر «رهان الجولة: −0» أُزيل.
- مفاتيح ترجمة جديدة: `ui.training` و`ui.trainingFreeShort` (4 لغات).

### 5) رامي — تجمّد المؤقّت في دور اللاعب الآلي
- `js/games/rami.js`: كل tick كان يُلغي `this.timerId` **الحالي** إن لم تكن المرحلة `PLAYING`،
  فإن جاء tick متأخر أثناء `LOBBY`/`ROUND_END` أتلف **المؤقّت الجديد** ⇒ تجمّد دائم في دور البوت.
  الحل: **مؤقّتات بجيل** (`_timerGen`) لا تلمس إلا نفسها + إيقاف مؤقت بدل الإتلاف
  + **شفاء ذاتي** في الحارس يعيد تشغيل المؤقّت.
- `_deferBotStep`: خطوة البوت المؤجّلة بمرجع واحد (لا تنفيذ مزدوج) ويستدركها الحارس إن تأخّرت > 3 ثوانٍ.

### 6) ضاما — خلل تطبيق القواعد (إصلاح جذري مُثبت بالاختبار)
- **الأولوية كانت للملك على الأطول** رغم أن القاعدة «الأكل الأكبر أولاً»: لاعب يُتمّ أطول
  سلسلة أكل صحيحة بقطعة أخرى كان **تُنفخ قطعة أخرى له** ظلماً.
- الآن: المرجع = `maxChainOverall` (أطول سلسلة متاحة في الدور)، والنفخ يقع **فقط عند تقصير الأكل**
  (`turnCaptures < أطول سلسلة`)، وعند التعادل يتقدّم الملك. التلميح التلقائي يُبرز القطعة القادرة على الأطول.
- اختبار انحدار جديد + تحديث اختبار البِدائية (fuzz) للقاعدة المصحّحة (459 حالة).
- **إكمال**: شريط النتيجة ولوحة نهاية المباراة في **التدريب المجاني** كانا يعرضان ربحاً وهمياً
  (`فوز ×3.0 +30 🪙`) بلا أي إضافة فعلية للرصيد ⇒ الآن يُعرض «فوز — 🎓 تدريب مجاني» و«🎓 تدريب مجاني — بلا رهان»
  بلا مبالغ ولا مضاعفات ولا استرداد وهمي عند التعادل، والرصيد يبقى ثابتاً تماماً.
  - `js/games/engines.js`: `gres(m, w, noRec, forceWin)` — معامل عرض اختياري ليبقى شريط الفوز أخضر بشفافية
    عند `w = 0` (بلا ربح) **دون** تغيير سلوك أي نداء قائم.
  - انحدار مضاد في نفس الاختبار: خارج التدريب ما زال المضاعف والمبلغ يظهران والرصيد يزيد فعلاً.

### 7) بارتشي — إعادة تخطيط كاملة حسب المواصفات الملزمة
- **بلا هامش ذهبي**: حدود حاوية اللعبة/منطقة اللوحة/الكانفاس = 0 وظل اللوحة أُزيل.
- **اللوحة تملأ الشاشة**: landscape ⇒ الضلعان الأعلى/الأسفل = حدود الشاشة · portrait ⇒ الأيمن/الأيسر = حدودها.
- **هندسة مكبَّرة**: القاعدة الملوّنة 194→**160** والخانة المستطيلة 64×27.5→**93.33×30**
  (+46% عرضاً) والذراع 280×240 والمركز 140→120 — الشكل والعدد (68 خانة) كما هما.
- **البيادق +15%**: 10.5→12.1 (الممر 11.5 والعش 13.8) مع ضبط التباعد داخل الخانة (25.2 > القطر 24.2).
- **الأيقونات والنرد OUTSIDE اللوحة**: landscape ⇒ أشرطة يمين/يسار الشاشة · portrait ⇒ أعلى/أسفل،
  والأيقونة فوق نرد صاحبها (`_prPlaceAll`) بحجم متجاوب مع اللوحة، بلا تداخل مع اللوحة أو النرد أو بعضها.
- **اللوحة كانت تصغر في متصفح الهاتف**: `setupBoardFit` صار يأخذ أصغر بُعد بلا حسم هامش/حشو وبلا سقف 640.

### 8) نظافة بيئة الاختبار
- `tests/_admin_payments_ui_test.js`: صار **ذاتي التنظيف** — يرفض بقايا الطلبات المعلّقة من تشغيلاته
  السابقة قبل القياس، ويستخدم مراجع فريدة لكل تشغيل، ويقرأ الجدول كاملاً بدل معاينة 400 حرف
  (كانت تفشل كذباً عند تراكم صفوف قديمة). + `reviewed_by='qa-cleanup'` لتفريغ طابور المعلّق في قاعدة الاختبار.

### التحقق (كله أخضر على خادم اختبار حقيقي بمتصفح Chromium)
- اختبارات جديدة: `_pay_v243_test` (**26/26**) · `_rami_freeze_test` (**13/13**) ·
  `_ronda_training_result_test` (**11/11**) · `_parchisi_layout_v243_test` (**26/26**).
- محدَّثة: `_dama_engine_test` (**84/0**) · `_dama_fuzz_test` (**459/0**) · `_parchisi_engine_test` (**153/0**) ·
  `_parchisi_ui_test` (**32/32**) · `_parchisi_browser_test` (**59/59**) ·
  `_dama_browser_test` (**66/66** — +14 فحصاً للتدريب/الرهان الحقيقي) · `_cf_payments_test` (**60/60**) ·
  `_admin_payments_ui_test` (**19/0**) · `_botchat_ui_test` (34/34) · `tests_browser_audit` (34/34) ·
  `_live_base_guard_test` (**17/17**).

---

## v2.42.1 (2026-09-18) — إصلاحات من التدقيق الحيّ على dtsg.pages.dev

- **CSP**: إضافة `wss://casino-phone.dmgames-api.workers.dev` إلى `connect-src` في `_headers`
  (كان الاتصال بالغرف عبر WebSocket محجوباً تماماً بالسياسة).
- **جسر WS** (`js/core/live-ws-bridge.js`): قرار SSE صار **متزامناً** عبر `localStorage.rc_api_base`
  (يضعه `api.js`)، مع **تراجع تلقائي WS→SSE** بعد أول فشل ⇒ لا حلقة «WebSocket فاشل» كل 3 ثوانٍ
  على النفق الذي لا يدعم WS، وأحداث الغرف تصل عبر `/api/live`.
- **لا طلبات بلا جلسة**: ودجت المساعدة و`support.html` لا يستدعيان `/api/support/status` قبل
  معرفة الجلسة (`AUTH.user` بعد `authRestore`) ⇒ **صفر أخطاء كونسول** لكل زائر غير مسجّل.
- تحقق حيّ: 8 صفحات على `dtsg.pages.dev` بلا أي خطأ كونسول (عدا تقطّع عابر لنفق SSE يُصلح نفسه).

---

## v2.42.0 (2026-09-18) — مركز المساعدة العائم + إشعارات المدفوعات المضمونة + إصلاحات من تدقيق بمتصفح حقيقي

**المطلوب من المالك:** (1) اختبار الموقع بمتصفح حقيقي لاكتشاف الأخطاء الخفية؛ (2) إزالة أيقونة واتساب العائمة
واستبدالها بأيقونة شات تربط بوت الشحن وبوت الدعم بالمستخدم؛ (3) إصلاح صفحة الدعم (ترجمة + أيقونات + فوتر)؛
(4) حل مشكلة إيداعين عالقين «قيد المعالجة» بلا أي إشعار للأدمن/السوبر أدمن.

### 1) إشعارات المدفوعات — كانت صامتة فعلاً (السبب الجذري)
- `cf-worker/payments-core.js`: `tgNotifyAdmin` أصبح **ثنائي القناة**: بوت المنصة (إن وُجد توكنه) **+ بوت الدعم**
  عبر الخطاف `__notifyAdminsPayment` ⇒ إشعار لكل السوبر/الأدمنز النشطين + شات الإدارة، حتى لو كان توكن بوت المنصة غائباً.
- نواة موحّدة **`adminActOnTransaction(env, db, txId, act)`** (مصدَّرة) تُستدعى من أزرار تيليغرام (`dapp_/drej_/wapp_/wrej_`)
  ومن لوحة المنصة ⇒ مصدر حقيقة واحد للموافقة/الرفض + إعادة الرصيد + التسجيل.
- `/api/payments/p2p` يقبل أسماء الحقول البديلة (`details|reference|ref|proof|receipt|code`) ويعيد `missing[]` + `hint`
  عند 400 ⇒ ينتهي خطأ **bad-input** الذي كان يظهر في بوت الشحن.
- أزرار الموافقة تعمل الآن لأي أدمن دعم مسجّل (`__adminCanAct`) لا شات الإدارة فقط.

### 2) لوحة المنصة — طلبات الدفع المعلّقة (كانت إحصاءات فقط)
- `GET /api/admin/payments/pending` + `POST /api/admin/payments/act {tx_id, action:approve|reject}` (تتطلب صلاحية أدمن).
- تبويب «المالية» صار يعرض قائمة الطلبات المعلّقة (إيداع/سحب، المبلغ، الوسيلة، المرجع) مع زرّي **تأكيد/رفض** مباشرة.
- إصلاح خلل: رفض السحب كان يفشل (`db` error) لأن أعمدة `reviewed_by/reviewed_at` غير موجودة ⇒ أُضيف ترحيل تلقائي،
  ورفض السحب يعيد الرصيد كاملاً ويُسجّل من نفّذ المراجعة.

### 3) مركز المساعدة العائم (بديل أيقونة واتساب)
- حُذفت `#waFab` نهائياً (index.html · css/09-chrome.css · js/ui/legal-ui.js).
- الجديد `js/ui/bot-chat.js`: زر عائم يفتح نافذة بتبويبين — **محادثة الدعم** (تذاكر حقيقية) و**الشحن والمدفوعات**
  (روابط بوت الدعم/المحفظة + تتبّع آخر 3 معاملات + كود ربط تيليغرام)، + اختصارات سريعة، + نقطة إشعار للردود الجديدة.
- مسار جديد في الخادم: `POST /api/support/message` ⇒ رسالة المنصة تُسجَّل في **نفس التذكرة** التي يستعملها البوت
  (يرى الأدمن «[من المنصة]» في تيليغرام ويرد بـ /r، فيظهر الرد في النافذة وفي صفحة الدعم) — ربط منضم وبلا هويات مكشوفة.

### 4) صفحة الدعم support.html — بنفس هوية الصفحات القانونية
- أُعيد بناؤها على نفس الهيكل: css 01/02/03/07/09 + Font Awesome + auto-lang + `injectLegalUI('support')`
  ⇒ هيدر بأيقوناته، سايدبار، شريط سفلي، فوتر قانوني منسجم + رابط «الدعم» الجديد في فوتر كل الصفحات.
- **ترجمة كاملة ar/da/fr/en** لقوالب الصفحة (كانت عربية فقط)، وتتبدّل مع مبدّل اللغة في نفس الجلسة.
- صندوق «اكتب رسالتك» يرسل عبر `/api/support/message` مباشرة من الصفحة.

### 5) أخطاء خفية اكتُشفت بالتدقيق الحقيقي (متصفح Chromium)
- `js/core/api.js` كان يضع **Promise** في `window.API_BASE_URL`، و`js/core/auth.js` يبني منه رابطاً
  `"[object Promise]/api/sync"` ⇒ **404 صامت عند كل إغلاق صفحة**. صار `API_BASE_URL` نصاً فقط (والوعد في `__API_BASE_PROMISE`).
- ودجت الشات كان يستدعي `/api/wallet/balance` بلا `user_id` ⇒ 400 دائماً ⇒ يُصلح: يمرّر معرّف المستخدم (AUTH أو /api/me).

### الاختبارات المنفّذة (كلها ✓ على خادم اختبار كامل + قاعدة sqlite حقيقية)
- `tests/_support_bot_test.js` 72/72 · `_cf_payments_test.js` 60/60 · `_security_static_test.js` 37/37
- `_pay_methods_v240_test.js` 11/11 · `_vouchers_ui_test.js` 17/17 · `_v219_check.js` 25/25
- **جديد:** `tests/_botchat_ui_test.js` 34/34 (متصفح: رئيسية/دعم/10 صفحات قانونية/لعبة + تغيير اللغة)
- **جديد:** `tests/_admin_payments_ui_test.js` 18/18 (موافقة إيداع من الواجهة، رفض سحب وإعادة الرصيد، اختصارات الودجت، صفر 4xx)
- تحقق حيّ لتيليغرام: إيداع p2p ⇒ `sendMessage` إلى السوبر أدمن مع أزرار تأكيد/رفض (اعتراض fetch) ✓

---

## v2.41.0 (2026-09-18) — بوت خدمة العملاء @dtsgsupports_bot + ربطه بالمنصة وبوت المدفوعات

**المطلوب:** بوت دعم يصل المستخدم بالأدمنز، وحده السوبر أدمن يتحكم فيه، وربطه ببوت المنصة وبالمنصة.

### المحرّك الجديد `server-support.js` (~700 سطر، بلا تبعيات)
- **تذاكر:** فتح تلقائي من أي رسالة، ترقيم، تصنيف تلقائي (مدفوعات/سحب/أكواد/حساب/لعب)، حالات open/claimed/closed،
  إعادة فتح، إغلاق من الطرفين، طمأنة «وصلت رسالتك» عند الانتظار.
- **أزرار فورية:** «🎯 استلام» و«🚫 إغلاق» على كل إشعار — تعمل من بوت الدعم ومن شات إدارة بوت المنصة.
- **الخصوصية بالتصميم:** لا تُمرَّر هوية أي طرف للآخر · تذاكر الزوّار للسوبر أدمن فقط · تذكرة الأدمن لا يراها غيره ·
  الملاحظات الداخلية لا تُرسل للمستخدم · الحظر يوقف حتى إشعارات المدفوعات.
- **صلاحيات:** أدمن دعم (يُضيفه السوبر أدمن بـ `/addadmin` ويُزيله بـ `/deladmin`) وسوبر أدمن فقط يملك:
  البثّ، بيانات الحسابات، السياق المالي، الإحصاءات، الإعدادات، الحظر، وسجل التدقيق `/audit`.
- **حدود:** 20 رسالة/دقيقة · تذكرتان زوّار/يوم · مفتاح إيقاف مؤقت لا يعطّل الأدمنز (كي يمكن إعادة التشغيل من البوت).

### الربط (بوت الدعم ⟷ بوت المنصة ⟷ dtsg.pages.dev)
- **هوية واحدة:** الربط يُكتب في `users.telegram_id` ⇒ ربط واحد يعمل في البوتين.
- **شات إدارة مشترك:** التذاكر تصل أيضاً لشات `TELEGRAM_ADMIN_CHAT_ID` مع نفس الأزرار.
- **إشعارات المدفوعات عبر بوت الدعم:** خطاف `env.__notifyUser` في `payments-core` (تأكيد/رفض إيداع وسحب).
- **`/finance <تذكرة>`:** السياق المالي لصاحب التذكرة داخل المحادثة (سوبر أدمن).
- **صفحة `support.html`:** إنشاء كود الربط (30 دقيقة) + متابعة التذاكر + **لوحة أدمنز** (طابور/رد/استلام/إغلاق)،
  ورابط دعم في أيقونات الصفحة الرئيسية وداخل المحفظة (كاش `pay5`).

### الأمان والاختبارات
- ويب هوك بترويسة سرّية `x-telegram-bot-api-secret-token` (403 لكل سرّ خاطئ).
- `tests/_support_bot_test.js`: **72/72 ✓** (خادم تيليغرام وهمي + خادم HTTP حقيقي للويب هوك).
- أدوات: `scripts/setup-telegram-bots.sh` (setWebhook + الأوامر + الوصف + فحص الوصول) وتحديث
  `scripts/update-phone-server.sh` (متغيرات البوت + 4 فحوص دعم محلية + فحص خارجي).

---

## v2.40.5 (2026-09-17) — إصلاح «تعذّر إنشاء كود تعبئة» + نموذج الاتصال + صفحة الرفع

**السبب الجذري (مُثبَت حيّاً):** زر «إنشاء الكود» في لوحة السوبر أدمن كان يستدعي
‏`fetch` بمسار **نسبي** إلى مسار إنشاء الأكواد ⇒ يُرسل إلى `dtsg.pages.dev` (واجهة Pages)
لا إلى الباكأند، فيردّ **Cloudflare Pages 405** (مُختبر: `POST /api/vouchers/create` على
dtsg.pages.dev = 405) ⇒ `r.json()` يفشل و«لا شيء يحدث» عند كل محاولة.

### الواجهة
- `js/main.js`: `adminMakeCode()` صار يستعمل `API.post('/api/vouchers/create', …)` (يحلّ عنوان
  الباكأند من `/api-url2.json` ويرسل كوكي الجلسة `sid`) + رسائل أخطاء واضحة
  (403 ⇒ «للسوبر أدمن فقط» · bad-tier ⇒ عرض الشرائح المتاحة).
- `contact.html`: نفس الداء في نموذج الاتصال (كان 405 كذلك) ⇒ `API.post('/api/contact', …)`.
- `deploy.html`: كان يقرأ مَنيِفست النشر بمسار نسبي ⇒ صار يحلّ العنوان من `/api-url2.json`.
- `index.html`: كاش-بست `js/main.js?v=dtsg7`.

### الخادم (server.js)
- **جديد `POST /api/contact`**: تحقّق من المدخلات · تخزين في `contact_messages` · حدّ إغراق
  (5 رسائل/ساعة لكل IP) · إشعار تيليغرام إن توفّر التوكن.
- **جديد `GET /api/admin/contact-messages`**: استعراض الرسائل (أدمن فقط).

### الاختبارات
- **جديد `tests/_vouchers_ui_test.js` (17/17 ✓)**: يثبت أن الواجهة تستعمل عميل API الصحيح،
  ولا يبقى أي نداء API نسبي في الواجهة كاملة، وأن الخادم ينشئ الكود فعلاً (سوبر ⇒ كود محفوظ
  في `pay_vouchers` · لاعب/بلا جلسة ⇒ 403 · شريحة خاطئة ⇒ 400 · الاسترجاع يشحن الرصيد ولا يُكرَّر).
- `tests/_security_static_test.js`: 37/37 ✓ (أُضيفت فحوص `/api/contact`).

---

## v2.40.4 (2026-09-17) — أمن الملفات + بصمة إصدار + تشخيص خادم الهاتف

**السبب:** فحص حيّ لخادم الباكأند بعد «تحديث وإعادة تشغيل» أظهر أن الشجرة المشغَّلة قديمة
(`package.json` = 2.5.1): كل مسارات المدفوعات 404، وقاعدة البيانات `data/royalcoin.db`
(1.7 م.ب) و `server.js` و `package.json` كانت **متاحة للتنزيل من الإنترنت**.

### الأمن (server.js)
- `isDeniedStatic()`: حجب `data/**` · `server*.js` · `package*.json` · `cf-worker/**` · `scripts/**`
  · `tests/**` · `.env` · `.git/**` · `tunnel-live.json` وكل `*.db*`/`*.sqlite*` — تردّ 404 كأنها غير موجودة.
- حماية تجاوز المسار: `..` · `%2e%2e` · `DATA/` · `Server.js` · `/data/../server.js`.
- `/api/health` صار يعيد `build` (إصدار package.json) + `payments: true` للتحقق عن بُعد.

### أدوات التشخيص والتحديث
- **جديد `scripts/phone-doctor.sh`**: تشخيص كامل بلا تعديل (pm2 + مجلده، من يخدم المنفذ، مطابقة
  ما يخدمه الخادم مع شجرة القرص، مسارات المدفوعات، الفحص الأمني، حُكم نهائي بالخطوة التالية).
- **`scripts/update-phone-server.sh`**: يكتشف مجلد pm2 الحقيقي (`pm_cwd`) ويحدّثه، نسخة احتياطية
  للشجرة + للقاعدة قبل أي تعديل، تحقق من ملفات المدفوعات بعد الجلب (exports سليمة + JSON صالح)،
  فحص بعد إعادة التشغيل (build محلي + خارجي + قائمة الوسائل + إغلاق التسريب) وفشل بصوت عالٍ.

### الاختبارات
- **جديد `tests/_security_static_test.js`**: 34 فحصاً (حجب 14 ملفاً حساساً · 8 ملفات مشروعة سليمة ·
  5 محاولات تجاوز مسار · المدفوعات + build). النتيجة: **34/34 ✓**.
- بلا تغيير في الواجهة، لذلك لا حاجة لنشر Pages جديد: هذا الإصدار يخص خادم الهاتف (يُسحَب من GitHub).

---

## v2.27 – v2.31 (حزمة الألعاب والمدفوعات الأولى)

### Dame (الضامة)
- **قاعدة Souffler رسمية ومقصودة** بقرار المالك: المحرك يطبقها صحيحة، والاختبارات التي كانت تفشل بسببها صُحِّحت هي لا المحرك.
- محركات متعددة: `tests/_dama_engine_test.js` (79)، فزّ `459` حالة، تسويات 12/12، غرف متعددة اللاعبين 24/24.

### تجديد الألعاب
- بطاقات/ثيمات جديدة للعبة الدومينو والطاولة (backgammon) مع غرف أونلاين (`backgammon-game/js/ui/bg-room.js`، `dominoes-game/js/ui/domino-room.js`).
- اختبارات ملاءمة الشاشات لكل الألعاب (`gamesfit_*`) على مقاسات هاتف وسطح مكتب.
- **الألعاب المعطَّلة من الأدمن تُخفى** من القائمة والغرف تماماً.

### بوت التدريب والمساعد
- بوت تدريب مجاني بدون رهان وبدون تسجيل دخول ولا يدخل الغرف الحقيقية.
- خبير AI للضامة والشطرنج (`ai_expert` 26/26، `chess_bot` 9/9، `chess_engine` 52/52).

### المدفوعات — قاعدة SQLite محلية (بقرار المالك: لا D1)
- `server-payments.js` + `cf-worker/payments-core.js`: منطق واحد يعمل محلياً (Node + SQLite) وفي الووركر.
- **Cryptomus مؤتمت**: إنشاء فاتورة → Webhook موقَّع (sign-then-credit: التحقق من التوقيع ثم الشحن) → شحن تلقائي.
- **Cash Plus مباشر**: طلب إيداع → رفع صورة الوصل من التطبيق → مراجعة الأدمن عبر تيليغرام (أزرار قبول/رفض callback) → شحن أو رفض مع إشعار.
- كوبونات `DTSG-XXXX-XXXX` إنشاءً واستبدالاً.
- سحب صريح داخل التطبيق مع حالة وإشعارات.
- صفحة `refund-policy.html`: سياسة الاسترداد وطرق الدفع بأربع لغات.

---

## v2.32 – v2.33 (أدوات النشر)

- `deploy.html`: رافع ملفات داخل المعاينة يستعمل Git Data API بتوكن المالك (لا يغادر متصفحه).
- `/api/deploy/manifest` (يُفعَّل بـ `DEPLOY_MANIFEST=1`) لقائمة ملفات المستودع كاملة.
- Webhooks: cryptomus/cashplus تُوقَّع وتُشحن بالترتيب الصحيح مع سجل معاملات كامل.
- أمر البوت `/payinfo` لعرض حسابات الشحن (سوبر أدمن فقط).

## v2.34 (ناقل مستقل)

- `deploy-server.js`: خادم مستقل على **:8080** يجعل `deploy.html` صفحته الجذرية (معاينة خاصة به مثل «ناقل ملفات» سابق)، لا مضمَّناً داخل معاينة المنصة.

## v2.35 (أشجار GitHub التدريجية)

- علاج خطأ GitHub 422 «input too large»: بناء الأشجار تدريجياً (شجرة فرعية لكل مجلد، قطع ≤100 مدخل متسلسلة بـ base_tree، والجذر بدون base_tree = استبدال كامل بلا تعارضات).

## v2.36 (الترجمات + أكواد الشرائح + البوت)

### إصلاح المفاتيح الخام
- **السبب الجذري**: `translateStatic()` في `js/core/utils.js` كانت تستبدل كل `data-i18n/data-k` بقيمة `T()` التي تُرجع المفتاح نفسه عند غيابه → نصوص مثل `wl.title` و`refund.*` تظهر خام.
- الحل: تجاوز المفاتيح الغائبة + إضافة كل المفاتيح الناقصة (ar/fr/en/da): `wl.title`، `ui.refund`، `legal.refund.link`، وكل `refund.*`.

### أكواد الشحن بالشرائح (سوبر أدمن فقط)
- مخطط: أعمدة `kind (std|admin|direct)`، `coins`، `bonus_pct` في جدول الكوبونات.
- **أدمنز (بونص)**: 100→+25% | 1000→+30% | 10000→+35% | 100000→+40% ($ أو درهم).
- **مباشر**: 10$/100 د.م = 1000 كوين | 100→+5% | 1000→+10% | 10000→+15%.
- السعر: 100 كوين/$ و10 كوين/درهم. الاستبدال يشحن الكوينز فوراً مع سجل معاملة.
- أوامر البوت: `/codes` (قائمة الشرائح) و`/code admin|direct <الشريحة> [usd|mad]` — مرفوضة لغير الموثَّق.
- شات `TELEGRAM_ADMIN_CHAT_ID` موثَّق دائماً؛ غيره عبر `/auth <PIN>`.
- واجهة لوحة السوبر: تبويب «🎟️ أكواد الشحن» (نوع/عملة/شريحة/إنشاء/نسخ).
- صلاحية الإنشاء: سر الأدمن **أو** جلسة سوبر (`__authRole`).
- **CIH Bank / CIH Express أصبحت live** بمعلومات الحساب الكاملة (الاسم/الرقم/RIB/IBAN/SWIFT).

## v2.37 (جسر المخططات + الهاتف + التوثيق)

### جسر مخطط المستخدمين (Schema-bridge)
- **علة جذرية اكتُشفت**: نواة المدفوعات كانت تفترض أعمدة `email/balance_usd/telegram_id` في جدول `users` بينما جدول المنصة الحقيقي (`gold/role/…`) لا يملكها → `/start plt_<id>` والرصيد يفشلان على الخادم الحقيقي رغم نجاح الاختبارات (كانت تبني مخططها الخاص).
- الحل: خطافات بيئة (`__setTelegram/__getTelegram/__findByTelegram/__balance/__creditUsd/__debitUsd`) تمر عبر مخطط المنصة، مع بقاء المسار المستقل للاختبارات/الووركر. كلا المسارين مُختبر (60/60).
- عمود `telegram_id` يُضاف تلقائياً لقاعدة المنصة.

### ربط تيليغرام
- `/start plt_<المعرف>` يثبّت `telegram_id` للمستخدم في قاعدة المنصة فعلياً (تحقق مباشر: لاعب QA ربط بحساب اختبار؛ المعرّف محجوب).
- `/balance` من الشات المربوط يعرض الكوينز وما يعادلها بالدولار.
- إيداع/سحب/رفض يُخطر المستخدم على تيليغرام عبر الربط نفسه.

### هاتف
- نافذة المحفظة: `z-index` 96 → **1200** (كان الهيدر 200 يغطي زر الإغلاق) — تحقق بمقاس 390×844: الزر ظاهر والنقر يغلق.

### الاختبارات
- `_cf_payments_test.js`: 46 → **60/60** (شرائح، جلسة سوبر، جسر المخططات، ربط تيليغرام).

---

## v2.40 (بطاقات الألعاب الرسمية + رموز QR الحقيقية + إثبات ملكية Cryptomus)

- **بطاقة الضومنة والطاولة**: صورة المستخدم الرسمية (`game-card-dominoes.png` /
  `game-card-backgammon.png`) صارت داخل `assets/games/dominoes/icon.webp` و
  `assets/games/backgammon/icon.webp` (512×512) + خلفيات المسرح
  (`background.webp` 1376×768 معتمة هادئة).
  إصلاح جانبي: `js/main.js` كان يطلب `icon.png` للعبتين — والملف غير موجود ⇒
  **طلب 404 على كل زيارة** ثم السقوط إلى webp. الآن webp أولاً (بلا 404) مع إبقاء png احتياطاً.
- **رموز QR حقيقية**: `assets/qr/binance-trc20.png` (رمز تطبيق Binance لعنوان
  USDT-TRC20 الفعلي) و`assets/qr/cashplus.png` (رمز تطبيق Cash Plus للرقم 0766672027).
  رمز CIH الحالي أُبقي (فُكّ وأُكِّد أنه يحمل IBAN الصحيح `MA64230815690408521101420024`).
- **صفحة الاسترداد**: قسم Cash Plus صار يعرض الرمز + بيانات الحساب؛ الرموز الثلاثة
  (Cash Plus/CIH/Binance) تُقرأ فعلياً بالكاميرا.
- **المحفظة**: اختيار أي وسيلة دفع محلية (Cash Plus / CIH / Binance) يعرض الآن
  **رمز QR مباشر** + بيانات الحساب كاملة (RIB/IBAN/SWIFT/الشبكة) داخل نافذة الشحن،
  بلا مغادرة المنصة — ومترجم بأربع لغات (`wl.scanQr`, `wl.transferTo`, `wl.network`, `wl.copy`).
- **إثبات ملكية Cryptomus**: `cryptomus_5bf79cae.html` في جذر الموقع
  (يُخدَم على `https://dtsg.pages.dev/cryptomus_5bf79cae.html` للتحقق الآلي).
- **إصلاح نشر مهم**: `refund-policy.html` كانت **غائبة عن `scripts/deploy-pages.sh`**
  رغم أنها مرتبطة من المحفظة والقوائم — أُضيفت، مع `payments-url.json` وملف Cryptomus.
- **`payments-url.json`** صار يشير إلى الووركر الوسيط `casino-phone.dmgames-api.workers.dev`
  (نفس مصدر الحقيقة للـAPI) بدل قيمة فارغة كانت تجعل المحفظة تطلب من Pages
  فتحصل على 404. + قاعدة بلا كاش في `_headers`.
- **ملف واجهة جديد**: `js/games/catalog.js` بلا تغيير؛ التعديلات: `js/main.js`،
  `js/wallet.js`، `js/i18n/translations.js`، `css/20-wallet.css`، `_headers`،
  `refund-policy.html`، `scripts/deploy-pages.sh` + أصول جديدة. رموز كاش: `pay2` / `dtsg8`.
- **اختبار جديد**: `tests/_verify_v240.js` — 17 فحصاً (البطاقتان تُحمَّلان فعلياً 512×512 ·
  الرموز الثلاثة تُخدَم وتُعرض في الصفحة والمحفظة · ملف Cryptomus 200 ويحوي المفتاح · صفر أخطاء JS).
- `package.json` → 2.40.0 · بصمة البناء في `js/main.js` → `v2.40.0`.

### v2.40 — إصلاحان في المدفوعات (اكتُشفا باختبار دورة كاملة على الخادم)

1. **سحب Binance كان يفشل كلياً**: عمود `method` في جدول المدفوعات المحلي
   (`pay_transactions`) كان يقبل `('sellix','cryptomus','cih','orange_money','cash_plus','voucher')`
   فقط — والمحفظة تعرض Binance كوسيلة حيّة ⇒ كل طلب سحب Binance كان يُرفض بـ
   `CHECK constraint failed`. أُضيف `binance` إلى المخطط + **ترحيل تلقائي** يعيد بناء
   الجدول وينقل صفوفه عند الإقلاع (يظهر في السجل: `[payments] migrated pay_transactions.method → +binance`).
2. **إيداع Binance اليدوي (TXID) كان مرفوضاً**: `/api/payments/p2p` كان يقبل
   `cash_plus/cih/orange_money` فقط ⇒ أُضيف `binance` (نفس مسار المراجعة اليدوية بالأزرار على تيليغرام).
3. `cf-worker/schema.sql` (مسار D1) حُدّث لنفس القائمة.
4. اختباران: `tests/_pay_methods_v240_test.js` (**11/11**) · `_cf_payments_test` ما زال **60/60**.
5. **`scripts/update-phone-server.sh`** (جديد): تحديث خادم الهاتف بخطوة واحدة —
   جلب الشيفرة، نسخة احتياطية للقاعدة، إعادة تشغيل pm2، ثم تحقق فعلي من `/api/payments/*`.

## ملاحظات التشغيل (إلزامية للإنتاج)

1. **الخادم الخلفي يجب أن يُحدَّث إلى v2.37**: الموقع المنشور يمرر `/api/*` إلى نسخة `server.js` قديمة؛ حدّثوها من مستودع `tarikchouika/DTSG` (git pull بعد تشغيل الناقل) وأعيدوا التشغيل بمتغيرات البيئة:
   `TELEGRAM_BOT_TOKEN`، `TELEGRAM_ADMIN_CHAT_ID=<من البيئة فقط>`، `TELEGRAM_ADMIN_PIN`، `ADMIN_API_SECRET`، `CRYPTOMUS_MERCHANT_ID`، `CRYPTOMUS_PAYMENT_KEY`، `USD_GOLD_RATE=100`، `DEPLOY_MANIFEST=1`.
2. **Webhook البوت** (من جهاز لديه إنترنت):
   `curl -s "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://<مضيف-الخادم>/api/telegram/webhook"`
3. التوكنات بيئة فقط — لا تُودَع في المستودع أبداً؛ يُنصح بتدوير أي توكن ظهر في محادثة.

## v2.38 (واجهات الإعدادات + صفحة الاسترداد)

- **إزالة خانة مبلغ الرهان وإشارات المضاعف** من إعدادات اللعب ضد الآلي/وجهاً لوجه في:
  فلات دوغ (حُذفت شارات ×2/×3 وشريطا الرهان)، الروندا، الرامي، البارتشي، البلياردو (5)، الدومينو، الطاولة.
  الرهان يُحدَّد حصرياً من فاتح الغرفة في إعدادات الغرف أونلاين؛ المحلي تدريب مجاني مع تلميح موحّد.
- **توحيد احترافي لخانات نوافذ الإعدادات** (كتلة GSet-Unify في 03-components.css): ألوان ذهبية/كحلية
  موحّدة للخانات والقوائم وأزرار الشرائح مع حالات focus/hover، وملاءمة مريحة للبورتريه واللاندسكيب.
- **نافذة قواعد فلات دوغ**: توضيح القواعد خطوة بخطوة وإزالة تعليمات الرهان والمضاعفات؛ جدول الأوضاع
  صار يشرح النمطين بدل المضاعفات.
- **صفحة الاسترداد**: قسمان جديدان — تحويل بنكي CIH/CIH Express وBinance (TRC20) مع **رموز QR**
  مولّدة من بيانات الحساب (assets/qr/)، وتحديث جدول الملخص ووسائل السحب.
- المحفظة: طريقة **Binance (TRC20)** تظهر live بعنوان الشبكة.
- اختبارات: parchisi_ui 32/32 · parchisi_browser كلها ✓ · rd_classic 44/44 · cf_payments 60/60.

## v2.39 (ترجمة كاملة + توحيد بأسلوب روندا + مواصفة للمساعدات)

- **صفحة الاسترداد متعددة اللغات بالكامل** (ar/fr/en/da): كل الفقرات والخطوات
  وصفوف الجدول وعناوين الحسابات عبر مفاتيح `data-k` في قاموس `D` — لا خلط لغوي
  بأي اتجاه (تحقق: EN بلا عربية، AR بلا إنجليزية).
- **المحفظة مترجمة**: تبويبات شحن/سحب/سجل، التسميات، الملاحظات، أسماء الوسائل
  (مفاتيح `wl.*` في translations.js) + `translateStatic()` فور بناء النافذة ديناميكياً.
- **توحيد نوافذ الإعدادات بأسلوب بطاقة روندا الكلاسيكية** (كتلة GSet-Ref v2.39):
  بطاقة حقل بحد ذهبي + عنوان ذهبي بخط متلاشٍ + زر بدء بتدرج ذهبي + حد داخلي
  مزدوج للحاوية — مطبق على الضاما/الشطرنج/البلياردو/البارتشي/الرامي/الدومينو/الطاولة
  مع الحفاظ على خلفية كل لعبة (لبدة، جوز…).
- **تلميحات «تدريب مجاني» مترجمة** عبر `data-i18n="ui.trainingFree"` ومفاتيح قصيرة
  (`ui.freeShort`/`ui.freeSide`) بدل النصوص المعلّبة.
- **docs/UI-SETTINGS-SPEC.md**: مواصفة مرجعية ملزمة للمساعدات الآلية القادمة
  (المرجع البصري، قاعدة الرهان، قاعدة الترجمة، فحوص ما قبل الالتزام).
- اختبارات: cf_payments 60/60 · frontend_tx 26/26 · rd_classic 44/44 · parchisi_ui 32/32.
