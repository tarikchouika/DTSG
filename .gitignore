# Dependencies
node_modules/

# SQLite — بيانات تشغيل (يخلق المخطط تلقائياً server.cjs عبر CREATE TABLE IF NOT EXISTS)
*.db
*.db-wal
*.db-shm

# Local runtime
server.log
.wrangler/
.dev.vars

# لقطات الاختبارات تُكتب في /tmp/dtsg-shots (ليست جزءاً من المستودع)
screenshots/
shot_*.png
_shot_*.png

# [v2.44] مخرجات تشغيل محلية (بوت الفوتشير/قاعدة الاختبار)
data/

# [v2.45] مجلد نشر البوت المحلي (يحتوي .env وقاعدة محلية) — لا يُرفع
telegram-voucher-bot/
